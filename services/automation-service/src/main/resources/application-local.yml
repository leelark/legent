spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:postgresql://localhost:5432/legent_automation
    username: legent
    password: legent

  kafka:
    bootstrap-servers: localhost:9092
