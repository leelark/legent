# Release History

Last updated: 2026-05-18.

- No release performed.
- 2026-05-18 admin/settings/content runtime refinement and validation: no release, commit, push, or deployment performed. Admin Control Plane and Enterprise Settings were hardened and aligned with the existing workspace design: safer admin polling, idempotent role seeding, settings load/save fail-closed behavior, response alias normalization, direct deliverability settings coverage, and foundation admin/global service guardrails. A runtime content-service scheduled purge error was also fixed by making the rendered snapshot bulk delete transactional at the repository boundary. Validation passed for full release gate after the content fix, frontend lint/build, admin E2E, smoke E2E, visual smoke, sanitizer E2E, full backend Maven clean package, foundation-service full tests, content-service full tests, release evidence validator self-test, repository artifact hygiene, route-map validation, `.env.example` validation, Docker Compose config, production overlay validation, Kustomize render, Compose build/up/health, Compose-host smoke E2E on port 3003, frontend/gateway/Postgres probes, fresh Compose log scan, and `git diff --check`. Containers were stopped after validation and `docker compose ps` showed no running services. Remaining release blockers are external evidence: digest-pinned registry provenance/SBOM/signature, reviewed production egress proof, GA evidence pack, live scale/restore/security/monitoring transcripts, and Audience V17 operator-reviewed production mapping.
- 2026-05-18 final Salesforce comparison implementation validation: no release, commit, push, or deployment performed. Local implementation completed for artifact hygiene, delivery content fail-closed behavior, missing-scope Kafka consumer retry/DLQ surfacing, frontend explicit-any cleanup to zero lint warnings, dead-code/export cleanup, and tracking-service Compose runtime startup. Validation passed for full `.\mvnw.cmd test`, `.\mvnw.cmd -DskipTests package`, frontend `npm run lint`, `npx tsc --noEmit --pretty false`, `npm run build`, Playwright smoke/sanitizer, compose frontend smoke, `docker compose build`, `docker compose up -d`, `validate-compose-health.ps1`, Postgres readiness, gateway health, route map, `.env.example`, Docker Compose config, production overlay validation, Kustomize render, artifact hygiene, and `git diff --check`. Remaining release blockers are external evidence only: reviewed production egress target data/application proof, GA evidence pack, registry digest/SBOM/signature/provenance proof, live load/restore/security/monitoring transcripts, and Audience V17 operator-reviewed production mapping export.
- 2026-05-18 Salesforce comparison audit implementation sweep: no release, commit, push, or deployment performed. Locally actionable report items were implemented: artifact hygiene false positive, delivery content fail-closed behavior, missing-scope Kafka consumer retry/DLQ surfacing across changed services, frontend explicit-any reduction, and confirmed dead-code/export cleanup. Validation passed for artifact hygiene self-test/validator, integrated focused Maven sweep across changed backend modules, frontend lint with 131 remaining warnings and 0 errors, frontend TypeScript no-emit, and `git diff --check`. Superseded later on 2026-05-18 by the final validation entry above, which closed remaining frontend warnings and local runtime proof. External blockers remain production egress evidence, GA evidence pack, registry image provenance/SBOM/signature evidence, live load/restore/security/monitoring transcripts, and Audience V17 operator-reviewed production mapping.
- 2026-05-18 frontend debt slice: core platform API wrapper explicit-any cleanup completed in `frontend/src/lib/core-api.ts` with backend-aligned request DTOs and unknown-safe dynamic response records. Not released; validation passed for `npm run lint` with warnings reduced from 263 to 218 and `npx tsc --noEmit`.
- 2026-05-18 Salesforce comparison full-repository audit refresh: generated `.codex/reports/salesforce-comparison-full-audit-2026-05-18.yaml` with official Salesforce baseline links, whole-tree metrics, module/feature/algorithm scores, bug and risk register, live-readiness decision, Salesforce-parity roadmap, and best-in-world roadmap. No release, commit, push, or deployment performed. Validation passed for full Maven tests, frontend lint/build, Playwright smoke/sanitizer suites, npm audit, route map, `.env.example`, Docker Compose config, production overlay validation, and YAML parsing. Artifact hygiene validation failed from a self-match false positive in `scripts/ops/validate-repo-artifact-hygiene.ps1`; public multi-tenant GA remains no-go until that and external evidence blockers are closed.
- 2026-05-18 Release-blocker source closure: production egress can now render reviewed CIDR/Cilium FQDN policy artifacts; GA/image evidence validators require live transcripts plus registry digest/SBOM/signature/provenance evidence; shared Kafka validates six high-risk outbound event contracts; campaign sends use row-backed recipient paging for new batches instead of active/retry JSON rewrites. Not released; affected backend Maven sweep, evidence validator self-test, image template validation, production overlay validation, route validation, Docker Compose config, frontend lint, and diff whitespace check passed. External evidence blockers remain target egress data, live GA transcripts, target cluster proof, and registry evidence.
- 2026-05-18 Worker A production evidence source-side hardening: added reviewed production egress policy rendering from validated evidence JSON, a GA evidence manifest template generator, a standalone rendered-image evidence validator, and docs/templates for GA/image evidence. GA evidence validation now requires registry image SBOM evidence, and release gate image evidence mode chains the stricter rendered-image/SBOM/signature/provenance validator. Not released; focused PowerShell validations passed for temporary reviewed egress evidence rendering, image template tie-back, a complete GA evidence fixture, expected placeholder template failures, expected strict image template failure, and `git diff --check` with CRLF warnings only. External blockers remain exact production CIDR/FQDN/CNI evidence, target cluster policy application proof, digest-pinned registry images, registry SBOM/signature/provenance proof, and live GA transcripts.
- 2026-05-17 five-loop caveman/cavecrew local hardening sweep: no release, commit, push, or deployment performed. Source changes were limited to local hygiene/guards, frontend routing/context, docs, and `.codex` memory/report alignment. Validation passed for repo artifact hygiene, release evidence validator self-test, frontend lint with existing `no-explicit-any` warnings, targeted Playwright `/launch` redirect test, `.env.example` validation, route-map validation, Docker Compose config, and production overlay validation with the expected image-digest warning. External GA blockers remain production egress evidence, GA evidence pack, live high-volume load, restore/security/monitoring transcripts, and registry image provenance.
- 2026-05-17 current `.codex`/codebase hardening sweep: identity OIDC federation outbound fetches now use public HTTPS issuer-origin pinning plus bounded HTTP response/timeouts; credentialed CORS wildcard config is rejected and unsafe cookie-auth writes cannot pass through literal `*`; content internal render token comparison uses the shared validator; MinIO Compose credentials are required and env validation includes them; high-volume send pipeline Kafka topics have explicit local partition definitions; delivery retry polling is bounded and inbox safety SQL failures fail closed; `.codex` command/report docs and Makefile Kafka UI port drift were corrected. Not released; combined focused Maven sweep passed for shared Kafka/security, identity, content, and delivery; `.env.example` validation and `docker compose --env-file .env.example config --quiet` passed; `git diff --check` passed with CRLF warnings only. Residual external release blockers remain unchanged, and tracked `id_log.txt` still needs explicit approval before generated-log removal/scrubbing.
- 2026-05-17 Salesforce comparison implementation pass: locally actionable source blockers from `.codex/reports/salesforce-comparison-full-audit-2026-05-17.yaml`, `unresolved-risks.md`, `security-findings.md`, `technical-debt.md`, and `root-cause-history.md` were implemented. Changes cover campaign signed service JWT readiness auth, centralized internal-token placeholder rejection, content-service durable rendered snapshots and internal routes, required-reference snapshot writes without optional per-recipient default-on mode, delivery tenant/workspace/reference retry recovery with Redis-miss fallback and bounded cache, automation atomic resume locks, and bounded multi-page Data Extension preview scanning. Not released; focused backend Maven gate passed with 80 tests across shared common/security/cache and affected audience/content/campaign/delivery/deliverability/automation modules. Env, Compose, route-map, and production overlay validators passed; production overlay still warns image digests/provenance evidence is absent. Full Maven reactor, frontend build/lint/E2E, strict release gate, and live load/restore/monitoring/security evidence were not run/available.
- 2026-05-17 Salesforce comparison full-repository audit: generated `.codex/reports/salesforce-comparison-full-audit-2026-05-17.yaml` after automated whole-tree scan, targeted high-risk manual source review, official Salesforce baseline review, frontend lint, focused Maven compile/tests for campaign/delivery/automation, route-map validation, Compose config validation, production overlay validation, and `.env.example` placeholder validation. No release; original report rated Legent at 64% Salesforce parity and 46% public multi-tenant GA readiness, and found P1 source blockers for campaign service-to-service readiness auth and delivery retry content recovery. Superseded later on 2026-05-17 by the implementation pass above; external live scale, production evidence, and Audience V17 mapping blockers remain.
- 2026-05-16 release-blocker evidence gates: production external egress, Audience V17 mapping, live GA evidence, tracking/campaign scale proof, and image supply-chain proof now have fail-closed validators or strict release-gate hooks. New tooling includes production egress evidence JSON validation, audience mapping CSV/JSON validation and SQL generation, GA evidence manifest validation, live-load evidence intake, and strict image digest/evidence manifest checks. Not released; local parser/focused validations and expected strict failures passed.
- 2026-05-16 platform workspace search RBAC split: `/api/v1/platform/search` now accepts workspace users with `search:read` or `workspace:read` while `/api/v1/admin/search` remains `ADMIN`/`PLATFORM_ADMIN` only, and frontend context preflight requires workspace context for search. Not released; focused platform Maven tests and frontend API-context Playwright passed.
- 2026-05-16 delivery/deliverability RBAC hardening: provider admin APIs now require delivery read/write permissions and sender-domain management APIs require deliverability read/write permissions. Not released; focused RBAC tests and combined Maven validation passed.
- 2026-05-16 campaign batch claim/retry hardening: batch execution now claims work atomically before processing and retry requeue fails closed if Kafka publish fails. Not released; `SendExecutionServiceTest` passed in focused and combined Maven validation.
- 2026-05-16 tracking Postgres raw-event retention execution: the tracking service now periodically calls the existing bounded `purge_expired_raw_events(batch_size)` function with a configurable enable flag, interval, and batch size, capped at 50,000 rows per run, plus purge metrics. Not released; focused retention Maven tests passed with 3 tests.
- 2026-05-16 shared workspace-context hardening: authenticated tenant JWTs without a workspace claim can no longer adopt `X-Workspace-Id` at shared service ingress; callers must use an identity/foundation-minted workspace JWT, while internal non-JWT service calls retain explicit tenant/workspace header context. Not released; shared-security focused tests passed with 20 tests.
- 2026-05-16 RBAC/workspace authorization controller hardening: automation workflow/studio APIs now require explicit workflow read/write permissions, tracking analytics/export/funnel/segment and BI endpoints have explicit tracking/analytics read/write gates, and platform admin alias/config RBAC has focused regression coverage. Not released; automation/platform RBAC Maven tests passed and integrated tracking RBAC tests passed.
- 2026-05-16 infra/release evidence hardening: production overlay now renders TLS references, restricted Pod Security labels, and deployment security contexts; production validation checks TLS/restricted/alert rules and has optional strict image digest mode; CI now uploads filesystem SBOM and rendered image supply-chain checklist artifacts; operations docs now separate locally verifiable checks from external TLS/admission/observability/registry evidence. Not released; validation passed for production Kustomize render, production overlay validation, image checklist generation, and expected strict digest failure on unpinned `legent/*:1.0.2` images.
- 2026-05-16 tracking raw-event/outbox local hardening: Redis tracking dedupe now uses atomic reservation, ClickHouse raw writes and schema preserve workspace lineage, raw schemas have local retention/partition guardrails, and outbox publish duration/lease-utilization metrics are available. Not released; focused tracking Maven command passed with 14 tests. Residual release blocker: live ClickHouse ingest/load and TTL proof still required.
- 2026-05-16 deliverability sender-domain ownership hardening: sender-domain registration/challenge rotation now issues tenant/workspace-specific DNS TXT challenges, verification requires the exact owned token plus SPF/DKIM/DMARC, verification history is persisted, and readiness/risk outputs fail closed without fresh ownership proof. Not released; focused deliverability Maven tests passed.
- 2026-05-16 docs/scripts drift hygiene: CSRF docs now match unsafe cookie-auth Origin/Referer fail-closed behavior, Compose health memory now matches live Compose publisher port detection, and the generic docs/scripts drift risk was closed after validating release-gate, synthetic-smoke, backup-restore, load-harness, and Compose health command surfaces. Not released; validation passed for PowerShell parser checks, route-map validation, scoped `git diff --check`, and stale-phrase grep. Live target GA evidence and external egress blockers remain open.
- 2026-05-16 full-audit wave 2 worker follow-up: campaign content render calls now carry workspace/environment/request/correlation scope and fail closed on missing batch workspace, unused campaign content read helpers were removed, frontend session roles are authoritative for admin UI and `/app/tracking` redirects to analytics, unknown search results route to workspace home, and production overlay validation now requires observability alert rules rendered from Kustomize. Not released; validation passed for the combined focused Maven sweep over changed backend modules, full tracking-service Maven slice, touched frontend ESLint, Next build, focused Playwright suites, observability Kustomize render, production overlay validation, release-gate infra slice, `git diff --check`, and backup parser/ignore checks.
- 2026-05-16 tracking outbox reliability hardening: tracking outbox publish now uses a conditional DB claim before Kafka publish, reducing duplicate publishes from concurrent outbox pollers and preserving stale `PUBLISHING` recovery through the existing retry lease timestamp. Not released; validation `.\mvnw.cmd -pl services/tracking-service -am test` passed. Residual release risk remains for `raw_events` retention/partitioning, Redis dedupe atomicity, and exact-once behavior if publish exceeds the claim lease.
- 2026-05-16 full-audit wave 2: platform webhook retry stale-claim recovery, shared delivery/tracking URL signature contract, delivery suppression workspace/type/deleted filtering, frontend credentialed API URL/public contact/401 cleanup, and backup/env artifact hygiene were implemented. Not released; validation passed for platform retry focused tests, shared/delivery/tracking signature focused tests, delivery inbox safety tests, frontend API-context Playwright, frontend TypeScript no-emit, backup script parser/guard checks, git ignore checks, and `git diff --check`. Full release remains blocked by exact live external egress data, target-environment smoke/load/restore/security/monitoring evidence, shared workspace membership authorization, audience production mapping metadata, and newly recorded tracking/campaign/infra residuals.
- 2026-05-16 production-blocker wave: production broad egress inheritance removed from production overlay, campaign readiness gate now calls deliverability/delivery services for DNS/auth, suppression health, warmup, and provider capacity, delivery rate/warmup reservations are durable/idempotent DB leases, content/platform authenticated resources are workspace-scoped, and campaign large send payloads externalize to references when required. Not released; validation passed for combined backend Maven reactor tests, full `scripts\ops\release-gate.ps1`, route-map validation, production overlay validation, Docker Compose config, frontend lint/build/sanitize/smoke/visual inside release gate, `npm audit --omit=dev --audit-level=high`, and load harness dry-run. Live production blockers remain exact external egress policy data, live high-volume load, restore drill, CI gitleaks/Trivy transcript, monitoring handoff evidence, and target synthetic smoke.
- 2026-05-16 campaign send payload externalization hardening: campaign send execution now fails closed when enabled rendered content reference creation is unavailable, requires references when inline rendered content is disabled or inline payloads exceed the configured byte cap, emits reference-only Kafka payloads without rendered HTML/text in those modes, and delivery resolves cached rendered references before first-attempt placeholder fallback. Not released; validation passed for campaign reference/send tests and focused delivery orchestration tests.
- 2026-05-16 platform webhook DNS rebinding hardening: dedicated webhook `WebClient` wrapper validates connector-time resolved addresses and production profile direct webhook egress requires a configured outbound webhook proxy. Not released; validation passed for focused shared-common/platform webhook Maven tests and scoped `git diff --check`. Release operators must configure `WEBHOOK_OUTBOUND_PROXY_URL` before running platform webhooks in `prod`.
- 2026-05-16 full-audit parallel hardening wave: shared security Origin/Referer and tenant fallback hardening, frontend context enforcement, identity federation/SCIM boundaries, audience/delivery RBAC, campaign sender-domain launch blockers, tracking workspace-aware dedupe, and platform webhook retry bounded claiming were implemented. Not released; validation passed for `git diff --check`, shared-security focused tests, frontend API-context Playwright tests, frontend lint with existing `no-explicit-any` warnings, focused Maven tests, and `.\mvnw.cmd -pl services/identity-service,services/campaign-service,services/audience-service,services/delivery-service,services/tracking-service,services/platform-service -am test`. Superseded residuals on 2026-05-16: production broad egress inheritance, tenant/workspace schema decisions for content/platform, deliverability-service launch gating, atomic rate reservation, payload externalization, frontend build/E2E smoke, and release gate were addressed; live target evidence remains.
- 2026-05-16 final runtime validation fix: Docker frontend build timeout is configurable with a 30-minute default in `frontend/Dockerfile`, and Compose now injects the existing required internal API token env into content/campaign services. Not released; `.\mvnw.cmd -T1 package -DskipTests`, `docker compose build`, `docker compose up -d content-service campaign-service gateway`, Compose health validation, direct health probes, post-health critical log scan, Compose config, `.env.example` validation, route validation, and `git diff --check` passed. Superseded residual: production broad-egress render blocker was fixed later on 2026-05-16; no secret value was added to the repository.
- 2026-05-16 five-loop repository organization hardening: identity invitation secrets are no longer returned by create/list APIs, invitation TTL is bounded, identity duplicate JWT filtering is de-registered, content latest-version reads have RBAC, foundation core reads/writes are workspace/tenant scoped, platform webhook inputs are constrained, campaign rendered-content reference metadata is inline-safe/default-off, and delivery retries fail closed on missing rendered content. Not released; validation passed for final touched backend Maven reactor sweep, full serial Maven clean compile, full Maven test, frontend lint/build with existing lint warnings, `.env.example`, route validation, Docker Compose config, production Kustomize render, and `git diff --check`. Superseded residual: production overlay/release gate broad-egress blocker was fixed later on 2026-05-16; exact live external egress data still required.
- 2026-05-16 identity security hardening: identity-local JWT filter is no longer component-scanned, preventing duplicate filter execution from overwriting the shared security principal; user preference endpoints now consume the shared principal. Not released; focused Surefire reports show the new identity tests passed, but Maven wrapper commands timed out after reports were produced.
- 2026-05-16 platform webhook validation hardening: webhook create now rejects unsupported/malformed/excessive event subscriptions and weak or untrimmed secrets before persistence. Not released; focused platform webhook Maven tests passed. The later 2026-05-16 webhook DNS rebinding hardening entry closes the connection-time resolver residual.
- 2026-05-16 `.codex` pending sweep: backend retry/idempotency fixes, frontend search/mobile shell fixes, dead-code cleanup, and documentation/memory refresh completed. Not released; validation passed for affected backend Maven module sweep, frontend lint/typecheck/build, admin/smoke/visual Playwright, route-map validation, Compose config, and diff whitespace check. Release blockers remain production egress policy data, content/platform workspace-scope semantics, audience production mapping metadata, and real target-environment GA evidence.
- 2026-05-16 frontend UI alignment change: admin and settings workspace screens now use shared workspace chrome and no local animation layer, with updated admin/smoke/visual Playwright assertions. Not released; validation passed for focused ESLint, TypeScript no-emit, full frontend lint with existing unrelated warnings, production frontend build, admin E2E, smoke E2E, and visual smoke E2E. External release blockers remain unchanged.
- 2026-05-14 pending-sweep hardening change: caller-token relay for platform-to-foundation settings, no-form landing-page sanitizer/default UX, campaign send payload byte cap before Kafka publish, platform workspace/environment async/event propagation, README/API-contract cleanup, and dead foundation helper removal. Not released; validation passed for focused content/platform/campaign Maven tests, foundation compile, frontend sanitizer Playwright, frontend lint/typecheck, route validation, and `.env.example` validation. Residual release blockers remain production egress policy data, content/platform workspace schema semantics/mapping, campaign rendered-content externalization, and audience production mapping metadata.
- 2026-05-14 final validation sweep: full `.\mvnw.cmd clean compile`, full `.\mvnw.cmd test`, `.\mvnw.cmd verify`, frontend `npm run lint`, `npx tsc --noEmit`, `npm run build`, Playwright sanitizer/smoke/visual, `docker compose config --quiet`, `docker compose build`, `docker compose up -d`, `validate-compose-health.ps1`, direct gateway/frontend health probes, recent Compose log error scan, route validation, production Kustomize render, `.env.example` validation, frontend production audit, and load harness dry-run passed. `npm ci` installed/audited dependencies with 0 vulnerabilities but hit the 15-minute tool timeout after completion text. Release-gate infra slice fails only at production overlay drift checks because broad inherited egress is intentionally blocked pending reviewed CIDR/FQDN policy data. No release, commit, or push performed.
- 2026-05-14 five-loop release-hardening change: delivery/tracking event scope mismatches now fail closed, pending idempotency claims can retry, platform config/content publishing gained RBAC, webhook secrets no longer serialize in responses, landing-page forms are inert by sanitizer policy, load/release/route/production validators fail closed on drift, and all service Flyway defaults are safe by default. Not released; focused backend/frontend/infra validations passed where run. Superseded residual: inherited broad egress was deleted from the production overlay on 2026-05-16.
- 2026-05-14 release-hardening change: audience data-extension legacy workspace mapping now requires reviewed operator metadata before production upgrades can pass V17; clean installs remain unaffected. Not released; validation `.\mvnw.cmd -pl services/audience-service -am test` passed. Residual: V17 validates reviewed mapping metadata, not target workspace existence outside the audience DB.
- 2026-05-13 orchestration setup is repo-process only; no product code shipped.
- 2026-05-13 release-hardening change: non-test service/Kubernetes base JPA DDL defaults now validate instead of update. Not released; validation limited to `rg` config check and `docker compose config --quiet`.
- 2026-05-13 release-hardening change: local Compose health validation now aligns with frontend host port and Redis optional auth. Not released; validation limited to parser and Compose config checks.
- 2026-05-13 security-hardening change: content-service HTML sanitization now uses jsoup DOM parsing with explicit allowlist policy and focused bypass tests for script/event attributes, encoded JavaScript URLs, form submit URL attributes, `srcset`, SVG/MathML, and unsafe CSS URLs. Not released; validation limited to `.\mvnw.cmd -pl services/content-service -Dtest=EmailContentValidationServiceTest test`.
- 2026-05-13 security-hardening change: Kafka JSON deserialization trust narrowed in shared config and service YAMLs. Not released; validation limited to shared Kafka Maven tests and grep scans.
- 2026-05-13 security-hardening change: shared `TenantFilter` rejects workspace/environment header conflicts against JWT context and preserves authenticated context when headers are omitted. Not released; validation limited to shared security Maven tests.
- 2026-05-13 parallel hardening change: audience resolved events are chunked, high-volume Kafka publisher keys avoid tenant fallback, and delivery send-request consumer failures rethrow after logging. Not released; validation limited to integrated Maven test for shared Kafka/security plus audience/delivery services.
- 2026-05-13 reliability-hardening change: shared Kafka listener error handling now publishes exhausted retries to declared central `kafka.dead-letter`, and service Kafka consumers rethrow unexpected processing failures after logging while preserving explicit invalid/malformed-event drops. Not released; validation limited to impacted Maven tests.
- 2026-05-13 audience-service hardening change: audience resolution now pages all-subscriber candidates by keyset cursor, checks suppressions per page, publishes chunks with one-pass lookahead last-chunk semantics, and fails closed by rethrowing suppression/client/publish failures. Not released; validation limited to impacted Maven tests.
- 2026-05-13 production-infra hardening change: production overlay renders pinned `legent/*:1.0.2` images, production ingress hosts, `CLICKHOUSE_DB=legent_analytics`, and `/ws/analytics` routing to tracking-service; release gate now runs production overlay drift checks. Not released; validation limited to route map, production overlay, and Kustomize release-gate slice.
- 2026-05-13 tracking/frontend integration hardening change: authenticated analytics WebSocket now preserves workspace context across refresh and uses API-base WebSocket origin; production CORS allows workspace/environment headers. Not released; validation limited to focused Maven tests and frontend lint.
- 2026-05-13 final validation change: local runtime health validation and gateway WebSocket route were fixed after full Compose startup exposed drift. Not released; validation passed across full Maven clean compile/test/package, frontend lint/build/Playwright smoke, Docker Compose build/start/health, route validation, production overlay validation, release-gate infra slice, Nginx config test, and recent log scan.
- 2026-05-13 release-hardening change: resumed Loop 1 fixed audience LIST/SEGMENT keyset paging, identity workspace-token membership validation, route/controller drift, frontend/backend HTML sanitizer contracts, and GA evidence documentation. Not released; validation so far is focused tests/lint/route/infra checks, with broader Loop 2-5 validation still pending.
- 2026-05-13 release-hardening change: production ingress regex no longer lets broad foundation public/admin routing shadow content landing-page or platform admin webhook/search routes, and route validation now models rendered ingress precedence. Not released; validation limited to route validation and production Kustomize render.
- 2026-05-13 release-hardening change: resumed Loop 2 fixed delegated-subject workspace validation, CSS escape sanitizer bypass, frontend sanitizer TypeScript build issue, and audience candidate repository test coverage. Not released; touched Maven slice, frontend build, route validation, Compose config, production Kustomize render, and diff check passed.
- 2026-05-13 security-hardening change: public landing-page sanitizers now strip form `action` and button `formaction` attributes in frontend rendering and backend content sanitization, failing closed until a controlled submission endpoint policy exists. Not released; validation limited to frontend TypeScript check and focused content-service test.
- 2026-05-13 Loop 3 hardening change: audience data extensions are now workspace-scoped, audience candidate paging has supporting indexes, completed campaign batches clear retained payload JSON, jsoup dependency version is centralized, and production overlay validation fails closed on broad inherited egress. Not released; focused Maven tests passed, and production overlay validation intentionally fails until a reviewed egress allowlist is available.
- 2026-05-13 Loop 4 infra/config validation change: production frontend ingress is explicitly allowed from ingress-nginx, tracking ingress is split away from the shared API 100r/s annotation, route-map parent shadowing is rejected, and Compose gateway API health validation uses `/api/v1/health`. Not released; route validation, Compose config, and production Kustomize render passed, while production overlay validation intentionally fails on broad inherited egress.
- 2026-05-13 Loop 4 foundation workspace-scope hardening change: public content admin update/publish now require matching tenant/workspace, and workspace enforcement extends to performance-intelligence/global/differentiation/compliance foundation routes plus existing workspace-aware request models. Not released; focused foundation Maven slice passed with 17 tests.
- 2026-05-13 Loop 4 campaign/identity/audience/frontend hardening change: campaign handoff failures now retry/DLQ instead of being swallowed, delegation validates requester workspace membership, audience data-extension migration preflights ambiguous data, frontend context hydration preserves workspace choice, and sanitizer Playwright regressions cover dangerous public HTML vectors. Not released; touched Maven slice, frontend lint/typecheck, route validation, Compose config, Kustomize render, and sanitizer Playwright passed.
- 2026-05-13 Loop 5 release/security hardening change: audience idempotency now rolls back on listener/publish failure; campaign idempotency is completion-based with awaited critical publishes and terminal failure reconciliation; production ExternalSecret runtime keys render and are validated; public optional-auth routes tolerate stale cookies while protected paths still 401; content public landing read is explicitly public; SSO/SCIM are tenant-header free; identity drops unvalidated client environment context; foundation dynamic helpers use workspace-scoped ID/update paths. Not released; focused Maven slices, frontend lint/typecheck, production Kustomize render, and production overlay validation to the known egress blocker passed.
- 2026-05-13 final validation sweep: full `.\mvnw.cmd clean compile`, full `.\mvnw.cmd test`, `.\mvnw.cmd verify`, frontend `npm ci`, `npm run lint`, `npx tsc --noEmit`, `npm run build`, Playwright smoke, Playwright visual smoke, sanitizer regression rerun with web-server skip, `docker compose config --quiet`, `docker compose build`, `docker compose up -d`, `validate-compose-health.ps1`, Nginx `nginx -t`, direct gateway/frontend health probes, route validation, Kustomize production render, and post-health log scan passed. Release-gate infra slice fails only at production overlay drift checks because broad inherited egress is intentionally blocked pending reviewed CIDR/FQDN policy data. No release, commit, or push performed.
