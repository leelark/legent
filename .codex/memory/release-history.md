# Release History

Fresh baseline date: 2026-05-20.

No product release entries exist in the fresh memory baseline.

Current release posture:
- Production release remains blocked without target-environment evidence.
- The `.codex` rebuild is an internal orchestration and documentation change, not a product release.
- 2026-05-22 local change set: reviewed AI content assistance evidence can now be applied to content drafts only, storing hash/reference metadata and blocking unresolved AI assistance metadata before publish or test-send. Validation passed: focused `AiContentAssistanceGovernanceServiceTest` with 10 tests, focused `TemplateWorkflowServiceTest,TemplateTestSendServiceTest` with 5 tests, full `.\mvnw.cmd -pl services/foundation-service,services/content-service -am test` with 59 target-service tests plus upstream shared modules, repo artifact hygiene, and scoped `git diff --check`. This is not provider integration, AI parity, or production evidence; cross-service audit verification, model calls, generated-draft UX, metering, and target release gates remain separate.
- 2026-05-22 local change set: latest audit safe local follow-ups completed alert runbook coverage, `.env.example` Compose validation, delivery legacy workspace guard, identity forgot-password scope resolution, tracking publisher cleanup, SES region regression coverage, subscriber bulk/query E2E, and landing-page public/studio E2E. Validation passed: focused delivery tests, focused identity tests, focused tracking tests, full `.\mvnw.cmd -pl services/delivery-service,services/identity-service,services/tracking-service -am test` with 95 target-module tests plus shared modules, `cd frontend; npm run lint`, `cd frontend; npm run build:ci`, focused Chromium subscriber/landing-page Playwright, sanitizer Playwright, production overlay validation, release evidence validator self-test, `docker compose --env-file .env.example config --quiet`, local-only release gate, repo artifact hygiene, Codex validation, and `git diff --check` with CRLF warnings only. This is not production promotion evidence; target migration review, release artifacts, provider capacity, monitoring handoff, load, restore, CI/security, TLS/admission, and external egress evidence remain required.
- 2026-05-22 local change set: audience tracking intelligence `tracking.ingested` consumption now uses a dedicated audience batch listener with bounded poll records, batch ack, invalid/unsupported event skip, in-batch duplicate skip, and retry on service failures. Validation passed: focused `AudienceIntelligenceConsumerTest,AudienceKafkaConsumerConfigTest,SubscriberIntelligenceServiceTest` with 14 tests, full `.\mvnw.cmd -pl services/audience-service -am test` with 148 audience-service tests plus upstream shared modules, repo artifact hygiene, Codex validation, and scoped `git diff --check`. This is not production throughput evidence; target Kafka lag, database contention, and subscriber-intelligence metrics remain separate.
- 2026-05-22 local change set: route-map validation now proves every route-map prefix is first-matched by Kubernetes ingress to the expected backend service. Validation passed: `scripts\ops\validate-route-map.ps1`, a temporary missing-prefix ingress fixture failed as expected for `/api/v1/templates`, production Kustomize render, production overlay validation, repo artifact hygiene, Codex validation, and scoped `git diff --check`. This is not target ingress-controller proof or production promotion evidence.
- 2026-05-22 local change set: workspace context fail-closed E2E coverage now proves a session without workspace context clears local user/role/tenant/workspace/environment context and redirects to login. Validation passed: frontend lint, frontend production build, targeted Chromium `context-bootstrap.spec.ts` with the mocked fail-closed case passing and the credential-gated login smoke skipped, repo artifact hygiene, Codex validation, and scoped `git diff --check`. This is not production auth evidence; target session behavior and deployment gates remain separate.
- 2026-05-22 local change set: protected content-service GET endpoints now require method-level content read/template authorization, and controller reflection tests enforce read RBAC with explicit public/internal exceptions. Validation passed: focused `ContentControllerRbacTest` with 7 tests, full `.\mvnw.cmd -pl services/content-service -am test` with 54 content-service tests plus upstream shared modules, repo artifact hygiene, Codex validation, and scoped `git diff --check`. This is not production promotion evidence; target auth, edge, and release evidence remain separate.
- 2026-05-22 local change set: scheduled campaign send processing now installs tenant/workspace context from the claimed job row before publishing audience-resolution requests. Validation passed: focused `SchedulingServiceTest,CampaignEventPublisherTest` with 7 tests, full `.\mvnw.cmd -pl services/campaign-service -am test` with 107 campaign-service tests plus upstream shared modules, repo artifact hygiene, Codex validation, and scoped `git diff --check`. This is not production send or throughput evidence; target multi-replica scheduler, provider, queue-lag, and release evidence remain separate.
- 2026-05-22 local change set: strict release-gate mode now rejects `-SkipBackend`, `-SkipFrontend`, `-SkipCompose`, and `-SkipKustomize` unless `-LocalOnly` is present. Validation passed: release evidence validator self-test with the strict-skip negative fixture, local-only release gate with all skip flags, repo artifact hygiene, and scoped `git diff --check`. This is not production promotion evidence; strict release still requires real target evidence and full unskipped local gates.
- 2026-05-22 local change set: frontend audience import status lifecycle hardening passed frontend lint, frontend production build, targeted Chromium Playwright `audience-imports.spec.ts` with 6 tests, repo artifact hygiene, Codex validation, and scoped `git diff --check`. This is not a production release or import throughput proof; backend worker/runtime and target storage evidence remain separate requirements.
- 2026-05-22 local change set: production alert runbook link validation passed production overlay validation, negative missing-runbook and missing-anchor fixtures, local-only release gate with backend/frontend/compose/Kustomize skips, repo artifact hygiene, Codex validation, and scoped `git diff --check`. This is not production promotion evidence; target alert firing, routing, dashboard, and on-call handoff evidence remain required.
- 2026-05-22 local change set: audience subscriber by-ID lookup scoping passed focused `SubscriberServiceTest` with 9 tests, full `.\mvnw.cmd -pl services/audience-service -am test` with 142 audience-service tests plus upstream shared modules, repo artifact hygiene, Codex validation, and scoped `git diff --check`. This is not a production release; target import/load and promotion evidence remain separate requirements.
- 2026-05-22 local change set: GA and image evidence validators now require manifest `schemaVersion` 1 and the release evidence self-test covers missing and unsupported GA/image schema versions. Validation passed: `scripts\ops\test-release-evidence-validators.ps1 -ChildProcessTimeoutSeconds 120`, local-only release gate with backend/frontend/compose/Kustomize skips, repo artifact hygiene, Codex validation, and scoped `git diff --check`. This is not production promotion evidence; strict release remains blocked without target egress, image, GA, load, restore, CI/security, TLS/admission, and monitoring artifacts.
- 2026-05-22 local change set: automation delayed-resume workspace scoping passed focused `WorkflowEngineTest,WorkflowQuartzJobTest` with 9 tests, full `.\mvnw.cmd -pl services/automation-service -am test` with 82 automation-service tests plus upstream shared modules, repo artifact hygiene, Codex validation, and scoped `git diff --check`. This is not a production release; target scheduler/runtime evidence remains part of strict promotion.
- 2026-05-22 local change set: deliverability internal suppression security-chain coverage passed focused `SecurityConfigTest` with 5 tests, full `.\mvnw.cmd -pl services/deliverability-service -am test` with 64 deliverability-service tests plus upstream shared modules, route-map validation for 49 routes and 8 source-discovered internal routes, repo artifact hygiene, Codex validation, and scoped `git diff --check`. This is not a production release; target auth/edge evidence remains part of strict promotion.
- 2026-05-22 local change set: identity invitation list workspace scoping passed focused identity service/controller tests with 32 tests, full `.\mvnw.cmd -pl services/identity-service -am test` with 51 identity-service tests plus upstream shared modules, repo artifact hygiene, Codex validation, and scoped `git diff --check`. This is not a production release; any tenant-global invitation administration remains a separate privileged product/security design.
- 2026-05-22 local change set: audience import row-error retention cap passed focused import tests with 9 tests, full `.\mvnw.cmd -pl services/audience-service -am test` with 138 audience-service tests plus upstream shared modules, repo artifact hygiene, and scoped `git diff --check`. This is not a production release or import-load proof; target import-load evidence and promotion gates remain required.
- 2026-05-21 local change set: campaign send-job checkpoint workspace scoping passed focused checkpoint/send-execution/consumer/scheduler tests with 54 tests, full `.\mvnw.cmd -pl services/campaign-service -am test` with 106 campaign-service tests plus upstream shared modules, repo artifact hygiene, JPA config scan, and scoped `git diff --check`. This is not a production release; target Flyway migration application, legacy checkpoint/resume row ownership review, and promotion evidence remain required.
- 2026-05-21 local change set: deliverability legacy reputation scoping passed focused reputation/RBAC tests with 19 tests, full `.\mvnw.cmd -pl services/deliverability-service -am test` with 59 deliverability-service tests plus upstream shared modules, repo artifact hygiene, JPA config scan, and scoped `git diff --check`. This is not a production release; target Flyway migration application, legacy-row ownership review, and promotion evidence remain required.
- 2026-05-21 local change set: route internal-deny source discovery passed `scripts\ops\validate-route-map.ps1` for 49 route-map entries and 8 source-discovered service-internal routes, negative temporary-config checks for missing public-edge coverage failed as expected, repo artifact hygiene passed, and scoped `git diff --check` passed. This is not a production release; target ingress-controller snippet behavior and strict promotion still require external evidence.
- 2026-05-21 local change set: audience internal security-chain coverage passed focused `SecurityConfigTest` with 5 tests, full `.\mvnw.cmd -pl services/audience-service -am test` with 136 audience-service tests, repo artifact hygiene, Codex validation, and scoped `git diff --check`. This is not a production release; future internal routes still require matching service-chain tests and edge-route validation.
- 2026-05-21 local change set: frontend context storage comment cleanup passed `cd frontend; npm run lint`, Codex validation, repo artifact hygiene, and scoped `git diff --check`. This is not a production release; future auth/context bootstrap work must preserve the cookie-backed session versus local routing-context boundary.
- 2026-05-21 local change set: frontend data-extension governance controls passed `cd frontend; npm run lint`, `cd frontend; npm run build:ci`, targeted Chromium Playwright via `node_modules\.bin\playwright.cmd test tests/e2e/audience-data-extensions.spec.ts --project=chromium --reporter=line`, artifact hygiene, Codex validation, and scoped `git diff --check`. This is not a production release; target migration proof, import/contact provenance population, indexed relationship execution, and promotion evidence remain required before parity or readiness claims.
- 2026-05-21 local change set: tracking outbox inline publish decoupling passed focused tracking outbox/ingestion/publisher tests with 14 tests, full `.\mvnw.cmd -pl services/tracking-service -am test` with 72 tests and 7 expected Testcontainers skips, artifact hygiene, Codex validation, and scoped `git diff --check`. This is not a production release; target tracking ingress latency, outbox depth/age, Kafka lag, ClickHouse behavior, and promotion evidence remain required before throughput or readiness claims.
- 2026-05-21 local change set: delivery replay queue bounded claim passed focused delivery operations tests with 8 tests, full `.\mvnw.cmd -pl services/delivery-service -am test` with 90 delivery-service tests, artifact hygiene, Codex validation, and scoped `git diff --check`. This is not a production release; target multi-worker replay, provider capacity, and release evidence remain required before readiness or throughput claims.
- 2026-05-21 local change set: release evidence validator self-test timeout guard passed `scripts\ops\test-release-evidence-validators.ps1`, local-only `scripts\ops\release-gate.ps1 -LocalOnly -SkipBackend -SkipFrontend -SkipCompose -SkipKustomize`, Codex validation, and scoped `git diff --check`. This is not production promotion evidence; strict release remains blocked without target egress, image, GA, load, restore, CI/security, TLS/admission, and monitoring artifacts.
- 2026-05-21 local change set: campaign feedback counter atomicity passed focused campaign event/repository tests with 25 tests, full `.\mvnw.cmd -pl services/campaign-service -am test` with 99 campaign-service tests, artifact hygiene, Codex validation, and scoped `git diff --check`. This is not a production release; target Kafka replay, DB contention, queue lag, and promotion evidence remain required before throughput or readiness claims.
- 2026-05-21 local change set: deliverability controller RBAC coverage passed focused controller tests with 17 tests, full `.\mvnw.cmd -pl services/deliverability-service -am test` with 50 deliverability-service tests, artifact hygiene, Codex validation, and scoped `git diff --check`. This is not a production release; target auth behavior and promotion still require release evidence gates.
- 2026-05-21 local change set: content send-governance internal security-chain alignment passed focused content security/controller/RBAC tests with 12 tests, route-map validation, Codex validation, and scoped `git diff --check`. This is not a production release; public edge denies and target service-to-service behavior still require release evidence before promotion.
- 2026-05-20 local change set: feature flag tenant-scoping fix passed `.\mvnw.cmd -pl services/foundation-service -am test`. This is not a production release; strict production promotion still requires external evidence and release gates.
- 2026-05-20 local change set: public-edge internal route denies passed route validation, Kustomize render, and Compose config. This is not a production release; strict production promotion still requires external evidence and release gates.
- 2026-05-20 local change set: audience deliverability suppression bulk-check path passed `.\mvnw.cmd -pl services/audience-service,services/deliverability-service -am test`, route validation, Kustomize render, Compose config, repo artifact hygiene, production overlay validation, Codex validation, and `git diff --check`. This is not a production release; strict production promotion still requires external evidence and release gates.
- 2026-05-20 local change set: delivery feedback outbox passed focused delivery tests, `.\mvnw.cmd -pl services/delivery-service -am test`, Codex validation, repo artifact hygiene, and `git diff --check`. This is not a production release; strict production promotion still requires external evidence and release gates.
- 2026-05-20 local change set: campaign send content-reference contract alignment passed focused campaign tests, focused shared Kafka tests, `.\mvnw.cmd -pl services/campaign-service,shared/legent-kafka -am test`, Codex validation, repo artifact hygiene, and `git diff --check`. This is not a production release; strict production promotion still requires external evidence and release gates.
- 2026-05-20 local change set: production egress evidence validator hardening passed `scripts\ops\test-release-evidence-validators.ps1`, expected-fail template validation, local release gate, Codex validation, and `git diff --check`. This is not a production release; strict production promotion still requires real target-environment egress, image, GA, load, restore, CI/security, TLS/admission, and monitoring evidence.
- 2026-05-20 local change set: Kafka DLQ sharding passed focused Kafka config tests, `.\mvnw.cmd -pl shared/legent-kafka -am test`, Compose config, fixed-DLQ drift scan, Codex validation, repo artifact hygiene, and `git diff --check`. This is not a production release; strict production promotion still requires external Kafka topology evidence and the other release evidence gates.
- 2026-05-20 local change set: Journey Builder runtime node contract alignment passed focused automation tests, `.\mvnw.cmd -pl services/automation-service -am test`, frontend lint, frontend production build, targeted Playwright builder spec, Codex validation, and `git diff --check`. This is not a production release; broader journey node families remain draft-only until separately implemented and validated.
- 2026-05-20 local change set: tracking ingress route-limit posture alignment passed route validation, production/global Kustomize renders, production overlay validation, Codex validation, and `git diff --check`. This is not a production release or throughput claim; strict promotion still requires target ingress-controller behavior and downstream tracking ingestion evidence.
- 2026-05-20 local change set: SSO tenant-cookie HTTP-only parity passed focused identity controller tests, `.\mvnw.cmd -pl services/identity-service -am test`, Codex validation, repo artifact hygiene, and `git diff --check`. This is not a production release; strict promotion still requires external evidence and release gates.
- 2026-05-20 local change set: delivery provider workspace isolation passed focused provider isolation tests, focused migration/health/capacity tests, `.\mvnw.cmd -pl services/delivery-service -am test`, Codex validation, repo artifact hygiene, and `git diff --check`. This is not a production release; legacy `workspace-default` backfills and all external release evidence still require target review before promotion.
- 2026-05-20 local change set: production egress policy render proof passed release evidence self-test, production overlay validation, production Kustomize render, local release gate, Codex validation, repo artifact hygiene, and `git diff --check`. This is not a production release; strict promotion still requires real target-environment evidence, but strict egress mode now proves reviewed policy generation/render inclusion when evidence is supplied.
- 2026-05-20 local change set: audience segment rule fail-closed behavior passed focused segment tests, `.\mvnw.cmd -pl services/audience-service -am test`, Codex validation, repo artifact hygiene, and `git diff --check`. This is not a production release; frontend operator filtering remains a UX follow-up and strict promotion still requires external evidence gates.
- 2026-05-20 local change set: Automation Studio live-run confirmation passed focused automation tests, `.\mvnw.cmd -pl services/automation-service -am test`, Codex validation, repo artifact hygiene, and `git diff --check`. This is not a production release; future live activity families must keep explicit confirmation and focused side-effect tests.
- 2026-05-20 local change set: platform event idempotency passed focused platform idempotency tests, `.\mvnw.cmd -pl services/platform-service -am test`, Codex validation, repo artifact hygiene, and `git diff --check`. This is not a production release; target database migration application and Kafka replay behavior still require environment evidence before release claims.
- 2026-05-20 local change set: audience resolution final eligibility first slice passed focused audience eligibility tests, `.\mvnw.cmd -pl services/audience-service -am test`, Codex validation, repo artifact hygiene, and `git diff --check`. This is not a production release and not the full campaign final-gate closure; campaign legacy recipient payloads still need a reviewed eligibility marker/compatibility contract.
- 2026-05-20 local change set: ClickHouse experiment lineage and rollup idempotency passed focused ClickHouse writer/rollup tests, `.\mvnw.cmd -pl services/tracking-service -am test`, Codex validation, repo artifact hygiene, and `git diff --check`. This is not a production release; target ClickHouse DDL migration/application and high-volume refresh evidence remain required before BI-grade experiment claims.
- 2026-05-20 local change set: Codex lifecycle state atomicity passed temp-copy lifecycle smoke, Codex validation, lease validation, utility parser checks, and `git diff --check`. This is an internal autonomous-organization hardening change, not a product release; less-used cleanup/worktree/module-lease utilities remain separate follow-up scope if heavy parallel orchestration expands.
- 2026-05-20 local change set: segment rule builder operator filtering passed frontend lint, production build, targeted Chromium Playwright segment spec via the local Playwright binary, Codex validation, repo artifact hygiene, and `git diff --check`. This is not a production release; backend segment validation remains the authoritative fail-closed boundary.
- 2026-05-20 local change set: send-time optimization governance passed focused optimization tests and full `.\mvnw.cmd -pl services/foundation-service -am test`. This is deterministic readiness and safety-gate evidence only, not model-backed STO, live scheduling, or a production release; campaign/delivery runtime integration and target evidence remain required.
- 2026-05-20 local change set: Automation Studio run-history visibility passed frontend lint, production build, and targeted Chromium Playwright automation-studio spec. This is not a production release; backend pagination, richer traces, activity dependency ordering, notifications, and broader orchestration parity remain future work.
- 2026-05-20 local change set: release evidence validator negative fixtures passed release evidence self-test and local-only release gate. This is not production evidence or a production release; strict promotion still requires real target egress, image, GA, load, restore, CI/security, TLS/admission, and monitoring artifacts.
- 2026-05-20 local change set: data-extension governance migration tests passed focused data-extension tests, `.\mvnw.cmd -pl services/audience-service -am test`, JPA config drift scan, Codex validation, repo artifact hygiene, and `git diff --check`. This is not a production release; PostgreSQL/Flyway target migration evidence against representative legacy rows remains required.
- 2026-05-20 local change set: frequency optimization deterministic policy contract passed focused foundation optimization tests, focused campaign safety tests, and `.\mvnw.cmd -pl services/foundation-service,services/campaign-service -am test`. This is deterministic readiness and safety-gate evidence only, not model-backed engagement-frequency optimization, live cadence control, provider-capacity evidence, parity, or a production release.
- 2026-05-20 local change set: AI content assistance governance passed focused `AiContentAssistanceGovernanceServiceTest` and `.\mvnw.cmd -pl services/foundation-service,services/content-service -am test`. This is first-class draft-only policy/audit persistence and publish/send denial evidence only, not model-backed generation, content workflow application, Salesforce Einstein parity, or a production release.
- 2026-05-20 local change set: flow analytics experimentation passed focused tracking/automation/Kafka tests, full `.\mvnw.cmd -pl services/tracking-service,services/automation-service -am test`, frontend lint, frontend production build, targeted Chromium analytics Playwright, Codex validation, repo artifact hygiene, JPA config scan, and `git diff --check`. This is observed journey analytics and conversion-lineage evidence only, not causal winner automation, BI-grade ClickHouse proof, or a production release.
- 2026-05-20 local change set: Automation Studio file/import/extract artifact ownership passed focused automation/audience tests, full `.\mvnw.cmd -pl services/automation-service,services/audience-service -am test`, frontend lint, frontend production build, targeted Chromium Automation Studio Playwright, route validation, Codex validation, repo artifact hygiene, and `git diff --check`. This is scoped artifact metadata, artifact-ID import handoff, validation-only file/extract history, and audience object-key hardening only, not target storage evidence, live file movement, or a production release.
- 2026-05-20 local change set: Automation Studio webhook/notification activity family passed focused automation/platform/shared tests, full `.\mvnw.cmd -pl services/automation-service,services/platform-service,shared/legent-common -am test`, frontend lint, frontend production build, targeted Chromium Automation Studio Playwright, route validation, Codex validation, repo artifact hygiene, and `git diff --check`. This is guarded platform-event handoff, terminal notification execution, and bounded/redacted platform webhook response persistence only, not target platform migration, Kafka replay, production egress, real endpoint evidence, or a production release.
- 2026-05-20 local change set: Automation Studio governed send activity handoff passed focused automation tests, focused campaign tests, shared Kafka contract tests, and full `.\mvnw.cmd -pl services/automation-service,services/campaign-service,services/delivery-service,services/deliverability-service -am test`. This is a local campaign-orchestration handoff contract only, not a production release, Salesforce parity claim, inbox-placement claim, or 10 lakh throughput proof; delivery-owned policy snapshots, target Kafka replay, provider capacity, deliverability evidence, and production gates remain required.
- 2026-05-20 local change set: automation send handoff review hardening passed focused automation/campaign/shared Kafka tests and full `.\mvnw.cmd -pl services/automation-service,services/campaign-service -am test`. This is a local backend contract fix for idempotency, unsafe override normalization, producer contract strictness, and campaign-wide handoff markers; it is not a production release, target Kafka replay proof, provider capacity proof, inbox-placement claim, or high-volume evidence.
- 2026-05-20 local change set: admin settings context mismatch hardening passed focused `AdminSettingsServiceTest`, full `.\mvnw.cmd -pl services/foundation-service -am test`, and `git diff --check` for touched files. This is not a production release; tenant lifecycle and config by-ID ownership findings remain open and strict promotion still requires external evidence.
- 2026-05-20 local change set: config by-ID tenant/scope hardening passed focused `ConfigServiceTest`, full `.\mvnw.cmd -pl services/foundation-service -am test`, and `git diff --check` for touched files. This is not a production release; tenant lifecycle policy, config version scoping, and global-config administration remain separate follow-ups.
- 2026-05-20 local change set: differentiation upsert workspace exact matching passed focused `DifferentiationPlatformServiceTest`, full `.\mvnw.cmd -pl services/foundation-service -am test`, and `git diff --check` for touched files. This is not a production release; list/evaluate visibility semantics and policy/schema-dependent foundation findings remain separate follow-ups.
- 2026-05-20 local change set: public contact admin platform-admin restriction passed focused contact tests, full `.\mvnw.cmd -pl services/foundation-service -am test`, and `git diff --check` for touched files. This is not a production release; tenant/workspace contact inbox behavior still requires schema/product design.
- 2026-05-20 local change set: config create/upsert context mismatch hardening passed focused `ConfigServiceTest`, full `.\mvnw.cmd -pl services/foundation-service -am test`, and `git diff --check` for touched files. This is not a production release; config version-history scoping and tenant lifecycle policy remain separate follow-ups.
- 2026-05-20 local change set: tenant get self-scope hardening passed focused `TenantServiceTest`, full `.\mvnw.cmd -pl services/foundation-service -am test`, and `git diff --check` for touched files. This is not a production release; tenant list/get-by-slug/lifecycle mutation policy remains a separate follow-up.
- 2026-05-20 local change set: audience Contact Data Designer preview governance passed focused `DataExtensionServiceTest,SegmentServiceTest`, full `.\mvnw.cmd -pl services/audience-service -am test`, Codex validation, repo artifact hygiene, and scoped `git diff --check`. This is not a production release; provenance/classification/audit tables, frontend relationship designer controls, indexed relationship execution, and target migration evidence remain follow-up requirements.
- 2026-05-20 local change set: audience Contact Data Designer governance metadata passed focused `DataExtensionServiceTest,DataExtensionGovernanceMigrationTest,AudienceControllerRbacTest`, full `.\mvnw.cmd -pl services/audience-service -am test`, frontend lint, Codex validation, lease validation, and `git diff --check`. This is not a production release; target Flyway migration evidence, frontend governance controls, import/contact provenance depth, indexed relationship execution, and parity claims remain follow-up requirements.
- 2026-05-20 local change set: Email governance policy objects passed focused content policy/RBAC/internal-token tests, focused campaign content-client/readiness/orchestration tests, full `.\mvnw.cmd -pl services/content-service,services/campaign-service,services/delivery-service -am test`, route validation, Codex validation, repo artifact hygiene, and `git diff --check`. This is not a production release, legal compliance proof, Salesforce parity claim, inbox-placement claim, or 10 lakh throughput evidence; delivery-owned policy snapshots and target migration/edge evidence remain follow-up requirements.
- 2026-05-20 local change set: foundation core-platform workspace guards passed focused `CorePlatformServiceTest`, full `.\mvnw.cmd -pl services/foundation-service -am test`, Codex validation, monitor check, lease validation, and scoped `git diff --check`. This is not a production release; role-binding/access-grant read scoping, nullable permission-list JSON defaults, and all external release evidence remain follow-up requirements.
- 2026-05-20 local change set: foundation core-platform role/access read scoping passed focused `CorePlatformServiceTest,CorePlatformControllerTest`, full `.\mvnw.cmd -pl services/foundation-service -am test`, Codex validation, monitor check, lease validation, and scoped `git diff --check`. This is not a production release; inherited null-workspace visibility semantics, typed preview membership proof, nullable permission-list JSON defaults, and all external release evidence remain follow-up requirements.
- 2026-05-20 local change set: foundation differentiation evaluate exact workspace scoping passed focused `DifferentiationPlatformServiceTest`, full `.\mvnw.cmd -pl services/foundation-service -am test`, Codex validation, monitor check, lease validation, and scoped `git diff --check`. This is not a production release; tenant/global fallback semantics, list/inventory visibility, and all external release evidence remain follow-up requirements.
- 2026-05-20 local change set: foundation compliance privacy-request workspace scoping passed focused `ComplianceEvidenceServiceTest`, full `.\mvnw.cmd -pl services/foundation-service -am test`, Codex validation, monitor check, lease validation, repo artifact hygiene, and scoped `git diff --check`. This is not a production release; tenant-global privacy request mutation policy and all external release evidence remain follow-up requirements.
- 2026-05-20 local change set: foundation permission-list JSON defaulting passed focused `CorePlatformServiceTest`, full `.\mvnw.cmd -pl services/foundation-service -am test`, V6 migration untouched check, Codex validation, monitor check, lease validation, repo artifact hygiene, and scoped `git diff --check`. This is not a production release; existing malformed permission JSON rows and all external release evidence remain follow-up requirements.
- 2026-05-20 local change set: foundation config version-history scoping passed focused `ConfigVersioningServiceTest,ConfigServiceTest,AdminSettingsServiceTest,ConfigVersionControllerTest`, full `.\mvnw.cmd -pl services/foundation-service -am test`, Codex validation, monitor check, repo artifact hygiene, and scoped `git diff --check`. This is not a production release; target V17 migration preflight for legacy duplicate tenant/key/version rows and all external release evidence remain follow-up requirements.
- 2026-05-21 local change set: tracking ingestion batch-consumer readiness hardening passed focused tracking tests, focused shared Kafka tests, full `.\mvnw.cmd -pl shared/legent-kafka,services/tracking-service -am test`, Codex validation, thread coordination validation, lease validation, repo artifact hygiene, and scoped `git diff --check`. This is not a production release, not throughput proof, and not BI-grade ClickHouse evidence; Docker/PostgreSQL idempotency proof and ClickHouse raw-events dedupe/reconciliation evidence remain required before closing the blocked item.

Record future releases with:
- version or change set,
- validation gates,
- evidence artifacts,
- deployment target,
- rollback plan,
- residual risk.
