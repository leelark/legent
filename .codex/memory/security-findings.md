# Security Findings

Fresh baseline date: 2026-05-20.

Current findings from 2026-05-20 audit:

| ID | Owner | Status | Source | Impact | Next Action |
|---|---|---|---|---|---|
| feature-flag-id-tenant-scope | FOUNDATION_SERVICE_OWNER | FIXED | `services/foundation-service/src/main/java/com/legent/foundation/service/FeatureFlagService.java`, `FeatureFlagController.java` | Feature flag get/update/delete by ID previously used raw IDs and could cross tenant boundaries if an ID was known. | Monitor for any explicit global feature flag admin workflow; current protected by-ID operations are tenant-scoped. |
| delivery-provider-workspace-isolation | DELIVERY_SERVICE_OWNER | FIXED | `services/delivery-service/src/main/java/com/legent/delivery/controller/ProviderController.java`, `SmtpProviderRepository.java`, `ProviderSelectionStrategy.java`, `ProviderCapacityService.java`, `V16__smtp_provider_workspace_scope.sql` | Provider CRUD, routing, selection, health checks/status, capacity profiles, and failover drills now require tenant+workspace provider ownership; V16 adds composite ownership constraints for provider-linked tables. | Monitor legacy rows backfilled to `workspace-default` and require target migration review before production promotion. |
| internal-routes-public-edge | API_ARCHITECT | FIXED | `config/nginx/nginx.conf`, `infrastructure/kubernetes/ingress/ingress.yml`, `scripts/ops/validate-route-map.ps1` | Service-to-service endpoints rely on a shared internal credential guard; public edge now returns 404 for source-discovered service-internal controller routes, including templated content paths and deliverability suppression child routes. | Keep route validation in release gates; any new service-internal controller route now fails validation until Nginx and ingress public-edge 404 coverage exists. |
| audience-internal-security-chain-coverage | AUDIENCE_SERVICE_OWNER | FIXED_LOCAL | `SecurityConfig.java`, `SecurityConfigTest.java` | Audience internal query-activity and import-start routes now have focused Spring security-chain coverage proving the routes pass unauthenticated to controller-level internal credential guards, while normal audience routes and RBAC-protected writes still require authentication/permission. | Preserve these allowed/denied cases when adding audience internal routes or changing service security-chain matchers. |
| audience-subscriber-by-id-scoped-lookups | AUDIENCE_SERVICE_OWNER | FIXED_LOCAL | `SubscriberService.java`, `SubscriberRepository.java`, `SubscriberServiceTest.java` | Subscriber get, update, and delete by ID now resolve through tenant+workspace scoped repository access, so known IDs outside the current workspace return not found before mutation, cache invalidation, or event publication. | Preserve scoped lookup and cross-workspace denial coverage for future subscriber profile mutation paths. |
| egress-evidence-validator-hardening | RELEASE_MANAGER | FIXED | `scripts/ops/validate-production-egress-evidence.ps1`, `scripts/ops/test-release-evidence-validators.ps1`, `scripts/ops/validate-production-egress-policy-render.ps1`, `scripts/ops/write-production-egress-policy.ps1` | Production egress evidence templates, `example-*` values, documentation CIDRs, unsupported FQDN evidence, broad/non-canonical CIDRs, future dates, unsupported schema versions, stale generated policies, missing generated policies, and uncovered app-label selectors now fail validation instead of satisfying release gates. | Keep production release blocked until real target-environment evidence exists; strict egress validation now proves generated policy render inclusion when reviewed evidence is supplied. |
| sso-tenant-cookie-httponly | IDENTITY_SERVICE_OWNER | FIXED | `services/identity-service/src/main/java/com/legent/identity/controller/SsoController.java`, `services/identity-service/src/test/java/com/legent/identity/controller/SsoControllerTest.java` | SSO tenant cookie now matches normal auth cookie posture: `HttpOnly`, `Secure`, `SameSite`, path, and max-age are covered for OIDC and SAML ACS callbacks; failed OIDC callback emits no cookies. | Preserve HTTP-only tenant cookie parity for any new SSO callback path. |
| identity-invitation-list-workspace-scope | IDENTITY_SERVICE_OWNER | FIXED_LOCAL | `AuthController.java`, `AuthService.java`, `AuthInvitationRepository.java`, `AuthServiceTest.java`, `AuthControllerTest.java` | Invitation listing now requires trusted workspace context and uses tenant+workspace repository access, preventing same-tenant users from listing invitation metadata across other workspaces. | Preserve missing-workspace fail-closed behavior and scoped listing tests for future invitation admin work. |
| ai-governance-optimization-foundation | SECURITY_ENGINEER | DESIGNED | `docs/product/ai-governance-optimization-foundation.md`, `PerformanceIntelligenceController.java`, `OperationsAssistanceService.java`, `OperationsAssistRequest.java`, `V15__performance_intelligence_control_plane.sql` | Current intelligence surfaces are deterministic, but telemetry/evidence JSON and future model-backed features create data-use, provider disclosure, retention, masking, opt-in, audit, and human-review requirements. Documented governance exists, but enforcement is not implemented yet. | Implement governed AI policy/audit slices before adding provider integration or product AI claims; add focused tests for policy denial, workspace mismatch, sensitive telemetry handling, and human-review gates. |
| ai-content-assistance-governance | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL_CONTRACT | `AiContentAssistanceGovernanceService.java`, `V16__ai_content_assistance_governance.sql`, `AiContentAssistanceGovernanceServiceTest.java` | Draft-only content assistance now has tenant/workspace policy and audit persistence with provider disclosure, data-class allow/block lists, training stance, retention, opt-in/out, kill switch, human review, hash-only prompt/output evidence, secret-key redaction, and publish/send/test-send denial. No model provider is invoked. | Add content-service draft application and publish/test-send review gates only after a separate model-provider/security review; keep external model credentials out of this slice. |
| ai-content-assistance-draft-application-contract | CONTENT_SERVICE_OWNER | FIXED_LOCAL_CONTRACT | `TemplateWorkflowDto.java`, `TemplateWorkflowController.java`, `TemplateWorkflowService.java`, `TemplateTestSendService.java`, `AiContentAssistanceMetadataSupport.java`, AI content workflow/test-send tests | Content draft save now accepts only already approved, human-reviewed `APPLY_TO_DRAFT` evidence, stores hash/reference metadata without raw prompt or output text, and blocks unresolved AI assistance metadata before publish/test-send side effects. No provider call or credential access is introduced. | Preserve fail-closed draft/publish/test-send tests; add cross-service foundation audit verification only in a separately reviewed contract. |
| frequency-optimization-governance | DELIVERABILITY_SERVICE_OWNER | FIXED_CONTRACT | `ClosedLoopOptimizationService.java`, `OptimizationPerformanceServiceTest.java`, `CampaignSendSafetyService.java`, `CampaignSendSafetyServiceTest.java`, `ReputationEngine.java`, `WarmupService.java`, `SendRateControlService.java` | Deterministic frequency recommendations now require tenant/workspace scope, low-data fallback, no cap increase for suppressed/unsubscribed/complaint-prone/bounce-risk/warmup/provider/rate-limit/over-cap states, human approval, and rollback evidence. This is a governance contract, not live autonomous cadence. | Preserve suppression, unsubscribe, warmup, rate-control, provider-capacity, deliverability, and frequency-ledger gates for any future live cadence slice; add direct reputation/warmup and send-execution no-publish tests before runtime use. |
| suppression-delete-tenant-scope | AUDIENCE_SERVICE_OWNER | FIXED | `SuppressionService.java`, `SuppressionRepository.java`, `SuppressionServiceTest.java` | Suppression deletion now resolves by tenant, workspace, ID, and non-deleted status before soft delete; known IDs outside the current workspace/tenant return not found and are not saved. | Preserve tenant+workspace repository lookups for future suppression update/delete paths. |
| segment-rules-fail-closed | AUDIENCE_SERVICE_OWNER | FIXED | `SegmentEvaluationService.java`, `SegmentService.java`, `SegmentEvaluationServiceTest.java`, `SegmentServiceTest.java` | Segment list-membership rules now generate tenant/workspace-scoped `EXISTS`/`NOT EXISTS` clauses instead of being skipped, and unsupported operators fail closed before evaluation or persistence. | Add frontend operator filtering as a follow-up so BASIC rule-building UX avoids backend-rejected list/operator combinations. |
| automation-studio-live-run-confirmation | AUTOMATION_SERVICE_OWNER | FIXED | `AutomationStudioDto.java`, `AutomationStudioService.java`, `AutomationStudioServiceTest.java` | Empty Automation Studio run requests now default to dry-run, and live execution requires explicit `confirmLiveRun=true` plus existing ACTIVE and verification gates. | Preserve explicit-confirmation tests for future Automation Studio live activity types. |
| automation-delay-resume-workspace-scope | AUTOMATION_SERVICE_OWNER | FIXED_LOCAL | `WorkflowEngine.java`, `WorkflowQuartzJob.java`, `WorkflowEngineTest.java`, `WorkflowQuartzJobTest.java` | Delayed workflow resumes now use tenant+workspace scope from the scheduled Quartz job before loading a workflow instance, preventing raw instance-ID resume across workspaces. | Preserve scoped resume propagation and denial tests for future delayed, scheduled, or recovery resume paths. |
| automation-activity-config-secret-boundary | SECURITY_ENGINEER | PARTIAL_FIXED_LOCAL | `docs/product/automation-studio-activity-security-contract.md`, `AutomationArtifact.java`, `AutomationArtifactService.java`, `AutomationStudioService.java`, `WebhookResponseSanitizer.java`, `V8__automation_artifact_ownership.sql`, `AutomationStudioServiceTest.java`, `AutomationArtifactServiceTest.java`, `WebhookResponseSanitizerTest.java` | Automation Studio now rejects raw sensitive-looking config keys before persistence, redacts stored/returned maps, requires scoped artifact IDs for file/import/extract configs, records artifact summaries without object keys, keeps file-drop/extract live movement disabled, publishes webhook side effects through platform-owned events only, enforces terminal notification policy, and bounds/redacts platform webhook responses. Send and script families still need their own family-specific controls before side effects. | Preserve no-raw-config, artifact-summary, webhook platform-event, notification terminal-policy, and response-redaction tests; implement governed campaign handoff separately and keep script execution blocked until sandbox evidence exists. |
| platform-event-idempotency | PLATFORM_SERVICE_OWNER | FIXED_LOCAL | `PlatformEventConsumer.java`, `PlatformEventIdempotencyService.java`, `V7__platform_event_idempotency.sql`, `PlatformEventConsumerTest.java`, `PlatformEventIdempotencyServiceTest.java` | Platform event consumers now claim tenant/workspace-scoped event identity before webhook, notification, and search side effects, and skip duplicate replay by `eventId` or `idempotencyKey`. | Require target DB migration and Kafka replay evidence before release claims; preserve duplicate-skip and mark-failure tests for future platform consumers. |
| audience-resolution-final-eligibility | AUDIENCE_SERVICE_OWNER | FIXED_LOCAL_REVIEW | `AudienceResolutionConsumer.java`, `SendEligibilityService.java`, `SuppressionRepository.java`, `AudienceResolutionConsumerTest.java`, `SendEligibilityServiceTest.java` | Resolved campaign audience chunks now pass through authoritative tenant/workspace-scoped eligibility with local suppressions, nested `channels.email=false`, and future `pausedUntil` before campaign batching. | Review/implement campaign legacy-batch eligibility marker before marking the parent final-gate item done. |
| predictive-segments-governance | AUDIENCE_SERVICE_OWNER | FIXED_CONTRACT | `SegmentController.java`, `PredictiveSegmentGovernanceService.java`, `SegmentService.java`, `SegmentEvaluationService.java`, predictive governance tests | Predictive segment creation and materialization now fail closed unless tenant/workspace context, policy enablement/version, allowed feature sources, protected-data exclusion, minimum readiness counts, freshness, bias/drift pass, human approval audit fields, and rollback snapshot evidence are present; preview is read-only behind `audience:read`. | Add first-class tenant AI policy/audit persistence and security filter-chain tests for internal permitAll routes in separate slices. |
| contact-data-designer-preview-governance | AUDIENCE_SERVICE_OWNER | FIXED_CONTRACT | `DataExtensionDto.java`, `DataExtensionService.java`, `SegmentService.java`, `DataExtensionServiceTest.java`, `SegmentServiceTest.java` | Data-extension create/update/preview contracts now cascade nested validation, cap high-risk collections, validate relationship cardinality/type/key requirements, reject unsafe relationship-path preview fields, sort before projection, require sendable fields to be required, lock effective sendable-key changes after records exist, and reject unsupported data-extension relationship metadata in segment conditions. | Add first-class provenance/classification/audit/source metadata tables and controller pagination/API validation tests in separate slices. |
| contact-data-designer-governance | AUDIENCE_SERVICE_OWNER | FIXED_LOCAL_CONTRACT | `DataExtension.java`, `DataExtensionField.java`, `DataExtensionGovernanceAudit.java`, `DataExtensionDto.java`, `DataExtensionService.java`, `DataExtensionController.java`, `V18__data_extension_governance_metadata.sql`, `data-extensions/page.tsx`, governance tests | Data-extension source/provenance metadata, data classification, field classification, governance review fields, scoped audit history, and frontend source/classification/audit controls now persist and render under tenant/workspace ownership, with RBAC-protected update/read surfaces and tests for invalid classification/source values. | Preserve no-record-payload/no-secret audit metadata; add target migration proof, import provenance population, and indexed relationship execution separately. |
| automation-send-activity-handoff-review | AUTOMATION_SERVICE_OWNER | FIXED_LOCAL | `SendEmailNodeHandler.java`, `AutomationStudioService.java`, `WorkflowGraphValidator.java`, `CampaignEventConsumer.java`, `EventContractValidator.java`, focused tests | Governed send handoff now claims before publish, releases on publish failure, normalizes unsafe override keys, rejects non-true send confirmations and idempotency mismatches before Kafka, and fails closed when a subscriber trace lacks campaign-orchestration markers. | Preserve these focused tests for future journey/contact-level send work; production send and replay evidence remain release blockers. |
| foundation-core-platform-workspace-guards | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `CorePlatformService.java`, `CorePlatformServiceTest.java` | Core-platform team, department, membership, role binding, access grant, and permission-group creation now reject cross-workspace/cross-tenant relationships before persistence; role binding checks principal membership and known resource workspace ownership where foundation can prove it. | Implement separately leased read-scope hardening for role-binding/access-grant lists and access-policy preview; preserve focused cross-workspace denial tests. |
| foundation-core-platform-read-scope | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `CorePlatformService.java`, `CorePlatformController.java`, `CorePlatformServiceTest.java`, `CorePlatformControllerTest.java` | Core-platform role-binding/access-grant list reads and access-policy preview now use authenticated roles to distinguish tenant-wide readers from workspace-scoped readers; workspace-scoped callers require current workspace context and receive exact-workspace rows only. | Decide whether tenant/global `workspace_id` null rows should be inherited into workspace reads before changing exact-match behavior; add typed principal membership proof if preview enumeration resistance becomes a product requirement. |
| foundation-differentiation-evaluate-scope | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `DifferentiationPlatformService.java`, `DifferentiationPlatformServiceTest.java` | Decision-policy evaluation, omnichannel simulation, and SLO evaluation no longer treat missing workspace context as a wildcard across all tenant workspace rows; exact nullable workspace predicates align evaluation with existing upsert/schema semantics and prevent wrong-workspace run/incident records. | Decide explicit tenant/global policy fallback semantics before broadening differentiation evaluation visibility beyond exact workspace/null-workspace rows. |
| foundation-compliance-privacy-request-workspace-scope | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `ComplianceEvidenceService.java`, `ComplianceEvidenceServiceTest.java` | Privacy-request status updates now require current workspace context, mutate by tenant+workspace+ID, and create immutable audit evidence only after the scoped update succeeds. | Decide explicit nullable/tenant-global privacy request admin policy before adding any tenant-global mutation path; list/export nullable workspace behavior remains separate. |
| foundation-config-version-history-scope | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `ConfigVersionHistory.java`, `ConfigVersionHistoryRepository.java`, `ConfigVersioningService.java`, `ConfigService.java`, `ConfigVersionController.java`, `AdminSettingsService.java`, `V17__config_version_history_scope.sql`, focused tests | Config version history no longer uses tenant/key-only reads or rollback. History writes include workspace/environment, exact nullable scope is used for reads/compare/rollback/max-version, rollback mutates only the exact scoped config, and V17 enforces exact-scope key/version uniqueness. | Preflight target DBs for duplicate legacy tenant/key/version rows before applying V17; legacy pre-scope rows remain nullable because historical workspace/environment ownership cannot be safely inferred. |
| admin-settings-context-mismatch | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `AdminSettingsService.java`, `AdminSettingsServiceTest.java` | Admin settings validate/apply/reset/impact now reject request-body `workspaceId` or `environmentId` mismatches against `TenantContext` before config changes can affect another workspace/environment. | Continue foundation ownership hardening for tenant lifecycle and config by-ID operations found in the same audit pass. |
| tenant-lifecycle-cross-tenant-control | FOUNDATION_SERVICE_OWNER | PARTIAL_LOCAL | `TenantController.java`, `TenantService.java`, read-only controller/service audits | Tenant `getTenant` now fails closed unless the requested ID matches `TenantContext`, but tenant list, get-by-slug, and lifecycle mutations still need platform-wide versus tenant self-management policy. | Decide platform-admin versus self-tenant policy, then add controller/service tests for the remaining tenant lifecycle paths. |
| tenant-get-self-scope | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `TenantService.java`, `TenantServiceTest.java` | `TenantService.getTenant` now requires the requested tenant ID to match `TenantContext` before repository lookup, hiding known cross-tenant IDs and failing closed when tenant context is missing. | Preserve missing-context and cross-tenant regression tests; handle remaining tenant lifecycle policy separately. |
| config-by-id-tenant-scope | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `ConfigService.java`, `ConfigRepository.java`, `ConfigServiceTest.java` | Config update/delete by ID now resolve only by current tenant and non-deleted status, then reject workspace/environment-scoped configs when current context is missing or mismatched. | Design explicit privileged global-config administration separately; current tenant endpoints fail closed for cross-tenant/scoped IDs and config version history is now scoped locally. |
| config-create-upsert-context-mismatch-fail-closed | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `ConfigService.java`, `ConfigServiceTest.java` | Config create/upsert now rejects request-body workspace/environment mismatches before repository lookup or save; explicit workspace/environment scope requires trusted args or matching `TenantContext`. | Preserve fail-closed body/context mismatch tests; config version-history scoping is now fixed locally. |
| differentiation-upsert-workspace-exact-match | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `DifferentiationPlatformService.java`, `DifferentiationPlatformServiceTest.java` | Differentiation upsert lookup now uses null-safe exact workspace matching, so tenant-scoped/null-workspace upserts cannot select arbitrary workspace rows before update. | Review list/evaluate query semantics separately before changing tenant-level plus workspace-level visibility behavior. |
| public-contact-admin-platform-admin-only | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `AdminContactRequestController.java`, `AdminContactRequestControllerSecurityTest.java` | Global public contact request admin list/status access is now restricted to PLATFORM_ADMIN because the backing table has no tenant/workspace ownership columns and contains public-contact PII. | Tenant/workspace-scoped contact inbox still requires schema migration and product ownership decision. |
| email-governance-policy-objects | CONTENT_SERVICE_OWNER | FIXED_LOCAL_CONTRACT | `SendGovernancePolicyController.java`, `SendGovernancePolicyService.java`, `CampaignLaunchReadinessGate.java`, `ContentServiceClient.java`, `V10__send_governance_policies.sql`, `V15__campaign_send_governance_policy.sql`, `nginx.conf`, `ingress.yml`, `validate-route-map.ps1` | Send governance policy selection is now tenant/workspace scoped and campaign launch/preflight fails closed for missing, unavailable, inactive, commercial unsafe, sender/domain/provider mismatched, or invalid retention policy. Public edge denies the new internal content lookup endpoint. | Add delivery-owned runtime policy snapshots and target migration/edge evidence before release or parity claims; preserve existing suppression, unsubscribe, warmup, rate, provider, and inbox-safety controls. |
| content-send-governance-internal-security-chain | CONTENT_SERVICE_OWNER | FIXED_LOCAL | `SecurityConfig.java`, `SecurityConfigTest.java`, `SendGovernancePolicyControllerTest.java` | Content-service now allows the already edge-denied internal send-governance policy lookup through its service security chain so campaign preflight can reach the controller-level internal-token guard without a JWT. Invalid internal tokens still fail before service access. | Keep public edge deny validation in route gates and add service security-chain coverage for any new internal route. |
| content-read-rbac-coverage | CONTENT_SERVICE_OWNER | FIXED_LOCAL | `EmailStudioController.java`, `TemplateController.java`, `ContentBlockController.java`, `TemplateVersionController.java`, `TemplateWorkflowController.java`, `EmailController.java`, `ContentControllerRbacTest.java` | Protected content-service GET endpoints now declare method-level `content:read` or `template:*` authorization, and reflection coverage fails future protected reads without RBAC while preserving public landing-page and guarded internal exceptions. | Preserve explicit read exception allowlists for public/internal routes and require focused RBAC coverage when adding content GET endpoints. |
| deliverability-controller-rbac-coverage | DELIVERABILITY_SERVICE_OWNER | FIXED_LOCAL | `SuppressionController.java`, `DmarcController.java`, `ReputationController.java`, `DomainControllerRbacTest.java` | Suppression list/history, DMARC report reads, and reputation reads now require `deliverability:read`; DMARC ingest now requires `deliverability:write`; internal suppression service endpoints keep their dedicated internal credential guard. | Preserve controller reflection coverage for future deliverability endpoints and keep release promotion blocked on normal target auth evidence. |
| deliverability-internal-suppression-security-chain-coverage | DELIVERABILITY_SERVICE_OWNER | FIXED_LOCAL | `services/deliverability-service/src/test/java/com/legent/deliverability/config/SecurityConfigTest.java` | Deliverability internal suppression list/check routes now have focused Spring security-chain coverage proving they can reach controller-level internal credential guards without JWT auth, while normal deliverability routes and method security remain enforced. | Preserve these allow/deny cases for future deliverability internal endpoints and keep public-edge route validation in release gates. |
| deliverability-legacy-reputation-scope | DELIVERABILITY_SERVICE_OWNER | FIXED_LOCAL | `ReputationController.java`, `ReputationScore.java`, `ReputationScoreRepository.java`, `ReputationService.java`, `V11__reputation_score_workspace_scope.sql`, reputation tests | Legacy reputation fallback no longer reads by domain alone; API fallback and legacy score updates now use current tenant/workspace context, normalized domain, scoped latest-row lookup, and a migration removes the old global same-domain uniqueness constraint. | Collect target migration evidence before promotion and keep current `domain_reputations` scoped path as the primary reputation source. |
| campaign-send-checkpoint-workspace-scope | CAMPAIGN_SERVICE_OWNER | FIXED_LOCAL | `SendJobCheckpointingService.java`, `SendJobCheckpointRepository.java`, `SendJobCheckpoint.java`, `V16__send_job_checkpoint_workspace_scope.sql`, `SendJobCheckpointingServiceTest.java` | Send-job checkpoint, resume, progress, and failed-batch retry paths now require current tenant/workspace context, prove the job belongs to that scope before checkpoint access, persist checkpoint workspace ownership, and backfill recovery tables with workspace IDs. | Collect target Flyway migration evidence for legacy checkpoint/resume/recovery rows before promotion. |
| latest-audit-safe-local-followups-20260522 | PROGRAM_MANAGER_AGENT | FIXED_LOCAL | `IdentityExperienceService.java`, `AuthController.java`, `ExperienceDto.java`, `UserRepository.java`, `V17__delivery_legacy_workspace_mapping_guard.sql`, `TrackingEventPublisher.java`, `AwsSesProviderAdapter.java`, subscriber and landing-page E2E specs | Forgot-password reset now remains enumeration-safe while requiring explicit/unambiguous tenant resolution and active workspace membership before token/event side effects; delivery V17 fails migration when unresolved `workspace-default` provider ownership rows remain; tracking exposes only the throwing outbox publisher path; SES region parsing rejects unsupported hosts instead of silently defaulting. | Preserve focused regressions for password reset scope, legacy migration guard, outbox publisher failures, SES endpoints, and frontend sanitized landing-page flows; collect target migration and release evidence before promotion. |
| frontend-forgot-password-tenant-hint | FRONTEND_OWNER | FIXED_LOCAL | `PublicAuthViews.tsx`, `auth-api.ts`, `marketing.spec.ts` | Public account recovery now forwards optional tenant/workspace hints through the public API path while omitting blanks and preserving generic accepted responses and browser-storage boundaries. | Preserve enumeration-safe UI and no-storage coverage for future public account recovery changes. |
| foundation-global-enterprise-workspace-fail-closed | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `GlobalEnterpriseService.java`, `GlobalEnterpriseServiceTest.java` | Workspace-owned global-enterprise list/upsert/read helpers now require workspace context and exact workspace predicates before repository access. | Keep legacy null-workspace rows hidden from workspace-owned surfaces unless a future tenant-global policy is approved. |
| foundation-performance-ledger-workspace-fail-closed | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `PerformanceLedgerSupport.java`, focused performance service tests | Performance-ledger scoped operations now require workspace context before read/write paths and avoid nullable wildcard workspace matching. | Preserve missing-workspace and mismatch tests when expanding performance-ledger surfaces. |

Standing security invariants:
- Never store secrets, `.env` values, raw tokens, private keys, credentials, or customer data in memory.
- Preserve HTTP-only cookie auth and refresh path scoping.
- Preserve unsafe-method origin/referer guard unless replaced with a stronger CSRF strategy.
- Tenant/workspace context must fail closed except documented public endpoints.
- Do not widen Kafka deserialization trust.
- Do not introduce production `ddl-auto=update`.
- Do not weaken HTML sanitization, outbound URL guard, signed tracking URLs, suppression checks, unsubscribe, warmup, rate controls, or inbox safety.

Security changes require focused tests or documented residual risk.
| campaign-send-execution-campaign-workspace-scope | CAMPAIGN_SERVICE_OWNER | FIXED_LOCAL | `SendExecutionService.java`, `SendExecutionServiceTest.java` | Send execution no longer loads campaigns by tenant-only scope after claiming a workspace-owned batch; cross-workspace campaign/batch coupling fails before content rendering or delivery handoff. | Preserve tenant+workspace campaign lookup in every send execution path; the separate legacy eligibility-marker policy blocker remains open. |
| parallel-safe-local-slices-integration-20260522 | PROGRAM_MANAGER_AGENT | FIXED_LOCAL | tracking URL verifier/controller, delivery content processing, frontend auth/sanitizer tests, audience subscriber schema, foundation public contact, content/identity security-chain tests, release validators | Integrated parallel security hardening for URL signing/rotation, click destination validation, workspace signup bootstrap, sanitized `_blank` anchors, subscriber-key workspace scope, public contact input normalization, security-chain coverage, and release validation. | Preserve focused tests and release validators; production release, target migration, provider capacity, monitoring, ClickHouse, and high-volume evidence remain blocked externally. |
| automation-schedule-trigger-job-fail-closed | AUTOMATION_SERVICE_OWNER | FIXED_LOCAL | `WorkflowScheduleTriggerJob.java`, `WorkflowScheduleTriggerJobTest.java` | Quartz schedule trigger jobs now validate required tenant/workspace/workflow/schedule data, load an active matching scoped schedule, and clear `TenantContext` after success or failure before publishing workflow trigger events. | Preserve fail-closed missing/stale job-data coverage for future scheduler recovery paths; production scheduler runtime evidence remains separate. |
| deliverability-domain-workspace-lookup | DELIVERABILITY_SERVICE_OWNER | FIXED_LOCAL | `SenderDomainRepository.java`, `DomainController.java`, `DomainVerificationService.java`, deliverability focused tests | Sender-domain challenge rotation and verification now resolve domains by tenant+workspace+ID before DNS lookup, save, or verification history writes. | Preserve cross-workspace denial tests and collect live DNS/provider evidence only through release validation. |
| content-template-events-workspace-envelope | CONTENT_SERVICE_OWNER | FIXED_LOCAL | `ContentEventPublisher.java`, `ContentPublishedEvent.java`, `TemplateWorkflowService.java`, `TemplateVersionService.java`, `EventContractValidator.java` | Content template submitted, approved, rejected, and published events now carry workspace scope in their envelopes, and `content.published` contract validation requires workspace metadata. | Preserve workspace-bearing event tests and review downstream consumer expectations before changing the content event contract again. |
| runtime-config-guard-ddl-kafka-trust | SHARED_PLATFORM_OWNER | FIXED_LOCAL | `RuntimeConfigurationGuard.java`, `KafkaConsumerConfig.java`, focused shared tests | Prod/production profiles now fail closed for unsafe `spring.jpa.hibernate.ddl-auto` values and Kafka consumer trusted packages must exactly match `java.lang,java.util,com.legent.kafka.model`. | Collect production startup evidence through strict release gates; do not widen Kafka deserialization trust. |
| tracking-outbox-publish-scope-claim | TRACKING_SERVICE_OWNER | FIXED_LOCAL | `TrackingOutboxService.java`, `TrackingOutboxEventRepository.java`, `TrackingOutboxServiceTest.java` | Tracking outbox publish claim and reload now require outbox ID plus tenant/workspace before Kafka publication; wrong-scope IDs do not publish or save. | Runtime Kafka/PostgreSQL evidence remains separate from this focused local guard. |
| delivery-feedback-outbox-publish-scope-claim | DELIVERY_SERVICE_OWNER | FIXED_LOCAL | `DeliveryFeedbackOutboxService.java`, `DeliveryFeedbackOutboxEventRepository.java`, `DeliveryFeedbackOutboxServiceTest.java` | Delivery feedback outbox publish claim and reload now require outbox ID plus tenant/workspace and non-deleted rows before Kafka publication. | Provider, replay, and production send evidence remain external blockers. |
| frontend-core-workspace-preflight-narrowing | FRONTEND_OWNER | FIXED_LOCAL | `api-client.ts`, `api-client-context.spec.ts` | Frontend no longer treats all `/api/v1/core` routes as workspace optional; workspace-bound core API calls fail before dispatch when workspace context is missing. | Keep backend authorization authoritative and preserve explicit optional endpoint tests. |
| identity-invitation-create-workspace-context-fail-closed | IDENTITY_SERVICE_OWNER | FIXED_LOCAL | `AuthController.java`, `AuthControllerTest.java` | Authenticated invitation creation now requires principal workspace context, rejects request workspace mismatch before service side effects, and normalizes blank request workspace to the principal workspace. | Preserve missing-workspace and mismatch tests for future invitation admin changes. |
| campaign-approval-workspace-scoped-lookups | CAMPAIGN_SERVICE_OWNER | FIXED_LOCAL | `CampaignWorkflowService.java`, `CampaignApprovalRepository.java`, `CampaignWorkflowServiceTest.java` | Campaign approval approve/reject/cancel now resolves approvals through tenant plus owning campaign workspace before any save, lock, or state transition. | Campaign legacy eligibility-marker policy remains a separate blocked decision. |
| frontend-platform-workspace-preflight-narrowing | FRONTEND_OWNER | FIXED_LOCAL | `api-client.ts`, `api-client-context.spec.ts` | Frontend no longer treats platform notifications or webhooks as workspace optional; these requests fail before dispatch when workspace context is missing. | Backend authorization remains authoritative; preserve optional endpoint allowlist tests. |
| frontend-reset-token-url-scrub | FRONTEND_OWNER | FIXED_LOCAL | `PublicAuthViews.tsx`, `marketing.spec.ts` | Public reset credential query parameters are captured once, then removed from browser URL state, and focused Playwright proves submit behavior plus no browser-storage persistence. | Preserve URL-scrub and storage-boundary tests for future public reset-screen changes. |
| foundation-admin-public-content-context-fail-closed | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `PublicContentService.java`, `PublicContentServiceTest.java` | Admin public-content list, upsert, and publish now require tenant and workspace context before repository lookup or save, preventing null-scoped admin CMS side effects when service code is reached without full context. | Preserve missing-context no-repository-interaction tests for future admin CMS changes. |
| frontend-template-library-context-scoped-persistence | FRONTEND_OWNER | FIXED_LOCAL | `page.tsx`, `template-builder.spec.ts` | Template library favorites and recents now use active tenant/workspace scoped browser-storage keys and ignore global legacy keys, preventing cross-workspace preference bleed in the UI. | Preserve workspace-switching, missing-workspace no-write, corrupt JSON, and no-global-fallback Playwright coverage when changing template library state. |
| audience-import-processing-scoped-job-claim | AUDIENCE_SERVICE_OWNER | FIXED_LOCAL | `ImportProcessingService.java`, `ImportService.java`, import tests | Async import processing now receives trusted tenant/workspace scope and reloads import jobs through scoped repository access before storage, save, event, or context side effects. | Preserve no-raw-ID job reload tests for future import worker, retry, and cancellation paths. |
| operations-assistance-telemetry-governance | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `OperationsAssistanceService.java`, `OperationsAssistancePerformanceServiceTest.java` | Operations assistance now requires workspace context and rejects restricted telemetry-key shapes before repository access while persisting only allowlisted operational fields. | Keep deterministic assistance surfaces minimized; external provider use remains blocked until separately reviewed. |
| campaign-send-batch-require-workspace-context | CAMPAIGN_SERVICE_OWNER | FIXED_LOCAL | `SendExecutionService.java`, `SendExecutionServiceTest.java`, `CampaignEventConsumerTest.java` | Campaign batch execution now requires workspace context before batch lookup and removes the tenant-only fallback path. | Preserve missing-workspace fail-closed coverage and event context setup tests for batch execution and retry paths. |
| tracking-conversion-rbac-write-guard | TRACKING_SERVICE_OWNER | FIXED_LOCAL | `IngestionController.java`, `TrackingAnalyticsControllerRbacTest.java`, `IngestionControllerTest.java` | Conversion ingestion now requires tracking or analytics write permission while public signed open/click methods remain method-public. | Preserve mixed public/protected controller authorization coverage for future tracking ingestion endpoints. |
| content-blank-target-rel-sanitizer | CONTENT_SERVICE_OWNER | FIXED_LOCAL | `EmailContentValidationService.java`, `EmailContentValidationServiceTest.java` | Backend sanitization now adds safe rel values for blank-target anchors, preserves approved rel tokens, and drops unsafe rel tokens. | Preserve rel/target sanitizer tests when changing allowed attributes or link rendering. |
| compose-safety-runtime-guardrails | DEVOPS_ENGINEER | FIXED_LOCAL | `validate-compose-safety.ps1`, `test-release-evidence-validators.ps1` | Local Compose safety validation now rejects privileged runtime, host namespaces, Docker control socket binds, and broad capability additions. | Keep negative runtime-safety fixtures in release-validator self-tests; strict production promotion still requires target evidence. |
| foundation-feature-flag-list-create-tenant | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `FeatureFlagController.java`, feature flag controller/service tests | Feature flag admin list and create now require trusted tenant context before service access while evaluation fallback remains unchanged. | Design explicit global feature flag administration separately if needed. |
| platform-notification-method-auth | PLATFORM_SERVICE_OWNER | FIXED_LOCAL | `NotificationController.java`, platform controller tests | Platform notification unread and mark-read endpoints now declare explicit authenticated method guards while preserving tenant/workspace/user scoping. | Decide separately whether notification-specific permissions are required. |
| route-map-negative-fixture-expansion | API_ARCHITECT | FIXED_LOCAL | `test-route-map-validator.ps1`, `validate-route-map.ps1` | Route-map fixture harness now catches tracking legacy-route reintroduction, tracking edge-policy drift, and ingress prefix coverage regressions. | Keep route fixture coverage current when adding or changing public/internal route prefixes. |
| frontend-login-context-failure-cleanup | FRONTEND_OWNER | FIXED_LOCAL | `PublicAuthViews.tsx`, `auth.spec.ts` | Public login/signup flows now clear local auth and routing context if workspace bootstrap fails after backend auth succeeds, preventing stale tenant/workspace UI metadata from surviving failure. | Preserve fail-closed bootstrap cleanup tests when auth/session hydration changes. |
| foundation-config-version-require-tenant | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `ConfigVersionController.java`, `ConfigVersionControllerTest.java` | Config version history, compare, single-version, and rollback endpoints now require tenant context before service access and prove no service interaction when tenant context is missing. | Preserve controller-level context guards for any new config version endpoint. |
| identity-user-invitation-list-bounds | IDENTITY_SERVICE_OWNER | FIXED_LOCAL | `AuthController.java`, `AuthService.java`, `UserController.java`, `UserService.java`, identity focused tests | Identity list reads are bounded and invitation create/list paths retain tenant/workspace fail-closed behavior, including principal workspace checks before invitation service side effects. | Preserve missing-workspace and workspace-mismatch coverage for future identity admin flows. |
| ci-ops-validator-selftests | DEVOPS_ENGINEER | FIXED_LOCAL | `.github/workflows/ci-security.yml`, `test-route-map-validator.ps1`, `validate-compose-safety.ps1` | CI compose smoke now runs local route-map fixture and Compose safety validators, reducing drift between local release checks and automated security/ops checks. | Confirm behavior in GitHub Actions during the next real CI run; strict release evidence remains separately blocked. |
| foundation-compliance-evidence-workspace-fail-closed | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL | `ComplianceEvidenceService.java`, `ComplianceController.java`, `ComplianceEvidenceServiceTest.java` | Compliance evidence create, list, retention, consent, privacy, and export paths now require trusted workspace context, reject mismatched request scope, and use exact workspace predicates before repository access. | Preserve tenant+workspace fail-closed coverage; design tenant-global compliance administration only as a separate privileged policy. |
| tracking-websocket-handshake-no-header-scope-fallback | TRACKING_SERVICE_OWNER | FIXED_LOCAL | `TenantHandshakeInterceptor.java`, `TenantHandshakeInterceptorTest.java` | Analytics WebSocket handshakes no longer accept upgrade request headers as tenant/workspace authority when authenticated context is missing. | Preserve trusted-context-only handshake coverage when adding WebSocket auth modes. |
| frontend-public-api-origin-guard | FRONTEND_PLATFORM_OWNER | FIXED_LOCAL | `api-client.ts`, `api-client-context.spec.ts` | Frontend public API helpers now reject external absolute targets before dispatch while keeping internal public calls credentialless and without tenant/workspace headers. | Keep origin-guard coverage for every new API helper path. |
| devops-pwsh-child-process-portability | DEVOPS_ENGINEER | FIXED_LOCAL | `release-gate.ps1`, `test-release-evidence-validators.ps1`, `validate-compose-health.ps1`, `.github/workflows/ci-security.yml` | Release validator child execution now reuses the active PowerShell executable where possible and CI exercises compose health parser self-tests, reducing local/CI drift. | Confirm behavior in real CI; strict production promotion remains blocked without target evidence. |
