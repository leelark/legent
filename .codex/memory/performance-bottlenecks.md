# Performance Bottlenecks

Fresh baseline date: 2026-05-20.

Audit-only bottlenecks from 2026-05-20 current source review. These are not measured load results.

| ID | Owner | Status | Source | Impact | Next Action |
|---|---|---|---|---|---|
| audience-import-error-cap | AUDIENCE_SERVICE_OWNER | FIXED-LOCAL | `ImportProcessingService.java`, `ImportProcessingServiceTest.java` | Audience CSV import processing now caps retained row-error detail samples while still counting every failed row, preventing high-error imports from accumulating unbounded in-memory error detail lists before final persistence. | Target import-load evidence is still required before broad high-volume import claims. |
| audience-tracking-intelligence-batch-consumer | AUDIENCE_SERVICE_OWNER | FIXED-LOCAL | `AudienceIntelligenceConsumer.java`, `AudienceKafkaConsumerConfig.java`, `AudienceIntelligenceConsumerTest.java`, `AudienceKafkaConsumerConfigTest.java` | Audience subscriber intelligence now consumes `tracking.ingested` events through an audience-scoped batch listener with batch ack, bounded max-poll records, and duplicate skip before subscriber side effects, reducing per-record listener overhead on tracking intelligence updates. | Collect target Kafka lag, batch size, audience DB write latency, idempotency conflict rate, and subscriber timeline update metrics before throughput claims. |
| audience-bulk-suppression-check | AUDIENCE_SERVICE_OWNER | FIXED | `AudienceResolutionConsumer.java`, `DeliverabilityServiceClient.java`, `SuppressionController.java`, `SuppressionListRepository.java`, `V10__suppression_bulk_lookup_index.sql` | Audience resolution now sends normalized, deduped candidate emails to a bounded deliverability bulk-check endpoint instead of fetching all workspace suppressions. | Monitor high-volume resolution metrics; live throughput proof still requires target-like load evidence. |
| delivery-feedback-outbox | DELIVERY_SERVICE_OWNER | FIXED-CODE | `DeliveryFeedbackOutboxService.java`, `DeliveryFeedbackOutboxEvent.java`, `V15__delivery_feedback_outbox_events.sql`, `DeliveryOrchestrationService.java` | Delivery sent/failed/bounced/retry feedback now writes a small durable outbox row and retries Kafka publication without changing provider send state. | Add live depth/oldest-age alerting, retention cleanup, and target-like outbox throughput evidence before high-volume claims. |
| delivery-replay-queue-bounded-claim | DELIVERY_SERVICE_OWNER | FIXED-LOCAL | `DeliveryOperationsService.java`, `DeliveryReplayQueueRepository.java`, `DeliveryOperationsServiceWorkspaceScopeTest.java` | Delivery replay processing now reads a bounded due page, atomically claims PENDING rows before orchestration side effects, skips rows claimed by another worker, and fails replay rows without a source content reference before dispatch. | Target multi-worker replay, database contention, and provider-capacity evidence are still required before throughput or production readiness claims. |
| tracking-outbox-inline-publish-decoupling | TRACKING_SERVICE_OWNER | FIXED-LOCAL | `TrackingOutboxService.java`, `TrackingOutboxServiceTest.java` | Tracking ingestion now writes the durable outbox row without inline post-commit publish by default; the existing poller, claim, retry, and failure path remains the publisher, reducing public tracking response coupling to Kafka health. | Target tracking ingress latency, outbox depth/age, Kafka lag, and ClickHouse evidence remain required before throughput claims. |
| campaign-send-content-reference-contract | CAMPAIGN_SERVICE_OWNER | FIXED-CONTRACT | `SendExecutionService.java`, `application.yml`, `SendExecutionServiceTest.java`, `EventContractValidatorTest.java` | Send-request events now require and carry durable content references, preventing inline-only Kafka sends; optional inline fallback can still add per-recipient body bytes when enabled. | Run target-like proof for per-recipient content snapshot writes, delivery reference fetches, queue lag, and decide whether production high-volume mode must disable inline fallback. |
| tracking-ingress-rate-policy | DEVOPS_ENGINEER | FIXED-CONFIG | `config/nginx/nginx.conf`, `infrastructure/kubernetes/ingress/ingress.yml`, `scripts/ops/validate-route-map.ps1`, `docs/operations/ga-evidence-matrix.md` | Local Nginx and Kubernetes ingress now explicitly align `/api/v1/tracking` to a reviewed elevated edge policy while keeping analytics and websocket routes on normal API limits. Kubernetes uses community ingress-nginx `limit-rps`, which is enforced per controller replica. This is protective configuration only. | Collect target-environment evidence for ingress 429 behavior, accepted/rejected rates, p95/p99 latency, tracking-service errors, Kafka lag, ClickHouse insert latency, outbox depth/age, dedupe rate, and alert routing before throughput claims. |
| clickhouse-experiment-lineage-rollup-idempotency | TRACKING_SERVICE_OWNER | FIXED-LOCAL | `ClickHouseWriter.java`, `ClickHouseRollupService.java`, `ClickHouseWriterTest.java`, `ClickHouseRollupServiceTest.java` | ClickHouse raw events now preserve `experiment_id`, `variant_id`, and `holdout`, and campaign-day rollup refresh deletes the scoped tenant/workspace/date window before insert so repeated local refreshes do not double count. | Collect target ClickHouse DDL migration/application evidence and high-volume refresh metrics before BI-grade experiment or throughput claims. |
| flow-analytics-experimentation | TRACKING_SERVICE_OWNER | TESTED-LOCAL | `WorkflowStudioService.java`, `InstanceHistoryRepository.java`, `TrackingIngestionService.java`, `AnalyticsService.java`, `ClickHouseWriter.java`, `ClickHouseRollupService.java`, `analytics/page.tsx` | Workflow analytics is bounded to the most recent 200 runs and reports deterministic observed step/path/goal metrics; tracking conversion lineage is additive and separated from campaign experiments. | Collect target ClickHouse DDL/load evidence and implement tracking-owned workflow-topic rollups before BI-grade journey analytics, winner automation, or throughput claims. |
| send-time-optimization-governance | CAMPAIGN_SERVICE_OWNER | FIXED-CONTRACT | `ClosedLoopOptimizationService.java`, `OptimizationPerformanceServiceTest.java`, `ai-governance-optimization-foundation.md`, `salesforce-parity-matrix.md` | STO policy evaluation now requires explicit data-readiness, fallback, confidence, and launch-timing safety evidence before future schedule changes can proceed. This prevents low-data or gate-failing recommendations from being treated as high-confidence timing decisions. | Add campaign/delivery runtime integration only after scheduling contract, quiet-hours/timezone behavior, provider capacity, warmup, suppression, and approval evidence are validated end to end. |
| frequency-optimization-governance | DELIVERABILITY_SERVICE_OWNER | FIXED-CONTRACT | `ClosedLoopOptimizationService.java`, `OptimizationPerformanceServiceTest.java`, `CampaignSendSafetyService.java`, `CampaignSendSafetyServiceTest.java`, `ReputationEngine.java`, `WarmupService.java`, `SendRateControlService.java`, `ai-governance-optimization-foundation.md` | Frequency policy evaluation now defines low-data fallback, saturation category, confidence, recommended cap, safety impact, approval/rollback, and no-increase guardrails before future cadence changes. Existing campaign send-safety caps still block before delivery handoff. | Keep live cadence control, scheduled-send revalidation or immutable snapshot tests, model-backed optimization, provider-capacity proof, direct warmup/reputation tests, and target load evidence as follow-up before frequency or throughput claims. |
| campaign-schedulers-bounded-claims | CAMPAIGN_SERVICE_OWNER | FIXED-LOCAL | `SchedulingService.java`, `SendExecutionService.java`, `SendJobRepository.java`, `SendBatchRepository.java`, scheduler tests | Scheduled send-job and partial-batch retry schedulers now read bounded pages and use tenant/workspace/id/status compare-and-claim updates before publishing audience-resolution or batch-retry events, reducing duplicate work under multiple scheduler replicas. | Target-like multi-replica scheduler/load evidence is still required before throughput or production readiness claims. |
| campaign-scheduled-publish-workspace-context | CAMPAIGN_SERVICE_OWNER | FIXED-LOCAL | `SchedulingService.java`, `CampaignEventPublisher.java`, scheduler and publisher tests | Scheduled send-job processing now sets tenant/workspace context from the claimed job row before publishing audience-resolution events, so the Kafka envelope and payload carry workspace ownership in scheduled-worker paths. | Target-like multi-replica scheduler and queue-lag evidence remains required before throughput or production readiness claims. |
| campaign-batch-requeue-paging | CAMPAIGN_SERVICE_OWNER | FIXED-LOCAL | `BatchingService.java`, `SendJobCheckpointingService.java`, `OrchestrationService.java`, `SendBatchRepository.java`, focused campaign tests | Campaign retry/requeue now uses bounded scoped batch reads and claim-before-publish behavior so stale or already-claimed batches do not amplify retry work. | Target multi-replica retry, database contention, and queue-lag evidence remain required before throughput or production readiness claims. |
| campaign-feedback-counter-atomicity | CAMPAIGN_SERVICE_OWNER | FIXED-LOCAL | `CampaignEventConsumer.java`, `SendJobRepository.java`, `SendBatchRepository.java`, `CampaignEventConsumerTest.java`, `CampaignFeedbackCounterRepositoryTest.java` | Delivery feedback now increments send-job and send-batch counters through scoped atomic repository updates before reloading job state for reconciliation, reducing lost-update risk under concurrent feedback events. | Target Kafka replay, DB contention, and queue-lag evidence are still required before throughput or production readiness claims. |
| platform-webhook-dispatch-fanout-bound | PLATFORM_SERVICE_OWNER | FIXED-LOCAL | `WebhookDispatcherService.java`, `WebhookConfigRepository.java`, `WebhookDispatcherServiceTest.java` | Platform webhook dispatch now pages active configs in stable ID order and dispatches one bounded page at a time instead of loading and building every workspace webhook dispatch task together. | Collect live webhook fanout/load evidence before throughput claims; tune page size only if target metrics require it. |
| tracking-outbox-publish-scope-claim | TRACKING_SERVICE_OWNER | FIXED-LOCAL | `TrackingOutboxService.java`, `TrackingOutboxEventRepository.java`, `TrackingOutboxServiceTest.java` | Tracking outbox publish now claims and reloads rows by outbox ID plus tenant/workspace before Kafka publication, preventing wrong-scope publish attempts from mutating or amplifying retry work. | Runtime Kafka/PostgreSQL outbox depth, retry, and lag evidence remains separate from this local scope fix. |
| delivery-feedback-outbox-publish-scope-claim | DELIVERY_SERVICE_OWNER | FIXED-LOCAL | `DeliveryFeedbackOutboxService.java`, `DeliveryFeedbackOutboxEventRepository.java`, `DeliveryFeedbackOutboxServiceTest.java` | Delivery feedback outbox publish now claims and reloads non-deleted rows by outbox ID plus tenant/workspace while preserving retry-lease and max-attempt behavior. | Provider/replay/runtime evidence remains external; collect target outbox throughput and stuck-row metrics before production claims. |
| outbox-backlog-observability-alerts | SRE_MONITORING_ENGINEER | FIXED-LOCAL | `TrackingOutboxService.java`, `DeliveryFeedbackOutboxService.java`, outbox repositories/tests, Prometheus alerts, Grafana overview, runbook | Tracking and delivery feedback outboxes now expose low-cardinality ready-depth and oldest-ready-age gauges with queue-only labels, plus local alert/dashboard/runbook coverage for backlog triage. | Tune thresholds and prove scrape, firing, routing, and runbook handoff with target environment evidence before monitoring readiness claims. |
| campaign-dead-letter-page-bound | CAMPAIGN_SERVICE_OWNER | FIXED-LOCAL | `CampaignEngineService.java`, `CampaignDeadLetterRepository.java`, `CampaignEngineController.java`, `CampaignEngineServiceTest.java` | Campaign dead-letter listing now uses a bounded first page with default 100 and max 500 rows instead of materializing every dead-letter row for a send job. | Collect target DLQ volume/load evidence and operator workflow metrics before throughput claims. |
| automation-workflow-run-history-page-bound | AUTOMATION_SERVICE_OWNER | FIXED-LOCAL | `WorkflowStudioService.java`, `WorkflowInstanceRepository.java`, `InstanceHistoryRepository.java`, `WorkflowController.java`, `WorkflowStudioServiceTest.java` | Automation workflow run and history reads now use bounded default/max pagination for run lists and run step/trace history. | Collect production workflow history volume evidence before scale claims. |
| content-template-test-send-history-bound | CONTENT_SERVICE_OWNER | FIXED-LOCAL | `TemplateTestSendService.java`, `TemplateTestSendRecordRepository.java`, `TemplateTestSendServiceTest.java` | Template test-send history now uses a bounded first-page repository query instead of materializing every matching test-send record. | Collect target history-volume/operator evidence only if UI pagination or deeper audit history is expanded. |
| deliverability-suppression-history-count-query | DELIVERABILITY_SERVICE_OWNER | FIXED-LOCAL | `SuppressionController.java`, `SuppressionListRepository.java`, `SuppressionControllerTest.java` | Suppression history now computes total with a scoped count query instead of loading all suppression rows and calling `.size()`. | Collect target suppression-volume/runtime evidence before making production scale claims. |
| delivery-provider-capacity-list-bound | DELIVERY_SERVICE_OWNER | FIXED-LOCAL | `DeliveryOperationsController.java`, `ProviderCapacityService.java`, `ProviderCapacityProfileRepository.java`, `ProviderCapacityServiceTest.java` | Provider-capacity list reads now use a bounded first page with default 50 and max 200 while preserving tenant/workspace filtering and list response shape. | Collect target provider-capacity volume/operator evidence only if deeper paging or search is expanded. |
| audience-import-processing-scoped-job-claim | AUDIENCE_SERVICE_OWNER | FIXED-LOCAL | `ImportProcessingService.java`, `ImportService.java`, import tests | Async import processing now uses scoped job lookups for progress, completion, failure, and cancellation paths and stops before storage/event side effects when the scoped job is missing. | Target import-load, storage, and cancellation timing evidence remain separate. |
| deliverability-reputation-recovery-page-bound | DELIVERABILITY_SERVICE_OWNER | FIXED-LOCAL | `ReputationEngine.java`, `DomainReputationRepository.java`, `ReputationEngineTest.java` | Scheduled reputation recovery now reads deterministic fixed-size pages instead of materializing every domain reputation row. | Collect target reputation-volume/runtime evidence before scale claims. |
| observability-alert-routing-handoff-gaps | SRE_MONITORING_ENGINEER | FIXED-LOCAL | `prometheus-alerts.yml`, `alertmanager.yml`, `grafana-legent-overview.json`, `validate-production-overlay.ps1`, runbook | Local validation now checks alert team ownership, routing, runbook anchors, Grafana dashboard/panel references, and receiver settings for monitoring handoff hygiene. | Target alert firing, notification delivery, live dashboard data, and monitoring handoff evidence remain external blockers. |
| data-extension-controller-pagination-guards | AUDIENCE_SERVICE_OWNER | FIXED-LOCAL | `DataExtensionController.java`, `DataExtensionControllerTest.java` | Data-extension list and record controller reads now clamp page/size params to shared default and maximum bounds before service calls. | Collect target data-extension volume evidence before scale claims; relationship execution remains separate. |
| segment-scheduled-recompute-page-bound | AUDIENCE_SERVICE_OWNER | FIXED-LOCAL | `SegmentEvaluationService.java`, `SegmentRepository.java`, `SegmentEvaluationServiceTest.java` | Scheduled segment recompute now scans due candidates in deterministic bounded pages rather than a single all-candidate read. | Durable chunked materialization and target segment-volume evidence remain separate. |
| content-send-governance-pagination-guard | CONTENT_SERVICE_OWNER | FIXED-LOCAL | `SendGovernancePolicyController.java`, `SendGovernancePolicyControllerTest.java` | Send-governance policy list reads now clamp page/size params to safe defaults and maximums before scoped list access. | Collect target operator-volume evidence only if deeper policy browsing expands. |
| foundation-bootstrap-page-active-tenants | FOUNDATION_SERVICE_OWNER | FIXED-LOCAL | `TenantBootstrapInitializer.java`, `TenantRepository.java`, `TenantBootstrapInitializerTest.java` | Startup bootstrap discovery now scans active non-deleted tenants in deterministic bounded pages instead of loading every tenant row. | Collect target startup/runtime evidence before scale claims. |
| platform-global-search-result-bound | PLATFORM_SERVICE_OWNER | FIXED-LOCAL | `GlobalSearchService.java`, `SearchIndexDocRepository.java`, `GlobalSearchServiceTest.java` | Platform global search now uses a bounded first-page repository read while preserving tenant/workspace filtering and list return shape. | Collect target search-volume evidence before search scale claims. |
| platform-unread-notifications-bound | PLATFORM_SERVICE_OWNER | FIXED-LOCAL | `NotificationEngine.java`, `NotificationRepository.java`, `NotificationEngineTest.java` | Unread notification reads now use a bounded first page sorted by newest records while preserving tenant/workspace/user filters. | Collect target notification-volume evidence before operator/user scale claims. |
| content-template-search-and-page-guards | CONTENT_SERVICE_OWNER | FIXED-LOCAL | `TemplateController.java`, `TemplateService.java`, `EmailTemplateRepository.java`, template tests | Template list reads now clamp invalid/oversize page params and template search now uses a bounded first-page query. | Collect target template-volume evidence if deeper search pagination is expanded. |
| delivery-provider-health-monitoring-bounds | DELIVERY_SERVICE_OWNER | FIXED-LOCAL | `ProviderHealthMonitoringService.java`, `SmtpProviderRepository.java`, `ProviderHealthCheckRepository.java`, provider health tests | Provider health monitoring now scans active providers in deterministic bounded pages and reads recent health history with a bounded newest-first query. | Collect target scheduler/provider-volume evidence before runtime scale claims. |
| campaign-experiment-evaluator-page-bound | CAMPAIGN_SERVICE_OWNER | FIXED-LOCAL | `CampaignEngineService.java`, `CampaignExperimentRepository.java`, campaign engine tests | Scheduled campaign experiment winner evaluation now scans active experiments through bounded deterministic pages. | Collect target scheduler/experiment-volume evidence before scale claims. |
| deliverability-suppression-list-bound | DELIVERABILITY_SERVICE_OWNER | FIXED-LOCAL | `SuppressionController.java`, `SuppressionListRepository.java`, suppression/RBAC tests | Suppression list endpoints now read bounded tenant/workspace scoped pages for public and internal list paths. | Collect target suppression-volume evidence before scale claims. |
| tracking-campaign-summary-list-bound | TRACKING_SERVICE_OWNER | FIXED-LOCAL | `AnalyticsController.java`, `CampaignSummaryRepository.java`, analytics controller tests | Tracking campaign summary listing now reads a bounded tenant/workspace scoped first page while keeping the list response shape. | Collect target analytics-volume evidence before scale claims. |
| delivery-warmup-rate-state-list-bounds | DELIVERY_SERVICE_OWNER | FIXED-LOCAL | `DeliveryOperationsController.java`, `WarmupService.java`, `SendRateControlService.java`, warmup/rate repositories/tests | Delivery warmup and rate-state operator endpoints now use bounded first-page scoped reads with default/max clamps. | Collect target provider/runtime state-volume evidence before scale claims. |
| foundation-admin-settings-list-query-bound | FOUNDATION_SERVICE_OWNER | FIXED-LOCAL | `AdminSettingsService.java`, `ConfigRepository.java`, admin settings tests | Admin settings listing now uses scoped visible-settings pageable queries instead of scanning all config rows in memory. | Collect target config-volume evidence before admin scale claims. |
| automation-workflow-schedule-activity-list-bounds | AUTOMATION_SERVICE_OWNER | FIXED-LOCAL | automation workflow/schedule/activity services, repositories, and tests | Automation workflow, schedule, and activity listing now use bounded first-page repository access; dependency-cycle validation follows scoped dependency IDs instead of relying on a full workspace activity scan. | Add paged API metadata only as a separate contract; collect target workflow/activity-volume evidence before scale claims. |
| platform-webhook-admin-list-bound | PLATFORM_SERVICE_OWNER | FIXED-LOCAL | `WebhookController.java`, `WebhookConfigRepository.java`, `WebhookControllerTest.java` | Platform webhook admin listing now uses bounded tenant/workspace scoped first-page access while preserving the existing list response shape. | Add deeper pagination only as an explicit admin API contract; collect target webhook-volume evidence before scale claims. |
| deliverability-dmarc-report-list-bound | DELIVERABILITY_SERVICE_OWNER | FIXED-LOCAL | `DmarcController.java`, `DmarcReportRepository.java`, `DmarcControllerTest.java` | DMARC report history now reads bounded tenant/workspace scoped first pages while preserving the list response shape. | Add pagination metadata only as a separate API contract; collect target DMARC-volume evidence before scale claims. |
| content-template-approval-history-bound | CONTENT_SERVICE_OWNER | FIXED-LOCAL | `TemplateWorkflowController.java`, `TemplateWorkflowService.java`, `TemplateApprovalRepository.java`, `TemplateWorkflowServiceTest.java` | Template pending-approval and approval-history reads now use bounded scoped first-page access. | Add deeper approval-history browsing only as a deliberate operator API/UI change. |
| tracking-analytics-windowed-rollups | TRACKING_SERVICE_OWNER | FIXED-LOCAL | `AnalyticsController.java`, `AnalyticsService.java`, `AnalyticsServiceTest.java`, `AnalyticsControllerTest.java` | Tracking analytics timeline and rollup reads now apply default bounded windows, explicit window support, and bucket caps for hour/day query shapes. | Collect target ClickHouse/runtime analytics latency and row-volume evidence before scale claims. |
| content-template-version-list-bound | CONTENT_SERVICE_OWNER | FIXED-LOCAL | `TemplateVersionController.java`, `TemplateVersionService.java`, `TemplateVersionRepository.java`, `TemplateVersionServiceTest.java` | Template version listing now uses bounded first-page scoped access, and publish cleanup avoids materializing every version row. | Add deeper version-history paging only as an explicit API/UI contract; collect live persistence evidence before scale claims. |
| identity-user-invitation-list-bounds | IDENTITY_SERVICE_OWNER | FIXED-LOCAL | `UserController.java`, `UserService.java`, `AuthController.java`, `AuthService.java`, identity focused tests | Identity user and invitation admin list reads now use bounded scoped access with default/max clamps while preserving list response shapes. | Add paged metadata only as a deliberate admin API change; collect target identity data-volume evidence before scale claims. |
| deliverability-reputation-read-bounds | DELIVERABILITY_SERVICE_OWNER | FIXED-LOCAL | `PredictiveDeliverabilityService.java`, `DeliverabilityInsightsController.java`, `DomainReputationRepository.java`, focused deliverability tests | Predictive deliverability and insight reputation reads now use bounded first-page windows with default/max clamps while preserving tenant/workspace scope and response shapes. | Insight averages now represent a bounded recent window; collect target reputation-volume evidence before scale claims or deeper analytics contracts. |
| content-block-version-list-bound | CONTENT_SERVICE_OWNER | FIXED-LOCAL | `ContentBlockService.java`, `ContentBlockController.java`, `ContentBlockVersionRepository.java`, `ContentBlockServiceTest.java` | Content block version listing now uses bounded scoped reads, and publish cleanup clears other published versions without materializing the full version set. | Add deeper content-block history paging only as an explicit API/UI contract; collect live persistence evidence before scale claims. |
| predictive-segment-materialization-scale | AUDIENCE_SERVICE_OWNER | OPEN | `SegmentEvaluationService.java`, `PredictiveSegmentGovernanceService.java`, `SegmentEvaluationServiceTest.java` | New governance blocks unapproved predictive segment materialization, but existing segment recompute still builds full membership sets in memory and rollback snapshots are contract fields, not durable chunked storage. | Implement cursor/chunked recompute with durable snapshots before high-volume predictive audience use. |
| data-extension-relationship-preview-scale | AUDIENCE_SERVICE_OWNER | PARTIAL-CONTRACT | `DataExtensionService.java`, `DataExtensionDto.java`, `DataExtensionServiceTest.java` | Query preview is now bounded by validation, rejects relationship path syntax, validates sort/filter/field names up front, and sorts before projection, but data-extension relationships remain JSON metadata and join execution is intentionally unsupported until an indexed schema/performance design exists. | Design additive relationship/provenance tables, indexes, cursor pagination, and measured preview/query limits before Contact Builder relationship joins or high-volume data-extension querying. |
| delivery-rate-control-hot-path | DELIVERY_SERVICE_OWNER | BLOCKED-CODE | `DeliveryOrchestrationService.java`, `SendRateControlService.java`, `ProviderCapacityService.java` | Per-message DB lock/reservation path and prelaunch-only capacity decisions are not target-proven for mature high-volume sends. | Design sharded/batched provider-domain reservations and hot-path provider capacity enforcement. |
| tracking-consumer-throughput | TRACKING_SERVICE_OWNER | TESTED-LOCAL/BLOCKED-EVIDENCE | `TrackingOutboxService.java`, `TrackingEventConsumer.java`, `TrackingKafkaConsumerConfig.java`, `KafkaTopicConfig.java` | Tracking ingestion now has local batch listener/write behavior, dedicated long-horizon retry for stale idempotency claims, and schema repair for writer lineage columns; target throughput and live runtime evidence remain absent. | Collect Docker/PostgreSQL, Kafka, and ClickHouse runtime evidence before throughput claims. |
| tracking-ingestion-batch-consumer-readiness | TRACKING_SERVICE_OWNER | TESTED-LOCAL/BLOCKED-EVIDENCE | `TrackingEventConsumer.java`, `TrackingKafkaConsumerConfig.java`, `TrackingEventIdempotencyService.java`, `ClickHouseWriter.java`, `ClickHouseRollupService.java`, `V13__tracking_idempotency_raw_write_phase.sql`, `.codex/checkpoints/20260520T214747Z-tracking-ingestion-batch-consumer-readiness.json` | 2026-05-21: local batch consumer and retry hardening is validated, but ClickHouse `raw_events` still lacks runtime dedupe/reconciliation proof, so ambiguous or partial `JdbcTemplate.batchUpdate` failures can duplicate raw analytics and overcount rollups. | Collect ClickHouse dedupe/reconciliation evidence plus Docker/PostgreSQL idempotency proof before closing the batch-consumer readiness item. |
| retry-dlq-target-readiness | KAFKA_CONTRACTS_OWNER | PARTIAL-FIX | `KafkaConsumerConfig.java`, `KafkaTopicConfig.java`, `docker-compose.yml`, `EmailFailedConsumer.java`, campaign/delivery retry scans | Shared Kafka DLQ now preserves source partition and defines six partitions in Java and Compose; retry scans, DLQ-specific observability, and production external Kafka topology evidence still need work before target-like load. | Add retry scan paging/sharding, DLQ volume/age/skew alerts, and release evidence proving production `kafka.dead-letter` partition/retention/replication settings. |

Standing performance constraints:
- Do not send high-volume campaigns from new or unwarmed domains.
- Do not materialize massive audiences into one event, list, transaction, or hot partition.
- Use chunked/cursor-based processing, idempotent chunk IDs, shard-aware Kafka keys, bounded rendering, provider health checks, and rate control.
- Keep tracking/analytics ingestion isolated from send execution pressure.

Required evidence for 10 lakh sends in 10 hours:
- load profile,
- warmed sender/domain status,
- provider capacity,
- queue partitioning,
- rate-control behavior,
- suppression/unsubscribe compliance,
- retry/DLQ results,
- operational dashboards and alerts.
