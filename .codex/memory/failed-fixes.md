# Failed Fixes

Last updated: 2026-05-13.

- No failed fix attempt recorded during orchestration bootstrap.
- 2026-05-13: During Compose health drift fix, two throwaway `docker compose config --quiet` checks with temporary `REDIS_PASSWORD` failed before Compose ran because wrapper quoting was wrong. Cause: shell-wrapper quoting, not Compose config. Avoid repeating by setting/removing `$env:REDIS_PASSWORD` within one PowerShell command and checking `$LASTEXITCODE`.
- 2026-05-13: First Kafka retry/DLQ validation failed in `KafkaConsumerConfigTest.kafkaErrorHandler_usesRecoveringHandlerForDlqPublishing` with `NullPointerException` from `DefaultKafkaConsumerFactory` because the direct unit test called `kafkaListenerContainerFactory()` without setting `@Value` fields. Cause: test setup, not production config. Fixed by populating injected fields with `ReflectionTestUtils` before factory creation.
- 2026-05-13: Initial final Compose health validation failed after stack startup. Signatures: frontend probe attempted `http://127.0.0.1:3000/api/health` while Compose published `3003->3000`, and gateway restarted with Nginx `proxy_http_version directive is duplicate`. Cause: validator used stale/default host port and `/ws/analytics` duplicated a directive already supplied by `proxy_params.conf`. Fixed by resolving frontend port from live Compose metadata and removing the duplicate Nginx directive.
- 2026-05-13: First resumed Loop 1 identity focused test attempt failed before `AuthServiceTest` ran because the local Maven cache had a stale `legent-security` artifact missing existing allow-expired JWT claim accessor methods. Cause: local dependency artifact drift, not source regression. Fixed by running `.\mvnw.cmd -pl shared/legent-security -DskipTests install`, then rerunning identity focused tests successfully.
- 2026-05-13: Early resumed Loop 2 Maven validation attempts failed from PowerShell argument handling: unquoted comma-separated `-Dtest=...` failed parsing, and unquoted `-Dsurefire.failIfNoSpecifiedTests=false` was treated as lifecycle phase `.failIfNoSpecifiedTests=false`. Correct pattern: quote comma-containing and dotted `-D` arguments, e.g. `".\mvnw.cmd ... \"-Dtest=A,B\" \"-Dsurefire.failIfNoSpecifiedTests=false\" test"`.
- 2026-05-13: First resumed Loop 2 frontend build failed in `frontend/src/lib/sanitize-html.ts` because `emailProfile.ADD_ATTR` has DOMPurify type `string[] | callback` and was spread as an array. Fixed by using a concrete `emailAdditionalAttrs` string-array constant; `npx tsc --noEmit` and `npm run build` passed.
- 2026-05-13: Loop 5 content security focused test setup failed across several attempts before passing. Signatures: timeout before result, missing/invalid `SecurityProperties` test bean, `@WebMvcTest` importing full app/JPA auditing, `@SpringBootTest` booting DB/Flyway with placeholder `DB_USER`, then missing JWT filter bean. Cause: security-slice test context was too broad or incomplete. Final working pattern: narrow test application with DB/Flyway/JPA auto-config exclusions and explicit `SecurityProperties`, `JwtTokenProvider`, and `JwtAuthenticationFilter` beans.
- 2026-05-13: Loop 5 production overlay required-secret validation first failed on `DB_USER` even though rendered output had the key. Cause: regex expected `- secretKey:` on one line, but Kustomize renders `secretKey:` as a child field after `remoteRef`. Fixed by matching `^\s*secretKey: KEY`.
- 2026-05-13: Loop 5 foundation focused test first failed strict Mockito because the fixture model lacked `workspace_id`, causing workspace-scoped update stubs to mismatch with `null`. Cause: outdated fixture, not implementation failure. Fixed by adding fixture workspace metadata and stubbing `updateByIdAndWorkspace`.
- 2026-05-13: Final sanitizer Playwright rerun with the default web-server path timed out after 20 minutes before output, while the backslash Windows path form failed immediately with "No tests found." Cause: command invocation/path handling for a spec that imports sanitizers and does not need a web server. Safer pattern: use Playwright's slash-style matcher and set `PLAYWRIGHT_SKIP_WEB_SERVER=1` for `sanitize-html.spec.ts`; that rerun exited 0.
- 2026-05-13: Final `.env.example` validation first failed because placeholder values for `LEGENT_SECURITY_JWT_SECRET` and `LEGENT_DELIVERY_CREDENTIAL_KEY` were shorter than their validator minimums. Cause: example placeholder drift, not a runtime secret issue. Fixed by lengthening placeholders without adding secrets; `.env.example` validation then passed.

Rule:

- When a fix fails, record command, error signature, attempted change, why it failed, and next safer approach.
