# Technical Debt

Fresh baseline date: 2026-05-20.

Accepted debt from 2026-05-20 audit:

| ID | Owner | Impact | Score | Next Action | Validation Required |
|---|---|---|---:|---|---|
| internal-routes-edge-blocking | API_ARCHITECT | FIXED: public Nginx and ingress now deny source-discovered service-internal controller routes, and route validation checks the source scan against Nginx and ingress coverage. | 61 | Preserve source-discovered route validation when adding controller routes, route map entries, or public-edge rules. | `scripts\ops\validate-route-map.ps1`, Kustomize render. |
| route-map-ingress-prefix-coverage | API_ARCHITECT | FIXED_LOCAL: route validation now parses Kubernetes ingress path/backend pairs and proves every route-map prefix is first-matched to the expected owning service, including regex alternations and specific-before-broad ownership. | 47 | Preserve prefix/backend coverage checks whenever route ownership, ingress grouping, or tracking split ingress rules change. | Route validation, missing-prefix negative fixture, production Kustomize render, production overlay validation, repo artifact hygiene, Codex validation, scoped `git diff --check`. |
| audience-tracking-intelligence-batch-consumer | AUDIENCE_SERVICE_OWNER | FIXED_LOCAL: audience-service `tracking.ingested` intelligence consumption now uses an audience-scoped batch Kafka listener with batch ack, bounded poll records, in-batch duplicate skip, invalid/unsupported event skip, and retry on service failures. | 50 | Collect target Kafka lag, DB contention, and subscriber intelligence update metrics before any high-volume intelligence throughput claim. | Focused audience Kafka/consumer/intelligence tests, full audience-service gate, repo artifact hygiene, Codex validation, scoped `git diff --check`. |
| audience-bulk-suppression-check | AUDIENCE_SERVICE_OWNER | FIXED: audience resolution now uses a bounded deliverability bulk-check API instead of broad suppression-list fetches. | 58 | Monitor live resolution/load metrics before any high-volume throughput claims. | `.\mvnw.cmd -pl services/audience-service,services/deliverability-service -am test`. |
| audience-import-error-cap | AUDIENCE_SERVICE_OWNER | FIXED_LOCAL: import processing now caps retained row-error samples at `AppConstants.IMPORT_MAX_ERRORS` during chunk processing while preserving full `errorRows` counts. | 49 | Add target import-load evidence before high-volume import readiness claims. | Focused import tests, full audience-service gate, repo artifact hygiene, Codex validation, scoped `git diff --check`. |
| audience-subscriber-by-id-scoped-lookups | AUDIENCE_SERVICE_OWNER | FIXED_LOCAL: subscriber get/update/delete now use tenant/workspace/id scoped repository access instead of raw ID lookup plus in-memory filtering. | 54 | Preserve scoped lookup coverage when adding subscriber profile mutation paths. | Focused `SubscriberServiceTest`, full audience-service gate, repo artifact hygiene, Codex validation, scoped `git diff --check`. |
| identity-invitation-list-workspace-scope | IDENTITY_SERVICE_OWNER | FIXED_LOCAL: identity invitation listing now fails closed without a workspace claim and queries invitations by tenant+workspace instead of tenant-only. | 56 | Preserve scoped invitation listing when invitation admin surfaces expand; design any tenant-global invitation admin path separately. | Focused identity service/controller tests, full identity-service gate, repo artifact hygiene, Codex validation, scoped `git diff --check`. |
| campaign-send-checkpoint-workspace-scope | CAMPAIGN_SERVICE_OWNER | FIXED_LOCAL: send-job checkpoint/resume/recovery compatibility tables and service paths now carry workspace ownership and use tenant/workspace/job-scoped repository lookups before checkpoint reads or retry mutation. | 59 | Collect target Flyway migration evidence for legacy rows and monitor resumed-job behavior under real recovery scenarios. | Focused checkpointing/send-execution/consumer/scheduler tests, full campaign-service gate, repo artifact hygiene, JPA config scan, Codex validation, scoped `git diff --check`. |
| deliverability-legacy-reputation-scope | DELIVERABILITY_SERVICE_OWNER | FIXED_LOCAL: legacy `reputation_scores` reads and writes now use tenant/workspace scoped latest-row lookups, and a forward migration removes the old global domain uniqueness bottleneck. | 61 | Collect target Flyway migration evidence before promotion; keep `domain_reputations` as the primary current reputation model. | Focused reputation controller/repository/service tests, full deliverability-service gate, repo artifact hygiene, JPA config scan, Codex validation, scoped `git diff --check`. |
| deliverability-internal-suppression-security-chain-coverage | DELIVERABILITY_SERVICE_OWNER | FIXED_LOCAL: deliverability internal suppression routes now have security-chain tests proving anonymous service-route pass-through and normal auth/RBAC enforcement for workspace routes. | 42 | Preserve this test pattern when adding deliverability service-internal endpoints. | Focused `SecurityConfigTest`, full deliverability-service gate, route validation, repo artifact hygiene, Codex validation, scoped `git diff --check`. |
| delivery-feedback-outbox | DELIVERY_SERVICE_OWNER | FIXED: delivery feedback now persists to a leased outbox with stable event IDs/idempotency keys and retries Kafka publication after commit. | 58 | Add retention/cleanup and live backlog alerts before high-volume claims. | `.\mvnw.cmd -pl services/delivery-service -am test`, Codex validation, repo artifact hygiene, `git diff --check`. |
| frequency-optimization-deterministic-policy-contract | DELIVERABILITY_SERVICE_OWNER | TESTED_LOCAL: deterministic frequency optimization policy evaluation now exposes readiness, fallback, saturation, recommended cap, confidence, approval/rollback, and safety-gate outputs; campaign safety tests prove tenant/workspace frequency caps suppress before delivery handoff. | 58 | Keep live cadence changes, model-backed optimization, provider-capacity proof, and parity claims blocked until separate implementation and target evidence exist. | Focused foundation optimization tests, focused campaign safety tests, `.\mvnw.cmd -pl services/foundation-service,services/campaign-service -am test`, Codex validation, repo artifact hygiene, `git diff --check`. |
| ai-content-assistance-governance | FOUNDATION_SERVICE_OWNER | TESTED_LOCAL: foundation policy/audit control plane now enforces draft-only AI content assistance governance with tenant/workspace scope, provider disclosure, data-class controls, opt-in/out, kill switch, human review, hash-only evidence, and publish/send denial. | 57 | Keep model provider calls, generated draft application, content-service publish/test-send hooks, usage metering, and AI parity claims as separate reviewed slices. | `.\mvnw.cmd -pl services/foundation-service -Dtest=AiContentAssistanceGovernanceServiceTest test`, `.\mvnw.cmd -pl services/foundation-service,services/content-service -am test`, Codex validation, repo artifact hygiene, `git diff --check`. |
| ai-content-assistance-draft-application-contract | CONTENT_SERVICE_OWNER | FIXED_LOCAL: content-service can now apply already approved, human-reviewed AI content assistance evidence to template drafts only, storing hash/reference metadata and blocking unresolved AI assistance metadata before publish or test-send. | 52 | Keep model/provider invocation, cross-service foundation evidence verification, raw prompt/output storage, generated-draft UX, metering, and parity claims as separate reviewed slices. | Focused AI governance/content workflow/test-send tests, full foundation+content service gate, repo artifact hygiene, Codex validation, scoped `git diff --check`. |
| automation-activity-orchestration-contract | AUTOMATION_SERVICE_OWNER | TESTED_LOCAL: SQL/import execution is proven locally; dependency metadata, failure policy, bounded run history, capability UI, config redaction, scoped automation artifacts, artifact-ID import handoff, validation-only file-drop/extract dry runs, audience object-key hardening, webhook platform-event handoff, terminal notification execution, and platform webhook response redaction are implemented locally. Multi-step execution, send handoff, live file movement, and script sandboxing remain separate slices. | 48 | Select send handoff only after campaign governance is ready; keep live file movement blocked until storage-adapter evidence exists; keep script blocked until sandbox evidence exists. | Full automation/platform/audience/shared backend gates as relevant, frontend lint/build/Playwright, route validation, Codex validation, repo artifact hygiene, `git diff --check`. |
| automation-delay-resume-workspace-scope | AUTOMATION_SERVICE_OWNER | FIXED_LOCAL: Quartz delayed workflow resumes now pass scheduled tenant/workspace scope into `WorkflowEngine`, which requires scope before lookup and uses tenant+workspace scoped instance access. | 56 | Preserve scoped delayed-resume tests when adding scheduled, retry, or recovery resume paths. | Focused workflow engine/Quartz job tests, full automation-service gate, repo artifact hygiene, Codex validation, scoped `git diff --check`. |
| email-governance-policy-objects | CONTENT_SERVICE_OWNER | TESTED_LOCAL: content-service now owns tenant/workspace-scoped send governance policy objects and campaign preflight/launch validates selected policies before send handoff. Delivery runtime snapshots, UI policy-management depth, and target migration/edge evidence remain separate work. | 50 | Resume with `automation-send-activity-handoff` or a delivery/message policy snapshot slice if governed automation sends or immutable delivery handoff evidence are next. | Focused content/campaign tests, full `.\mvnw.cmd -pl services/content-service,services/campaign-service,services/delivery-service -am test`, route validation, Codex validation, repo artifact hygiene, `git diff --check`. |
| content-read-rbac-coverage | CONTENT_SERVICE_OWNER | FIXED_LOCAL: protected content-service GET mappings now require method-level content read/template authorization, and reflection tests enforce read RBAC except documented public/internal routes. | 49 | Preserve read-route reflection coverage when adding content controllers; add exceptions only for documented public or guarded service-internal routes. | Focused `ContentControllerRbacTest`, full content-service gate, repo artifact hygiene, Codex validation, scoped `git diff --check`. |
| campaign-send-content-reference-contract | CAMPAIGN_SERVICE_OWNER | FIXED: campaign send handoff now always creates and publishes a durable `contentReference` before `email.send.requested`; shared Kafka tests reject inline-only or blank-reference send payloads. | 56 | Review reference-only mode for production high-volume campaigns after target-like load evidence. | `.\mvnw.cmd -pl services/campaign-service,shared/legent-kafka -am test`, Codex validation, repo artifact hygiene, `git diff --check`. |
| campaign-scheduled-publish-workspace-context | CAMPAIGN_SERVICE_OWNER | FIXED_LOCAL: scheduled campaign job processing now installs tenant/workspace context from the claimed job row before publishing audience-resolution requests and restores prior thread context afterward. | 56 | Preserve scheduled-worker context wrappers whenever campaign scheduled paths publish Kafka events through context-dependent publishers. | Focused scheduler/publisher tests, full campaign-service gate, repo artifact hygiene, Codex validation, scoped `git diff --check`. |
| egress-evidence-validator-hardening | RELEASE_MANAGER | FIXED: validator now rejects template/example evidence, placeholder values, documentation CIDRs, unsupported FQDN evidence, broad/non-canonical CIDRs, future dates, and unsupported schema versions. | 54 | Keep template validation as an expected negative test; real production egress evidence is still external. | `scripts\ops\test-release-evidence-validators.ps1`, expected-fail template validation, local release gate, Codex validation, `git diff --check`. |
| release-ga-image-schema-version-validation | RELEASE_MANAGER | FIXED_LOCAL: GA and image release evidence validators now require supported manifest `schemaVersion` 1 before accepting local evidence paths or image entries. | 43 | Preserve version checks when evolving evidence manifest contracts; real target artifacts remain external release blockers. | Release evidence self-test, local release gate, repo artifact hygiene, Codex validation, scoped `git diff --check`. |
| release-gate-strict-skip-guard | RELEASE_MANAGER | FIXED_LOCAL: strict non-local release-gate mode now rejects local gate skip flags before evidence validation, while local-only mode keeps skip support for non-promotional validation. | 53 | Preserve the distinction between strict promotion and local-only validation when release-gate flags evolve. | Release evidence self-test, local-only release gate, repo artifact hygiene, Codex validation, scoped `git diff --check`. |
| kafka-dlq-sharding | KAFKA_CONTRACTS_OWNER | FIXED: shared listener DLQ routing preserves source partitions, Java/Compose define six DLQ partitions, and tests guard DLQ/source-topic partition compatibility. | 51 | Add DLQ-specific observability and production Kafka topology evidence before high-volume claims. | `.\mvnw.cmd -pl shared/legent-kafka -am test`, Compose config, drift scan, Codex validation, repo artifact hygiene, `git diff --check`. |
| journey-runtime-node-contract | AUTOMATION_SERVICE_OWNER | FIXED: Journey Builder, validation, simulation, publish, rollback/resume, and runtime handler paths now distinguish draft-only nodes from live runtime-supported nodes and fail closed before publish/execution. | 48 | Implement broader journey node families only as separate validated runtime slices. | `.\mvnw.cmd -pl services/automation-service -am test`, frontend lint/build, targeted Playwright builder spec, Codex validation, `git diff --check`. |
| tracking-ingress-rate-policy | DEVOPS_ENGINEER | FIXED-CONFIG: Kubernetes now splits tracking ingestion from analytics/websocket ingress and declares the reviewed `/api/v1/tracking` ingress-nginx `limit-rps` posture, with route validation enforcing local/Kubernetes drift checks. | 43 | Keep live rate-limit and downstream ingestion proof as release evidence, not a code-complete claim. | `scripts\ops\validate-route-map.ps1`, Kustomize render, Codex validation, `git diff --check`. |
| tracking-ingestion-batch-consumer-readiness | TRACKING_SERVICE_OWNER | TESTED_LOCAL_BLOCKED: local batch listener, raw-write/finalization ledger phases, long-horizon stale-claim retry, Docker-independent migration check, and ClickHouse schema repair are implemented; Docker/PostgreSQL and ClickHouse dedupe/reconciliation evidence remain missing. | 55 | Collect runtime evidence for idempotency, ambiguous/partial batch writes, and raw-events dedupe/reconciliation before marking DONE or making throughput/BI claims. | Tracking focused tests, full shared Kafka plus tracking gate, Codex validation, lease validation, artifact hygiene, scoped `git diff --check`, plus future Docker/ClickHouse evidence. |
| sso-tenant-cookie-httponly | IDENTITY_SERVICE_OWNER | FIXED: SSO callback tenant cookies now use the same HTTP-only posture as normal auth while preserving access/refresh cookie path scope. | 42 | Preserve parity if new OIDC/SAML callback variants are added. | `.\mvnw.cmd -pl services/identity-service -am test`, Codex validation, repo artifact hygiene, `git diff --check`. |
| delivery-provider-workspace-isolation | DELIVERY_SERVICE_OWNER | FIXED: delivery provider configuration and provider-linked operational rows now carry tenant+workspace ownership through CRUD, selection, health, capacity, failover, and forward migration constraints. | 65 | Review legacy `workspace-default` backfills against real workspace ownership before strict production promotion. | `.\mvnw.cmd -pl services/delivery-service -am test`, Codex validation, repo artifact hygiene, `git diff --check`. |
| production-egress-policy-render-gate | DEVOPS_ENGINEER | FIXED: strict egress validation now proves the reviewed external egress NetworkPolicy is generated or checked, rendered through a temporary production Kustomize overlay, hash-matched to evidence, and covered by app-labeled Deployment pod templates. | 47 | Keep real target-environment egress evidence as a production release requirement; do not check generated proof artifacts into the normal local overlay. | `scripts\ops\test-release-evidence-validators.ps1`, production overlay validation, Kustomize render, local release gate, Codex validation, repo artifact hygiene, `git diff --check`. |
| release-evidence-validator-negative-fixtures | RELEASE_MANAGER | FIXED: release evidence self-test now covers GA manifest success plus missing/unsupported schema version, placeholder, missing artifact, absolute path, path escape, and missing field failures; image evidence now covers missing/unsupported schema version, missing production image, extra image, digest mismatch, absolute evidence path, and path escape failures. | 41 | Keep this local-only; real production evidence remains blocked until target artifacts are collected and strict release gate runs. | `scripts\ops\test-release-evidence-validators.ps1`, local release gate, repo artifact hygiene, Codex validation, `git diff --check`. |
| observability-alert-runbook-link-validation | SRE_MONITORING_ENGINEER | FIXED_LOCAL: production alert `runbook_url` annotations now resolve to a checked-in runbook file and anchors, and production overlay validation fails on missing local runbook targets. | 40 | Collect target Prometheus/Alertmanager firing and routing evidence before GA monitoring handoff claims. | Production overlay validation, negative runbook link fixtures, local release gate, repo artifact hygiene, Codex validation, scoped `git diff --check`. |
| latest-audit-safe-local-followups-20260522 | PROGRAM_MANAGER_AGENT | FIXED_LOCAL: latest audit follow-ups closed alert runbook coverage, reproducible Compose env validation, delivery legacy workspace guard, identity forgot-password scope resolution, tracking publisher cleanup, SES region parsing coverage, subscriber bulk-delete/query E2E, and landing-page public/studio E2E. | 69 | Keep production release, high-volume, provider capacity, target migration, and monitoring handoff evidence blocked until collected from target environments. | Focused delivery/identity/tracking tests, full delivery+identity+tracking backend gate, frontend lint/build/focused Playwright, production overlay, release evidence self-test, Compose config, local-only release gate, artifact hygiene, Codex validation, `git diff --check`. |
| frontend-forgot-password-tenant-hint | FRONTEND_OWNER | FIXED_LOCAL: public account recovery now accepts optional tenant/workspace hints, trims them, omits blanks, and keeps the generic accepted state. | 31 | Preserve no-storage and generic-response behavior when changing public auth forms. | Frontend lint, build, and marketing Playwright. |
| campaign-batch-requeue-paging | CAMPAIGN_SERVICE_OWNER | FIXED_LOCAL: retry/requeue paths now use bounded scoped batch reads and publish only for rows successfully claimed for retry. | 50 | Collect target multi-replica retry and queue-lag evidence before throughput claims. | Focused campaign tests, full foundation plus campaign reactor, scoped diff check. |
| foundation-global-enterprise-workspace-fail-closed | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: workspace-owned global-enterprise surfaces now require workspace context and exact workspace predicates instead of nullable wildcard matching. | 44 | Decide any tenant-global/null-workspace visibility policy separately before widening reads. | Focused foundation tests, full foundation plus campaign reactor, scoped diff check. |
| foundation-performance-ledger-workspace-fail-closed | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: performance-ledger scoped reads/writes now require workspace context and avoid nullable workspace wildcard behavior. | 44 | Preserve missing-workspace tests for optimization, extension governance, personalization, and benchmark surfaces. | Focused foundation performance tests, full foundation plus campaign reactor, scoped diff check. |
| product-docs-parity-backlog-split | PRODUCT_MANAGER_AGENT | FIXED_DOCS: parity docs now distinguish completed local contracts from narrow future/evidence-bound follow-ups without new external claims. | 28 | Refresh external market sources before any new parity claim or roadmap promise. | Docs diff check plus Codex validation. |
| codex-checkpoint-audit-hygiene-20260522 | DOCUMENTATION_ENGINEER | FIXED_INTERNAL: audit event casing validation and completed checkpoint statuses are normalized while preserving history. | 26 | Keep checkpoint and audit cleanup dry-run first; compact lease history only after behavior review. | Codex lifecycle/system validation, cleanup dry-run, scoped diff check. |
| platform-event-idempotency | PLATFORM_SERVICE_OWNER | FIXED_LOCAL: platform consumers now claim event/idempotency identity before side effects and skip duplicate replay. | 46 | Collect target migration and Kafka replay evidence before release claims. | `.\mvnw.cmd -pl services/platform-service -am test`, Codex validation, repo artifact hygiene, `git diff --check`. |
| campaign-legacy-batch-eligibility-marker | CAMPAIGN_SERVICE_OWNER | REVIEW: audience resolution final eligibility is fixed locally, but `SendExecutionService` still has a legacy JSON recipient payload path without durable proof that recipients came through audience eligibility. | 71 | Review and implement an eligibility marker/contract for campaign batch rows and legacy payload compatibility before closing `campaign-audience-eligibility-final-gate`. | Campaign and audience focused tests plus full relevant backend module gates. |
| clickhouse-experiment-lineage-rollup-idempotency | TRACKING_SERVICE_OWNER | FIXED_LOCAL: raw ClickHouse analytics now preserve experiment lineage and campaign-day rollup refresh replaces the scoped date window before insert. | 52 | Prove target ClickHouse schema migration and refresh behavior with environment data before release or BI-grade experiment claims. | `.\mvnw.cmd -pl services/tracking-service -am test`, Codex validation, repo artifact hygiene, `git diff --check`. |
| flow-analytics-experimentation | TRACKING_SERVICE_OWNER | TESTED_LOCAL: workflow analytics now reports bounded step metrics, observed paths, path-test target counts, exit-goal hits, and deterministic diagnostics; tracking conversion events and raw analytics schemas now carry journey goal lineage separately from campaign experiment fields. | 39 | Add tracking-owned workflow-topic ingestion/rollups and collect target ClickHouse migration/load evidence before BI-grade journey analytics or winner automation claims. | Focused tracking/automation/Kafka tests, full `.\mvnw.cmd -pl services/tracking-service,services/automation-service -am test`, frontend lint/build/analytics Playwright, Codex validation, repo artifact hygiene, `git diff --check`. |
| codex-lifecycle-state-atomicity | PROGRAM_MANAGER_AGENT | FIXED_CORE: core promote/start/acquire/checkpoint/update/complete/thread heartbeat/audit mutations now share a named state lock and atomic JSON writes; start now creates lease/checkpoint before moving queue state, and acquire rejects overlaps before persisting. | 40 | Convert less-used cleanup/worktree/module-lease utilities in a separate scoped slice if heavy multi-thread operations expand beyond the core lifecycle path. | `powershell -ExecutionPolicy Bypass -File .codex\utilities\test-codex-lifecycle-state.ps1`, Codex validation, lease validation, parser check, `git diff --check`. |
| codex-state-dashboard-convergence | PROGRAM_MANAGER_AGENT | FIXED_INTERNAL: dashboard item IDs now render as Markdown code spans, team-state queue mirrors match queue.json, and Codex validation catches ready/backlog/active mirror drift. | 24 | Keep future manual queue edits synchronized through Codex state utilities so validation stays green. | `monitor-autonomous-org.ps1`, `validate-codex-system.ps1`, `cleanup-autonomous-org.ps1 -DryRun`, and scoped `.codex` `git diff --check`. |
| data-extension-governance-migration-tests | AUDIENCE_SERVICE_OWNER | TESTED_LOCAL: data-extension workspace-scope migration contracts and service governance behaviors now have focused tests for relationship scoping, field validation, sendable-field validation, retention clearing, and scoped soft delete. | 38 | Add PostgreSQL/Flyway runtime migration proof with representative legacy rows before release claims. | Focused data-extension tests, `.\mvnw.cmd -pl services/audience-service -am test`, JPA config scan, Codex validation, repo artifact hygiene, `git diff --check`. |
| predictive-segments-governance | AUDIENCE_SERVICE_OWNER | TESTED_LOCAL: predictive segment preview contract and fail-closed create/evaluate/recompute guards now require tenant policy, source/data-class governance, protected-data exclusion, freshness/count thresholds, bias/drift pass, approval audit fields, and rollback evidence before materialization. | 53 | Add model provider/model-card evidence, chunked materialization, durable rollback snapshots, and linkage to the first-class AI policy/audit control plane before AI parity or production claims. | Focused predictive governance tests, full `.\mvnw.cmd -pl services/audience-service -am test`, repo artifact hygiene, Codex validation, `git diff --check`. |
| contact-data-designer-preview-governance | AUDIENCE_SERVICE_OWNER | TESTED_LOCAL: no-schema backend hardening now validates nested data-extension metadata, relationship cardinality/type/key compatibility, sendable required/primary-key invariants, preview field/filter/sort shapes, sort-before-projection behavior, and segment rejection of unsupported data-extension relationship metadata. | 49 | Keep parent `contact-data-designer-governance` open for additive provenance/classification/audit tables, import/source metadata, indexed relationship execution, controller API validation, and frontend relationship-designer controls. | Focused `DataExtensionServiceTest,SegmentServiceTest`, full `.\mvnw.cmd -pl services/audience-service -am test`, Codex validation, repo artifact hygiene, scoped `git diff --check`. |
| contact-data-designer-governance | AUDIENCE_SERVICE_OWNER | TESTED_LOCAL: data-extension governance metadata, field classification, scoped governance audit persistence, and frontend source/classification/audit controls now exist for create and governance update workflows. | 49 | Continue with target migration proof, import/contact provenance population, and indexed relationship execution as separate slices. | Focused data-extension governance tests, full `.\mvnw.cmd -pl services/audience-service -am test`, frontend lint/build/targeted Playwright, Codex validation, `git diff --check`. |
| automation-send-activity-handoff-review | AUTOMATION_SERVICE_OWNER | FIXED_LOCAL: send handoff review findings are fixed locally with claim/publish/mark/release idempotency, normalized unsafe override detection, strict `send.requested` confirmation/idempotency validation, and fail-closed subscriber-trace campaign handoff markers. | 63 | Keep target Kafka replay, provider capacity, deliverability, production send, and high-volume evidence as separate release blockers. | Focused automation/campaign/shared Kafka tests, full automation+campaign backend gate, Codex validation, `git diff --check`. |
| mode-aware-workflow-contract | FRONTEND_OWNER | FIXED_LOCAL: BASIC/ADVANCED mode now has a typed frontend metadata contract, render-time Settings nav filtering, separate Admin role gating, and campaign Experiment Engine render/payload gating. | 38 | Continue migrating budget/frequency/template/automation advanced controls in separate slices; keep backend authorization and safety defaults authoritative. | `cd frontend; npm run lint`, `npm run build:ci`, targeted Playwright `ui-mode` and campaign specs, smoke E2E, Codex validation, `git diff --check`. |
| frontend-audience-import-status-lifecycle | FRONTEND_OWNER | FIXED_LOCAL: audience import list, wizard, and detail views now classify backend `VALIDATING`, `PROCESSING`, `COMPLETED`, `COMPLETED_WITH_ERRORS`, `FAILED`, and `CANCELLED` states consistently. | 39 | Keep backend import worker, parser, storage, and throughput proof as separate validation surfaces. | Frontend lint, frontend production build, targeted Chromium Playwright import lifecycle spec, repo artifact hygiene, Codex validation, scoped `git diff --check`. |
| workspace-context-fail-closed-e2e | FRONTEND_OWNER | FIXED_LOCAL: workspace route E2E coverage now proves a session without workspace context clears local routing context and redirects to login instead of rendering the app shell. | 49 | Preserve the mocked missing-workspace case whenever workspace layout, context bootstrap, or auth/session hydration changes. | Frontend lint, production build, targeted Chromium Playwright `context-bootstrap.spec.ts`, repo artifact hygiene, Codex validation, scoped `git diff --check`. |
| campaign-budget-frequency-mode-contract | FRONTEND_OWNER | FIXED_LOCAL: Campaign Budget Guard and Workspace Frequency Policy now use ADVANCED_ONLY metadata, are absent from BASIC DOM/focus order, and skip hidden advanced budget/frequency API payloads in BASIC. | 36 | Continue with Template Studio and Automation Studio mode-metadata slices; keep simple Frequency Cap visible in BASIC as a safety control unless product/backend owners decide otherwise. | `cd frontend; npm run lint`, `npm run build:ci`, targeted campaign/UI-mode Playwright, Codex validation, `git diff --check`. |
| automation-mode-contract | FRONTEND_OWNER | FIXED_LOCAL: Automation Studio activity authoring/execution, manual workflow trigger, and draft-only journey-node saving now respect BASIC/ADVANCED mode metadata without treating UI mode as authorization. | 37 | Keep backend workflow/activity validation authoritative; future automation parity work remains separate from UI mode metadata. | `cd frontend; npm run lint`, `npm run build:ci`, targeted automation/UI-mode Playwright, Codex validation, `git diff --check`. |
| template-studio-mode-contract | FRONTEND_OWNER | FIXED_LOCAL: Template Studio advanced tabs/actions, builder advanced blocks, conditional rules, raw HTML snapshot, and optional advanced resource loads now respect BASIC/ADVANCED mode metadata; BASIC save payloads scrub conditional block settings. | 33 | Keep content-service authorization and sanitization authoritative; future Template Studio parity work should add backend-backed capability/permission checks rather than relying on UI mode. | `cd frontend; npm run lint`, `npm run build:ci`, targeted Template Studio/UI-mode Playwright, Codex validation, `git diff --check`. |
| admin-settings-context-mismatch-fail-closed | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: admin settings service-level validation now rejects request-body workspace/environment mismatches and reset missing scoped context before config changes can cross scope. | 52 | Continue with tenant lifecycle, config history, and differentiation ownership hardening from the foundation audit. | Focused AdminSettingsServiceTest, full `.\mvnw.cmd -pl services/foundation-service -am test`, `git diff --check`. |
| config-by-id-tenant-scope | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: ConfigService update/delete now resolve by current tenant and non-deleted status, then fail closed for missing or mismatched workspace/environment context before mutation. | 54 | Design explicit privileged global-config administration and scope-aware config version history in separate slices. | Focused ConfigServiceTest, full `.\mvnw.cmd -pl services/foundation-service -am test`, `git diff --check`. |
| config-create-upsert-context-mismatch-fail-closed | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: ConfigService create/upsert now rejects body workspace/environment mismatches before scope lookup or mutation. | 41 | Add schema-backed config version-history scoping separately. | Focused ConfigServiceTest, full `.\mvnw.cmd -pl services/foundation-service -am test`, `git diff --check`. |
| differentiation-upsert-workspace-exact-match | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: Differentiation upsert lookup now uses null-safe exact workspace matching instead of broad null-workspace matching before update. | 48 | Review list/evaluate query semantics separately before changing tenant-level plus workspace-level visibility behavior. | Focused DifferentiationPlatformServiceTest, full `.\mvnw.cmd -pl services/foundation-service -am test`, `git diff --check`. |
| public-contact-admin-platform-admin-only | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: global public contact request admin list/status endpoints now require PLATFORM_ADMIN because the table has no tenant/workspace ownership columns. | 44 | Design tenant/workspace contact inbox only with schema migration and product ownership decision. | Focused AdminContactRequestControllerSecurityTest/PublicContactServiceTest, full `.\mvnw.cmd -pl services/foundation-service -am test`, `git diff --check`. |
| tenant-get-self-scope | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: TenantService.getTenant now fails closed unless the path tenant ID equals TenantContext before repository lookup. | 45 | Decide platform-admin versus self-tenant policy for tenant list, get-by-slug, and lifecycle mutations. | Focused TenantServiceTest, full `.\mvnw.cmd -pl services/foundation-service -am test`, `git diff --check`. |
| foundation-tenant-config-ownership | FOUNDATION_SERVICE_OWNER | OPEN: read-only foundation audits still show tenant lifecycle list/get-by-slug/mutation policy gaps; config update/delete raw-ID access, config create/upsert context mismatch, differentiation null-workspace upsert/evaluate matching, config version history scoping, and getTenant self-scope are fixed locally. | 64 | Design policy-backed tenant lifecycle controls. | Focused service tests plus full foundation-service gate for each slice. |
| foundation-differentiation-evaluate-scope | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: decision-policy evaluation, omnichannel simulation, and SLO evaluation now use exact nullable workspace matching instead of null-workspace wildcard predicates; focused tests capture SQL/params for current and null workspace contexts. | 50 | Leave inherited tenant/global fallback and list/inventory visibility semantics for an explicit product/security decision. | Focused `DifferentiationPlatformServiceTest` (9 tests), full `.\mvnw.cmd -pl services/foundation-service -am test`, Codex validation, monitor check, lease validation, and scoped `git diff --check`. |
| foundation-core-platform-workspace-guards | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: team, department, membership, role binding, access grant, and permission-group tenant mismatch creation paths now fail closed on cross-workspace or cross-tenant relationships before insert. | 51 | Preserve relationship-guard regression coverage. | Focused `CorePlatformServiceTest` (28 tests), full `.\mvnw.cmd -pl services/foundation-service -am test`, Codex validation, monitor check, lease validation, and scoped `git diff --check`. |
| foundation-core-platform-read-scope | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: role-aware core-platform role-binding/access-grant lists and access-policy preview now scope workspace-readable callers to current workspace while tenant-wide roles retain tenant-wide reads. | 51 | Decide inherited/null-workspace visibility semantics before broadening workspace reads beyond exact workspace rows; consider typed preview membership proof separately. | Focused `CorePlatformServiceTest,CorePlatformControllerTest` (45 tests), full `.\mvnw.cmd -pl services/foundation-service -am test`, Codex validation, monitor check, lease validation, and scoped `git diff --check`. |
| foundation-compliance-privacy-request-workspace-scope | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: compliance privacy-request status update now requires current workspace context and updates by tenant+workspace+ID before audit evidence is written. | 46 | Preserve same-workspace, missing-workspace, and scoped-row-missing no-audit tests; decide nullable/tenant-global privacy request admin policy separately. | Focused `ComplianceEvidenceServiceTest` (5 tests), full `.\mvnw.cmd -pl services/foundation-service -am test`, Codex validation, monitor check, lease validation, repo artifact hygiene, and scoped `git diff --check`. |
| foundation-permission-list-json-defaulting | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: role-definition, permission-group, and delegated access grant creation now serializes omitted permission lists as JSON arrays `[]` instead of object-shaped `{}`. | 30 | Decide separately whether existing malformed rows need read-time normalization or data cleanup; preserve metadata `{}` defaulting. | Focused `CorePlatformServiceTest` (41 tests), full `.\mvnw.cmd -pl services/foundation-service -am test`, Codex validation, monitor check, lease validation, repo artifact hygiene, migration untouched check, and scoped `git diff --check`. |
| foundation-permission-group-tenant-context | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: permission group creation now rejects request `tenantId` values that differ from `TenantContext`, matching role-definition behavior. | 39 | Preserve permission-group tenant mismatch regression coverage. | Covered inside `foundation-core-platform-workspace-guards`; focused `CorePlatformServiceTest` and full foundation-service gate passed. |
| foundation-config-version-history-scope | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: config version history rows, queries, compare, and rollback now use exact nullable tenant/workspace/environment scope; rollback invalidates/publishes config side effects and scoped version numbers have a database uniqueness guard. | 54 | Preflight target databases for legacy duplicate tenant/key/version rows before applying V17; add retry handling only if same-scope concurrent version races surface in runtime evidence. | Focused config versioning/config/admin/controller tests (38 tests), full `.\mvnw.cmd -pl services/foundation-service -am test`, Codex validation, monitor check, repo artifact hygiene, and scoped `git diff --check`. |
| segment-rule-builder-operator-filtering | FRONTEND_OWNER | FIXED: SegmentRuleBuilder now filters list operators to `list_membership`, filters scalar operators away from `list_membership`, and normalizes invalid loaded/edited field-operator combinations before submit. | 33 | Keep backend fail-closed validation authoritative; future list picker work should validate actual list IDs without trusting client filtering. | `cd frontend; npm run lint`, `npm run build:ci`, targeted Playwright segment spec, Codex validation, repo artifact hygiene, `git diff --check`. |
| frontend-context-storage-comment | FRONTEND_OWNER | FIXED: frontend auth comments now distinguish HTTP-only session credentials from localStorage-backed tenant/workspace/environment routing context used for request headers. | 40 | Preserve this distinction when auth/context bootstrap changes. | `cd frontend; npm run lint`, Codex validation, repo artifact hygiene, scoped `git diff --check`. |

Initial debt-discovery targets:
- Very large service classes, route files, frontend consoles, and shared modules.
- Runtime/docs drift around ports, validation flags, and local commands.
- Missing focused tests around tenant isolation, event retries, high-volume paths, and route ownership.
- Release evidence gaps that local validators cannot satisfy.

Debt entries must include owner, impact, score, next action, and validation required for removal.
| campaign-send-execution-campaign-workspace-scope | CAMPAIGN_SERVICE_OWNER | FIXED_LOCAL: send execution campaign lookup now uses the claimed batch workspace and fails closed before rendering or send handoff when the campaign is not in that workspace. | 57 | Preserve scoped campaign lookup when changing batch execution or retry paths; legacy eligibility-marker compatibility remains separate. | Focused `SendExecutionServiceTest`, full campaign-service gate, repo artifact hygiene, Codex validation, scoped `git diff --check`. |
| parallel-safe-local-slices-integration-20260522 | PROGRAM_MANAGER_AGENT | FIXED_LOCAL: integrated and review-hardened parallel local slices across security, frontend, audience schema, foundation input hygiene, route/release validators, and parity docs. | 69 | Continue with the next disjoint safe local work item; keep external production/high-volume/provider/migration evidence blocked. | Focused backend/frontend/release gates, route harness, local-only release gate, Codex validation, repo artifact hygiene, and `git diff --check`. |
| automation-schedule-trigger-job-fail-closed | AUTOMATION_SERVICE_OWNER | FIXED_LOCAL: scheduled workflow trigger execution now validates scoped active schedule ownership and malformed job data before context install or event publish. | 56 | Preserve schedule/workflow matching and context-clear tests when scheduler recovery paths expand. | Focused automation gate plus integrated backend module gate. |
| deliverability-domain-workspace-lookup | DELIVERABILITY_SERVICE_OWNER | FIXED_LOCAL: sender-domain side-effect paths now use tenant+workspace+ID ownership before challenge rotation or DNS verification. | 54 | Keep repository/controller/service scoped lookup coverage for future domain operations. | Focused deliverability gate plus integrated backend module gate. |
| content-template-events-workspace-envelope | CONTENT_SERVICE_OWNER | FIXED_LOCAL: content template workflow events and `content.published` now carry and validate workspace scope. | 50 | Review downstream consumer contract expectations before adding new content event shapes. | Focused content/shared Kafka gate plus integrated backend module gate. |
| platform-webhook-dispatch-fanout-bound | PLATFORM_SERVICE_OWNER | FIXED_LOCAL: webhook dispatch now pages active webhook configs and dispatches one bounded page of Monos at a time instead of loading/building all workspace webhooks. | 50 | Collect live webhook fanout/load evidence before throughput claims; consider configurability only if target metrics require it. | Focused platform tests plus full platform/tracking/delivery/shared backend gate. |
| runtime-config-guard-ddl-kafka-trust | SHARED_PLATFORM_OWNER | FIXED_LOCAL: production DDL auto modes fail closed and Kafka trusted packages are no longer runtime-widenable. | 52 | Preserve exact allowlist tests; production startup proof remains release evidence. | Focused shared tests plus full platform/tracking/delivery/shared backend gate. |
| tracking-outbox-publish-scope-claim | TRACKING_SERVICE_OWNER | FIXED_LOCAL: tracking outbox publish claim/reload now carries tenant/workspace scope from enqueue and poller rows. | 54 | Runtime Kafka/PostgreSQL evidence remains separate from this local scope fix. | Focused tracking outbox test plus full platform/tracking/delivery/shared backend gate. |
| delivery-feedback-outbox-publish-scope-claim | DELIVERY_SERVICE_OWNER | FIXED_LOCAL: delivery feedback outbox publish claim/reload now carries tenant/workspace scope and preserves non-deleted retry behavior. | 54 | Provider/replay/production send evidence remains external. | Focused delivery outbox test plus full platform/tracking/delivery/shared backend gate. |
| frontend-core-workspace-preflight-narrowing | FRONTEND_OWNER | FIXED_LOCAL: broad core API workspace-optional preflight was removed and core routes now require workspace context in the frontend client. | 45 | Preserve optional endpoint allowlist tests when adding frontend API roots. | API-client/admin Playwright, frontend lint, and production build. |
| identity-invitation-create-workspace-context-fail-closed | IDENTITY_SERVICE_OWNER | FIXED_LOCAL: invitation creation now fails closed without principal workspace context and rejects workspace mismatch before service side effects. | 49 | Preserve same-workspace/missing-workspace/mismatch tests when adding invitation admin flows. | Focused identity tests plus full identity/campaign/automation combined backend gate. |
| compose-health-env-isolation | DEVOPS_ENGINEER | FIXED_LOCAL: compose health validation now uses explicit env-file/compose-file arguments and self-test fixtures instead of ambient Compose state. | 45 | Running-container health still requires Docker/runtime evidence. | Compose health self-test, AllowNotRunning env-file check, Compose config, artifact hygiene, Codex validation. |
| campaign-dead-letter-page-bound | CAMPAIGN_SERVICE_OWNER | FIXED_LOCAL: campaign dead-letter list reads are bounded with default and max limits while preserving tenant/workspace/job scoping. | 41 | Collect target DLQ volume/load evidence before throughput claims. | Focused campaign engine test plus full identity/campaign/automation combined backend gate. |
| campaign-approval-workspace-scoped-lookups | CAMPAIGN_SERVICE_OWNER | FIXED_LOCAL: approval approve/reject/cancel mutations now scope approval lookup through tenant and owning campaign workspace before state changes. | 55 | Keep legacy campaign eligibility-marker policy blocked until the compatibility decision is made. | Focused campaign workflow test plus full identity/campaign/automation combined backend gate. |
| automation-workflow-run-history-page-bound | AUTOMATION_SERVICE_OWNER | FIXED_LOCAL: workflow run listing and run step/trace history now use bounded default/max reads. | 41 | Collect production workflow history volume evidence before scale claims. | Focused workflow studio test plus full identity/campaign/automation combined backend gate. |
| frontend-platform-workspace-preflight-narrowing | FRONTEND_OWNER | FIXED_LOCAL: platform notification and webhook frontend requests now require workspace context before dispatch. | 41 | Preserve optional endpoint allowlist tests for future API-client roots. | API-client Playwright, frontend lint, and production build. |
| frontend-reset-token-url-scrub | FRONTEND_OWNER | FIXED_LOCAL: public reset route now captures the reset credential once, scrubs query parameters from browser URL state, and proves no browser-storage persistence in Playwright. | 49 | Preserve URL-scrub and storage-boundary coverage when touching public auth views. | Marketing Playwright, frontend lint, production build, Codex validation, artifact hygiene, `git diff --check`. |
| outbox-backlog-observability-alerts | SRE_MONITORING_ENGINEER | FIXED_LOCAL: tracking and delivery feedback outboxes now expose queue-only ready-depth and oldest-ready-age gauges with alerts, Grafana panels, and runbook guidance. | 52 | Tune thresholds and collect target scrape/firing/routing evidence before monitoring handoff claims. | Focused outbox tests, full delivery+tracking backend gate, production overlay validation, Grafana JSON parse, Codex validation, artifact hygiene, `git diff --check`. |
| foundation-admin-public-content-context-fail-closed | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: admin public-content list/upsert/publish now require tenant and workspace context before repository access or save. | 50 | Preserve service-level context guards even though the HTTP filter also protects the admin route. | Focused public-content service tests, integrated foundation+content backend gate, scoped diff check. |
| content-template-test-send-history-bound | CONTENT_SERVICE_OWNER | FIXED_LOCAL: template test-send history now uses a bounded first-page repository query while preserving scoped filtering and list response shape. | 43 | Add UI/controller pagination only as a separate contract change if operators need deep history navigation. | Focused content service tests, integrated foundation+content backend gate, scoped diff check. |
| ci-compose-envfile-safety-gate | DEVOPS_ENGINEER | FIXED_LOCAL: CI Compose smoke now uses an explicit reviewed env-file path and release self-tests guard against ambient Compose config discovery. | 42 | Keep CI and release-gate Compose inputs aligned; strict production promotion still requires external evidence. | Release evidence self-test, Compose safety self-test, Compose safety validation, Compose config, scoped diff check. |
| deliverability-suppression-history-count-query | DELIVERABILITY_SERVICE_OWNER | FIXED_LOCAL: suppression history total now uses a scoped count query instead of row materialization. | 39 | Preserve count-query summary behavior when extending suppression history. | Focused suppression controller test, full deliverability-service gate, scoped diff check. |
| frontend-template-library-context-scoped-persistence | FRONTEND_OWNER | FIXED_LOCAL: template library favorites/recents now use active tenant/workspace scoped browser-storage keys and ignore global legacy keys. | 43 | Preserve scoped preference tests for workspace switching, missing context, corrupt stored JSON, and recent cap behavior. | Template-builder Playwright, frontend lint/build, scoped diff check. |
| delivery-provider-capacity-list-bound | DELIVERY_SERVICE_OWNER | FIXED_LOCAL: delivery provider-capacity list reads are bounded with default/max limit handling while preserving current list response shape. | 43 | Add deeper paging/search only as a separate operator UX/API contract if needed. | Focused provider-capacity tests, full delivery-service gate, scoped diff check. |
| audience-import-processing-scoped-job-claim | AUDIENCE_SERVICE_OWNER | FIXED_LOCAL: async import processing now carries trusted tenant/workspace scope into job reloads and side-effect boundaries. | 45 | Preserve scoped job reload coverage for import retries, cancellation, and worker recovery. | Focused import tests, integrated backend gate, scoped diff check. |
| deliverability-reputation-recovery-page-bound | DELIVERABILITY_SERVICE_OWNER | FIXED_LOCAL: scheduled reputation recovery reads fixed-size pages instead of all rows. | 41 | Tune batch size only with target runtime evidence. | Focused reputation tests, integrated backend gate, scoped diff check. |
| operations-assistance-telemetry-governance | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: operations assistance persists only allowlisted operational telemetry and rejects restricted key shapes before repository access. | 47 | Keep provider-backed assistance blocked until separate policy/audit/provider review. | Focused foundation tests, integrated backend gate, scoped diff check. |
| observability-alert-routing-handoff-gaps | SRE_MONITORING_ENGINEER | FIXED_LOCAL: production overlay validation now checks alert ownership, routing, runbook anchors, dashboard panel references, and receiver settings. | 43 | Collect target firing/routing/dashboard evidence before monitoring readiness claims. | Production overlay validation, release evidence self-test, local-only release gate, scoped diff check. |
| data-extension-controller-pagination-guards | AUDIENCE_SERVICE_OWNER | FIXED_LOCAL: data-extension controller list and record reads clamp invalid and excessive page/size params before service calls. | 40 | Preserve controller-bound pagination guards when expanding data-extension list or record browsing. | Focused controller tests, integrated audience+content backend gate, scoped diff check. |
| segment-scheduled-recompute-page-bound | AUDIENCE_SERVICE_OWNER | FIXED_LOCAL: scheduled segment recompute scans due segment candidates in deterministic bounded pages. | 40 | Implement durable chunked materialization only as a separate design-backed slice. | Focused segment tests, integrated audience+content backend gate, scoped diff check. |
| content-send-governance-pagination-guard | CONTENT_SERVICE_OWNER | FIXED_LOCAL: send-governance list reads clamp invalid and excessive page/size params while preserving scope and response contracts. | 39 | Preserve bounds if deeper operator pagination/search is added. | Focused controller tests, integrated audience+content backend gate, scoped diff check. |
| foundation-bootstrap-page-active-tenants | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: tenant bootstrap initialization now uses active non-deleted paged scans instead of all-tenant startup reads. | 39 | Preserve deterministic bounded startup scans when bootstrap recovery expands. | Focused bootstrap tests, integrated foundation+content+platform backend gate, scoped diff check. |
| platform-global-search-result-bound | PLATFORM_SERVICE_OWNER | FIXED_LOCAL: global search result reads are bounded while preserving tenant/workspace filters and list response shape. | 40 | Add deeper pagination or ranking only as a separate API/search contract. | Focused global search tests, integrated foundation+content+platform backend gate, scoped diff check. |
| platform-unread-notifications-bound | PLATFORM_SERVICE_OWNER | FIXED_LOCAL: unread notification reads are bounded while preserving tenant/workspace/user filters. | 37 | Preserve newest-first bounded reads when notification UX expands. | Focused notification tests, integrated foundation+content+platform backend gate, scoped diff check. |
| content-template-search-and-page-guards | CONTENT_SERVICE_OWNER | FIXED_LOCAL: template list and search endpoints now apply default/max result bounds while preserving scope and response contracts. | 39 | Add search pagination only as a separate API/UI contract if operators need deep result navigation. | Focused content tests, integrated foundation+content+platform backend gate, scoped diff check. |
| delivery-provider-health-monitoring-bounds | DELIVERY_SERVICE_OWNER | FIXED_LOCAL: provider health monitoring now uses deterministic bounded active-provider scans and bounded recent-check history reads. | 39 | Preserve bounded scheduler scans when health monitoring expands. | Focused provider health tests, integrated delivery/campaign/deliverability/tracking/foundation backend gate, scoped diff check. |
| campaign-experiment-evaluator-page-bound | CAMPAIGN_SERVICE_OWNER | FIXED_LOCAL: scheduled experiment winner evaluation now scans active experiments through bounded deterministic pages. | 42 | Preserve page/cursor scans when adding experiment scheduler behavior. | Focused campaign engine tests, integrated delivery/campaign/deliverability/tracking/foundation backend gate, scoped diff check. |
| deliverability-suppression-list-bound | DELIVERABILITY_SERVICE_OWNER | FIXED_LOCAL: public and internal suppression list reads now use bounded scoped pages and aligned method-security coverage. | 44 | Add deep pagination only as an explicit operator API contract. | Focused suppression/RBAC tests, integrated delivery/campaign/deliverability/tracking/foundation backend gate, scoped diff check. |
| tracking-campaign-summary-list-bound | TRACKING_SERVICE_OWNER | FIXED_LOCAL: tracking campaign summary listing now uses bounded scoped first-page repository access. | 44 | Preserve list response compatibility unless a paged analytics API is introduced. | Focused analytics tests, integrated delivery/campaign/deliverability/tracking/foundation backend gate, scoped diff check. |
| delivery-warmup-rate-state-list-bounds | DELIVERY_SERVICE_OWNER | FIXED_LOCAL: delivery warmup and rate-state operator endpoints now clamp optional limits and use bounded scoped first-page reads. | 41 | Add deeper state browsing only as a deliberate operator UX/API change. | Focused warmup/rate tests, integrated delivery/campaign/deliverability/tracking/foundation backend gate, scoped diff check. |
| foundation-admin-settings-list-query-bound | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: admin settings listing now uses scoped pageable visible-settings queries instead of service-memory filtering over all config rows. | 42 | Add repository integration coverage if query shape changes; collect target config-volume evidence before admin scale claims. | Focused admin settings tests, integrated delivery/campaign/deliverability/tracking/foundation backend gate, scoped diff check. |
| campaign-send-batch-require-workspace-context | CAMPAIGN_SERVICE_OWNER | FIXED_LOCAL: batch execution no longer has a tenant-only lookup fallback when workspace context is missing. | 47 | Preserve workspace-context fail-closed behavior across direct execution, retry, and event consumer paths. | Focused campaign tests, integrated campaign/tracking/content/automation/platform backend gate, scoped diff check. |
| tracking-conversion-rbac-write-guard | TRACKING_SERVICE_OWNER | FIXED_LOCAL: conversion ingestion now has method-level write permission coverage while signed open/click paths remain public. | 45 | Keep method-level RBAC tests for mixed public/protected tracking controllers. | Focused tracking tests, integrated campaign/tracking/content/automation/platform backend gate, scoped diff check. |
| content-blank-target-rel-sanitizer | CONTENT_SERVICE_OWNER | FIXED_LOCAL: backend sanitizer now normalizes safe rel values for blank-target links. | 33 | Preserve rel/target sanitizer tests when link-attribute policy changes. | Focused content sanitizer tests, integrated campaign/tracking/content/automation/platform backend gate, scoped diff check. |
| automation-workflow-schedule-activity-list-bounds | AUTOMATION_SERVICE_OWNER | FIXED_LOCAL: workflow, schedule, and activity list reads now use bounded first-page repository methods. | 40 | Add paged response metadata only as a separate API contract if operators need deep browsing. | Focused automation tests, integrated campaign/tracking/content/automation/platform backend gate, scoped diff check. |
| platform-webhook-admin-list-bound | PLATFORM_SERVICE_OWNER | FIXED_LOCAL: webhook admin listing now uses bounded scoped first-page access. | 37 | Add deeper pagination only as a separate platform-admin API change. | Focused platform tests, integrated campaign/tracking/content/automation/platform backend gate, scoped diff check. |
| compose-safety-runtime-guardrails | DEVOPS_ENGINEER | FIXED_LOCAL: Compose safety validation now rejects privileged runtime, host namespaces, Docker control socket binds, and broad capability additions. | 35 | Keep local release-validator self-tests aligned with Compose safety checks. | Compose safety self-test, release evidence validator self-test, scoped diff check. |
| frontend-context-bootstrap-environment-match | FRONTEND_OWNER | FIXED_LOCAL: context bootstrap now uses preferred environment as a deterministic tie breaker inside tenant/workspace candidates. | 35 | Preserve tenant/workspace/environment bootstrap coverage when context storage or session hydration changes. | Frontend lint, build, and context Playwright. |
| deliverability-dmarc-report-list-bound | DELIVERABILITY_SERVICE_OWNER | FIXED_LOCAL: DMARC report history now uses bounded first-page scoped reads. | 41 | Add paged response metadata only as a separate API contract. | Focused DMARC tests, integrated backend gate, route/artifact/Codex validation. |
| foundation-feature-flag-list-create-tenant | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: feature flag admin list/create now fail closed without tenant context. | 42 | Keep global feature-flag admin separate from tenant endpoints. | Focused feature flag tests and integrated backend gate. |
| platform-notification-method-auth | PLATFORM_SERVICE_OWNER | FIXED_LOCAL: platform notification read/mark-read methods now declare authentication explicitly. | 41 | Consider notification-specific permissions only with product policy. | Focused platform tests and integrated backend gate. |
| content-template-approval-history-bound | CONTENT_SERVICE_OWNER | FIXED_LOCAL: template approval history and pending approval reads are bounded. | 41 | Add deeper history pagination only as a separate UI/API contract. | Focused content workflow tests and integrated backend gate. |
| route-map-negative-fixture-expansion | DEVOPS_ENGINEER | FIXED_LOCAL: route-map fixture harness now includes tracking route/policy/ingress drift cases and accumulated error output. | 35 | Keep negative route fixtures in sync with new public/internal prefixes. | Route fixture harness, live route validation, artifact/Codex validation. |
| frontend-login-context-failure-cleanup | FRONTEND_OWNER | FIXED_LOCAL: public auth views now treat workspace bootstrap as part of local auth success and clear client context on failure. | 40 | Preserve local-context cleanup if auth/session hydration is refactored. | Frontend lint, production build, auth Playwright, scoped diff check. |
| tracking-analytics-windowed-rollups | TRACKING_SERVICE_OWNER | FIXED_LOCAL: tracking timeline and rollup analytics now have default windows, explicit windows, and bucket caps. | 44 | Add paged/deeper analytics APIs only as explicit contracts and collect target analytics evidence. | Focused tracking tests, integrated backend gate, scoped diff check. |
| content-template-version-list-bound | CONTENT_SERVICE_OWNER | FIXED_LOCAL: content template version listing and publish cleanup avoid unbounded scoped row materialization. | 41 | Add deeper version-history browsing only as a deliberate operator API/UI contract. | Focused content tests, integrated backend gate, scoped diff check. |
| identity-user-invitation-list-bounds | IDENTITY_SERVICE_OWNER | FIXED_LOCAL: identity user and invitation admin list reads are bounded while preserving current list response shapes. | 42 | Add paged metadata only as a separate admin API contract. | Focused identity tests, integrated backend gate, scoped diff check. |
| foundation-config-version-require-tenant | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: config version controller endpoints now fail closed on missing tenant context before service calls. | 46 | Preserve controller context guards when config version endpoints expand. | Focused foundation tests, integrated backend gate, scoped diff check. |
| ci-ops-validator-selftests | DEVOPS_ENGINEER | FIXED_LOCAL: CI compose smoke now includes route-map fixture and Compose safety validator steps. | 36 | Verify in live GitHub Actions; keep local validator and CI command drift low. | Route fixture harness, Compose safety gates, route validation, scoped diff check. |
| foundation-compliance-evidence-workspace-fail-closed | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: compliance evidence paths now require trusted workspace context and exact scoped predicates. | 49 | Keep tenant-global compliance administration as a separate privileged policy decision. | Focused foundation tests, integrated backend gate, Codex validation, scoped diff check. |
| tracking-websocket-handshake-no-header-scope-fallback | TRACKING_SERVICE_OWNER | FIXED_LOCAL: WebSocket handshake scope now comes from trusted context or signed query scope, not request headers. | 43 | Preserve trusted-context-only WebSocket tests when auth modes change. | Focused tracking tests, integrated backend gate, Codex validation, scoped diff check. |
| deliverability-reputation-read-bounds | DELIVERABILITY_SERVICE_OWNER | FIXED_LOCAL: predictive deliverability and insights reputation reads now use bounded first-page windows. | 46 | Add full-history analytics only as a separate contract; collect target reputation-volume evidence before scale claims. | Focused deliverability tests, integrated backend gate, Codex validation, scoped diff check. |
| content-block-version-list-bound | CONTENT_SERVICE_OWNER | FIXED_LOCAL: content block version list and publish cleanup now avoid unbounded scoped row materialization. | 41 | Add deeper content-block version history only as a deliberate API/UI contract. | Focused content tests, integrated backend gate, Codex validation, scoped diff check. |
| frontend-public-api-origin-guard | FRONTEND_OWNER | FIXED_LOCAL: public frontend API helpers now reject external absolute targets before adapter dispatch. | 39 | Keep origin-guard and credentialless public-helper tests aligned when adding frontend helpers. | Frontend lint, production build, API-client Playwright, scoped diff check. |
| devops-pwsh-child-process-portability | DEVOPS_ENGINEER | FIXED_LOCAL: release validator child processes now reuse the active PowerShell executable and CI runs compose health parser self-tests. | 37 | Confirm behavior in live CI and keep local release utility assumptions portable. | Release validator self-test, local release gate, compose health/safety validators, route validators, scoped diff check. |
