# Technical Debt

Last updated: 2026-05-18.

Open:

- 2026-05-18 admin/settings hot-file risk reduced but not eliminated: `EnterpriseAdminConsole.tsx` and `EnterpriseSettingsConsole.tsx` now have safer polling/save/normalization behavior and stronger E2E coverage, but they remain large console files. Next action: split admin command/users/roles and settings panes into domain components/hooks during the next feature pass.
- 2026-05-18 shared common utility dead-code debt reduced: `DateTimeUtil`, `JsonUtil`, and the dedicated `JsonUtilTest` were removed after `rg` across Java/XML/TS/docs found only self/test/docs references. Next action: keep shared utility cleanup dependency-driven and avoid adding broad helpers without service callers.
- 2026-05-18 DEBT-2026-05-18-FRONTEND-ANY closed for current `frontend/src` lint scope: typed API wrapper/admin-panel cleanup plus workspace page/component cleanup removed the audit's 263 `no-explicit-any` warnings. Cleared explicit `any` from core/compliance/differentiation/template/automation/admin/global/performance APIs, admin panels, audience/campaign/email/automation/launch/deliverability workspace routes, marketing auth views, `useApi`, and shared audience/automation/deliverability components; deleted several confirmed-unused frontend exports. Validation: `npm run lint` passed with zero warnings and `rg` found no explicit-any patterns in `frontend/src`.
- 2026-05-18 Salesforce comparison audit refreshed in `.codex/reports/salesforce-comparison-full-audit-2026-05-18.yaml` after whole-tree inventory, targeted high-risk source inspection, official Salesforce source review, and local validation. Initial scores: Salesforce parity 67%, functional readiness 76%, controlled private beta readiness 80%, low-volume limited live readiness 63%, public multi-tenant GA readiness 47%, best-in-world readiness 40%. Evidence base then: 1,275 tracked files, 1,254 parsed text files, 152,275 parsed LOC, 530 backend tests passing, frontend lint/build passing, 18 Playwright smoke/sanitizer tests passing. The artifact hygiene self-match and frontend explicit-any blockers were closed later on 2026-05-18; remaining release debt is external production evidence, Audience V17 target mapping, image digest/SBOM/signature/provenance, and live load/restore/security/monitoring proof.
- 2026-05-18 repository artifact hygiene validator self-match debt is closed. `validate-repo-artifact-hygiene.ps1` now detects full generated Spring password output without matching prose/source references, and `test-repo-artifact-hygiene.ps1` covers the regression. Remaining release-tooling debt is external CI evidence for gitleaks/Trivy, not this validator.
- 2026-05-17 five-loop sweep reduced docs/ops/frontend drift: runtime log artifacts are now rejected by a dedicated hygiene validator, fail-closed evidence validators have a temp-fixture self-test, CI runs both before scans, Next 16 legacy redirects live in `src/proxy.ts`, and README/AGENTS/architecture/report memory no longer claim resolved Kafka trust/DDL/audience risks are open. Updated 2026-05-18: frontend `no-explicit-any` cleanup is closed for current `frontend/src` lint scope and lint now fails on regressions. Remaining debt: broader inbound consumer schema/version migration strategy, large hot-file decomposition, and external production evidence.
- 2026-05-17 current `.codex` command/report drift around release-gate behavior, default/strict evidence validation, report coexistence, repo file count, and Kafka UI port was corrected in `.codex/bootstrap.md`, `.codex/commands/full-audit.md`, `.codex/commands/release-pass.md`, `.codex/reports/final-audit-2026-05-16.md`, and `Makefile`. Keep command docs tied to script readback after future release-gate changes.
- 2026-05-17 Salesforce comparison audit produced and was updated after implementation in `.codex/reports/salesforce-comparison-full-audit-2026-05-17.yaml`. Current post-implementation scores: Salesforce parity 66%, functional readiness 73%, private beta readiness 78%, public multi-tenant GA readiness 48%, best-in-world readiness 39%. Report is valid YAML and is based on 1,317 parsed text files / 175,224 LOC plus targeted manual inspections and validation commands.
- Delivery retry content recovery source debt is closed for the missing-route/cache-scope bug: campaign writes durable rendered snapshots through content-service, delivery fetches by tenant/workspace/reference with internal auth, and caches by scoped reference. Remaining debt: retention/load dashboards, operator purge controls, and live high-volume retry/replay proof.
- Automation workflow distributed locking source debt is closed: `CacheService.setIfAbsent` uses Redis SET NX PX semantics and `WorkflowEngine` uses it for resume locks. Remaining debt: durable scheduler, journey versioning, query/import/file activities, and journey analytics.
- Data Extension preview false-negative risk is reduced: it now scans a bounded 5,000-row multi-page preview window before filtering/sorting and emits scan warnings. SQL Query Activity source MVP was added on 2026-05-18 with a safe SELECT subset and APPEND/OVERWRITE/UPDATE/UPSERT data-extension write modes through audience-owned execution; subscriber and data-extension import activities can now start real audience import jobs. Remaining debt before claiming Contact Builder/Automation Studio parity: true database-backed query planning/explain, marketer query builder UI, query governance/concurrency controls, lineage, data views, file transfer, extract artifacts, and bulk import performance proof.
- Campaign required content references now create durable snapshots even when optional per-recipient snapshotting is disabled; optional inline-send snapshotting remains default-off to avoid one synchronous content-service write per recipient. Remaining debt: batch/outbox snapshot storage, live render/Kafka/provider load proof, and stack-level readiness smoke.
- Current release-blocker state is encoded as strict validation, not as proof of production readiness. Evidence validators now exist for external egress, Audience V17 mapping, GA artifact packs, live load proof, and image digest/provenance, but real target evidence must still be collected and attached.
- Current audit reports are `.codex/reports/final-audit-2026-05-16.md` for the 2026-05-16 release-blocker consolidation and `.codex/reports/salesforce-comparison-full-audit-2026-05-17.yaml` for Salesforce-parity comparison. Keep future audit updates additive and avoid contradicting resolved memory entries.
- Frontend API-client hardening now uses typed error normalization and the current frontend source passes ESLint without explicit-any warnings. Continue keeping new API wrappers domain-typed and prefer `unknown` plus record guards for dynamic backend maps.
- Tracking raw-event local cleanup was closed on 2026-05-16 with Postgres retention guardrails, a scheduled bounded Postgres purge executor, ClickHouse workspace-lineage schema contracts, atomic Redis dedupe reservations, and outbox lease metrics. Remaining debt is target-environment ClickHouse ingest/load, Postgres purge scheduling evidence, and TTL proof before high-volume GA.
- Infra release validation now checks observability alert render, ingress TLS references, restricted Pod Security labels, deployment hardening, and optional strict image digest pinning. Remaining debt is live Prometheus/Alertmanager proof, TLS certificate ownership proof, restricted admission proof, and real registry SBOM/signature/digest provenance evidence.
- Large frontend route/components over 700-1200 lines make UI changes risky: `PublicPageView.tsx`, workspace template editor, admin/settings consoles, campaign creation page. Admin/settings visual chrome was aligned on 2026-05-16, but the consoles remain large and should still be split by panel/domain when next touched.
- Large backend services over 600-990 lines concentrate too many responsibilities: foundation global/core platform, identity federation, campaign launch orchestration, delivery orchestration.
- Docs/scripts drift around ports and validation flags was rechecked on 2026-05-17 with command readback, Compose port check, and `.codex` command/report updates; keep future docs changes paired with script validation.
- Content/platform workspace-scope migrations intentionally leave legacy rows nullable; operators need reviewed backfill/mapping before those rows become visible in workspace-scoped authenticated APIs.
- 2026-05-16 cleanup removed dependency-proven unreachable frontend tracking/content/admin/layout files, `useFeatureFlag`, old tracking frontend API/WebSocket wrappers, dead `TenantException`, and an unused delivery fallback method. Keep future deletion work dependency-driven rather than filename-driven.
- Generated `target/classes/db/migration` directories exist locally; do not modify generated outputs.
- Kafka consumer error handling debt was reduced on 2026-05-13 with shared retry/DLQ wiring and owned listener rethrow behavior. Outbound contracts for six high-risk topics are validated before publish as of 2026-05-18, and the 2026-05-18 sweep added focused fail-closed invalid-scope tests for audience, automation, campaign, deliverability, delivery failed-email, foundation, platform, and tracking consumers. Remaining debt: broader inbound payload schema registry/version migration strategy and any unreviewed low-risk consumers outside this sweep.
- Frontend workspace shell has high blast radius: session hydration, tenant bootstrap, preferences, redirect, shell chrome, and toasts are coupled in one layout.
- Tenant/workspace metadata can drift because layout, header, auth views, context bootstrap, auth store, and tenant store all write context. Tokens are not stored in localStorage, but non-secret context still needs consistency tests.
- Flyway version gaps exist in identity, tracking, and automation. Flyway allows gaps, but audit docs should note them to avoid future renumbering.
