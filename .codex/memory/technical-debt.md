# Technical Debt

Fresh baseline date: 2026-05-20.

Accepted debt from 2026-05-20 audit:

| ID | Owner | Impact | Score | Next Action | Validation Required |
|---|---|---|---:|---|---|
| internal-routes-edge-blocking | API_ARCHITECT | FIXED: public Nginx and ingress now deny known internal routes, and route validation checks those rules. | 61 | Extend the deny list when new `/internal` routes are added. | `scripts\ops\validate-route-map.ps1`, Kustomize render. |
| audience-bulk-suppression-check | AUDIENCE_SERVICE_OWNER | FIXED: audience resolution now uses a bounded deliverability bulk-check API instead of broad suppression-list fetches. | 58 | Monitor live resolution/load metrics before any high-volume throughput claims. | `.\mvnw.cmd -pl services/audience-service,services/deliverability-service -am test`. |
| delivery-feedback-outbox | DELIVERY_SERVICE_OWNER | FIXED: delivery feedback now persists to a leased outbox with stable event IDs/idempotency keys and retries Kafka publication after commit. | 58 | Add retention/cleanup and live backlog alerts before high-volume claims. | `.\mvnw.cmd -pl services/delivery-service -am test`, Codex validation, repo artifact hygiene, `git diff --check`. |
| frequency-optimization-deterministic-policy-contract | DELIVERABILITY_SERVICE_OWNER | TESTED_LOCAL: deterministic frequency optimization policy evaluation now exposes readiness, fallback, saturation, recommended cap, confidence, approval/rollback, and safety-gate outputs; campaign safety tests prove tenant/workspace frequency caps suppress before delivery handoff. | 58 | Keep live cadence changes, model-backed optimization, provider-capacity proof, and parity claims blocked until separate implementation and target evidence exist. | Focused foundation optimization tests, focused campaign safety tests, `.\mvnw.cmd -pl services/foundation-service,services/campaign-service -am test`, Codex validation, repo artifact hygiene, `git diff --check`. |
| campaign-send-content-reference-contract | CAMPAIGN_SERVICE_OWNER | FIXED: campaign send handoff now always creates and publishes a durable `contentReference` before `email.send.requested`; shared Kafka tests reject inline-only or blank-reference send payloads. | 56 | Review reference-only mode for production high-volume campaigns after target-like load evidence. | `.\mvnw.cmd -pl services/campaign-service,shared/legent-kafka -am test`, Codex validation, repo artifact hygiene, `git diff --check`. |
| egress-evidence-validator-hardening | RELEASE_MANAGER | FIXED: validator now rejects template/example evidence, placeholder values, documentation CIDRs, unsupported FQDN evidence, broad/non-canonical CIDRs, future dates, and unsupported schema versions. | 54 | Keep template validation as an expected negative test; real production egress evidence is still external. | `scripts\ops\test-release-evidence-validators.ps1`, expected-fail template validation, local release gate, Codex validation, `git diff --check`. |
| kafka-dlq-sharding | KAFKA_CONTRACTS_OWNER | FIXED: shared listener DLQ routing preserves source partitions, Java/Compose define six DLQ partitions, and tests guard DLQ/source-topic partition compatibility. | 51 | Add DLQ-specific observability and production Kafka topology evidence before high-volume claims. | `.\mvnw.cmd -pl shared/legent-kafka -am test`, Compose config, drift scan, Codex validation, repo artifact hygiene, `git diff --check`. |
| journey-runtime-node-contract | AUTOMATION_SERVICE_OWNER | FIXED: Journey Builder, validation, simulation, publish, rollback/resume, and runtime handler paths now distinguish draft-only nodes from live runtime-supported nodes and fail closed before publish/execution. | 48 | Implement broader journey node families only as separate validated runtime slices. | `.\mvnw.cmd -pl services/automation-service -am test`, frontend lint/build, targeted Playwright builder spec, Codex validation, `git diff --check`. |
| tracking-ingress-rate-policy | DEVOPS_ENGINEER | FIXED-CONFIG: Kubernetes now splits tracking ingestion from analytics/websocket ingress and declares the reviewed `/api/v1/tracking` ingress-nginx `limit-rps` posture, with route validation enforcing local/Kubernetes drift checks. | 43 | Keep live rate-limit and downstream ingestion proof as release evidence, not a code-complete claim. | `scripts\ops\validate-route-map.ps1`, Kustomize render, Codex validation, `git diff --check`. |
| sso-tenant-cookie-httponly | IDENTITY_SERVICE_OWNER | FIXED: SSO callback tenant cookies now use the same HTTP-only posture as normal auth while preserving access/refresh cookie path scope. | 42 | Preserve parity if new OIDC/SAML callback variants are added. | `.\mvnw.cmd -pl services/identity-service -am test`, Codex validation, repo artifact hygiene, `git diff --check`. |
| delivery-provider-workspace-isolation | DELIVERY_SERVICE_OWNER | FIXED: delivery provider configuration and provider-linked operational rows now carry tenant+workspace ownership through CRUD, selection, health, capacity, failover, and forward migration constraints. | 65 | Review legacy `workspace-default` backfills against real workspace ownership before strict production promotion. | `.\mvnw.cmd -pl services/delivery-service -am test`, Codex validation, repo artifact hygiene, `git diff --check`. |
| production-egress-policy-render-gate | DEVOPS_ENGINEER | FIXED: strict egress validation now proves the reviewed external egress NetworkPolicy is generated or checked, rendered through a temporary production Kustomize overlay, hash-matched to evidence, and covered by app-labeled Deployment pod templates. | 47 | Keep real target-environment egress evidence as a production release requirement; do not check generated proof artifacts into the normal local overlay. | `scripts\ops\test-release-evidence-validators.ps1`, production overlay validation, Kustomize render, local release gate, Codex validation, repo artifact hygiene, `git diff --check`. |
| release-evidence-validator-negative-fixtures | RELEASE_MANAGER | FIXED: release evidence self-test now covers GA manifest success plus placeholder, missing artifact, absolute path, path escape, and missing field failures; image evidence now covers missing production image, extra image, digest mismatch, absolute evidence path, and path escape failures. | 41 | Keep this local-only; real production evidence remains blocked until target artifacts are collected and strict release gate runs. | `scripts\ops\test-release-evidence-validators.ps1`, local release gate, repo artifact hygiene, Codex validation, `git diff --check`. |
| platform-event-idempotency | PLATFORM_SERVICE_OWNER | FIXED_LOCAL: platform consumers now claim event/idempotency identity before side effects and skip duplicate replay. | 46 | Collect target migration and Kafka replay evidence before release claims. | `.\mvnw.cmd -pl services/platform-service -am test`, Codex validation, repo artifact hygiene, `git diff --check`. |
| campaign-legacy-batch-eligibility-marker | CAMPAIGN_SERVICE_OWNER | REVIEW: audience resolution final eligibility is fixed locally, but `SendExecutionService` still has a legacy JSON recipient payload path without durable proof that recipients came through audience eligibility. | 71 | Review and implement an eligibility marker/contract for campaign batch rows and legacy payload compatibility before closing `campaign-audience-eligibility-final-gate`. | Campaign and audience focused tests plus full relevant backend module gates. |
| clickhouse-experiment-lineage-rollup-idempotency | TRACKING_SERVICE_OWNER | FIXED_LOCAL: raw ClickHouse analytics now preserve experiment lineage and campaign-day rollup refresh replaces the scoped date window before insert. | 52 | Prove target ClickHouse schema migration and refresh behavior with environment data before release or BI-grade experiment claims. | `.\mvnw.cmd -pl services/tracking-service -am test`, Codex validation, repo artifact hygiene, `git diff --check`. |
| codex-lifecycle-state-atomicity | PROGRAM_MANAGER_AGENT | FIXED_CORE: core promote/start/acquire/checkpoint/update/complete/thread heartbeat/audit mutations now share a named state lock and atomic JSON writes; start now creates lease/checkpoint before moving queue state, and acquire rejects overlaps before persisting. | 40 | Convert less-used cleanup/worktree/module-lease utilities in a separate scoped slice if heavy multi-thread operations expand beyond the core lifecycle path. | `powershell -ExecutionPolicy Bypass -File .codex\utilities\test-codex-lifecycle-state.ps1`, Codex validation, lease validation, parser check, `git diff --check`. |
| data-extension-governance-migration-tests | AUDIENCE_SERVICE_OWNER | TESTED_LOCAL: data-extension workspace-scope migration contracts and service governance behaviors now have focused tests for relationship scoping, field validation, sendable-field validation, retention clearing, and scoped soft delete. | 38 | Add PostgreSQL/Flyway runtime migration proof with representative legacy rows before release claims. | Focused data-extension tests, `.\mvnw.cmd -pl services/audience-service -am test`, JPA config scan, Codex validation, repo artifact hygiene, `git diff --check`. |
| predictive-segments-governance | AUDIENCE_SERVICE_OWNER | TESTED_LOCAL: predictive segment preview contract and fail-closed create/evaluate/recompute guards now require tenant policy, source/data-class governance, protected-data exclusion, freshness/count thresholds, bias/drift pass, approval audit fields, and rollback evidence before materialization. | 53 | Add first-class tenant AI policy/audit tables, model provider/model-card evidence, chunked materialization, and durable rollback snapshots before AI parity or production claims. | Focused predictive governance tests, full `.\mvnw.cmd -pl services/audience-service -am test`, repo artifact hygiene, Codex validation, `git diff --check`. |
| contact-data-designer-preview-governance | AUDIENCE_SERVICE_OWNER | TESTED_LOCAL: no-schema backend hardening now validates nested data-extension metadata, relationship cardinality/type/key compatibility, sendable required/primary-key invariants, preview field/filter/sort shapes, sort-before-projection behavior, and segment rejection of unsupported data-extension relationship metadata. | 49 | Keep parent `contact-data-designer-governance` open for additive provenance/classification/audit tables, import/source metadata, indexed relationship execution, controller API validation, and frontend relationship-designer controls. | Focused `DataExtensionServiceTest,SegmentServiceTest`, full `.\mvnw.cmd -pl services/audience-service -am test`, Codex validation, repo artifact hygiene, scoped `git diff --check`. |
| mode-aware-workflow-contract | FRONTEND_OWNER | FIXED_LOCAL: BASIC/ADVANCED mode now has a typed frontend metadata contract, render-time Settings nav filtering, separate Admin role gating, and campaign Experiment Engine render/payload gating. | 38 | Continue migrating budget/frequency/template/automation advanced controls in separate slices; keep backend authorization and safety defaults authoritative. | `cd frontend; npm run lint`, `npm run build:ci`, targeted Playwright `ui-mode` and campaign specs, smoke E2E, Codex validation, `git diff --check`. |
| campaign-budget-frequency-mode-contract | FRONTEND_OWNER | FIXED_LOCAL: Campaign Budget Guard and Workspace Frequency Policy now use ADVANCED_ONLY metadata, are absent from BASIC DOM/focus order, and skip hidden advanced budget/frequency API payloads in BASIC. | 36 | Continue with Template Studio and Automation Studio mode-metadata slices; keep simple Frequency Cap visible in BASIC as a safety control unless product/backend owners decide otherwise. | `cd frontend; npm run lint`, `npm run build:ci`, targeted campaign/UI-mode Playwright, Codex validation, `git diff --check`. |
| automation-mode-contract | FRONTEND_OWNER | FIXED_LOCAL: Automation Studio activity authoring/execution, manual workflow trigger, and draft-only journey-node saving now respect BASIC/ADVANCED mode metadata without treating UI mode as authorization. | 37 | Keep backend workflow/activity validation authoritative; future automation parity work remains separate from UI mode metadata. | `cd frontend; npm run lint`, `npm run build:ci`, targeted automation/UI-mode Playwright, Codex validation, `git diff --check`. |
| template-studio-mode-contract | FRONTEND_OWNER | FIXED_LOCAL: Template Studio advanced tabs/actions, builder advanced blocks, conditional rules, raw HTML snapshot, and optional advanced resource loads now respect BASIC/ADVANCED mode metadata; BASIC save payloads scrub conditional block settings. | 33 | Keep content-service authorization and sanitization authoritative; future Template Studio parity work should add backend-backed capability/permission checks rather than relying on UI mode. | `cd frontend; npm run lint`, `npm run build:ci`, targeted Template Studio/UI-mode Playwright, Codex validation, `git diff --check`. |
| admin-settings-context-mismatch-fail-closed | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: admin settings service-level validation now rejects request-body workspace/environment mismatches and reset missing scoped context before config changes can cross scope. | 52 | Continue with tenant lifecycle, config history, and differentiation ownership hardening from the foundation audit. | Focused AdminSettingsServiceTest, full `.\mvnw.cmd -pl services/foundation-service -am test`, `git diff --check`. |
| config-by-id-tenant-scope | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: ConfigService update/delete now resolve by current tenant and non-deleted status, then fail closed for missing or mismatched workspace/environment context before mutation. | 54 | Design explicit privileged global-config administration and scope-aware config version history in separate slices. | Focused ConfigServiceTest, full `.\mvnw.cmd -pl services/foundation-service -am test`, `git diff --check`. |
| config-create-upsert-context-mismatch-fail-closed | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: ConfigService create/upsert now rejects body workspace/environment mismatches before scope lookup or mutation. | 41 | Add schema-backed config version-history scoping separately. | Focused ConfigServiceTest, full `.\mvnw.cmd -pl services/foundation-service -am test`, `git diff --check`. |
| differentiation-upsert-workspace-exact-match | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: Differentiation upsert lookup now uses null-safe exact workspace matching instead of broad null-workspace matching before update. | 48 | Review list/evaluate query semantics separately before changing tenant-level plus workspace-level visibility behavior. | Focused DifferentiationPlatformServiceTest, full `.\mvnw.cmd -pl services/foundation-service -am test`, `git diff --check`. |
| public-contact-admin-platform-admin-only | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: global public contact request admin list/status endpoints now require PLATFORM_ADMIN because the table has no tenant/workspace ownership columns. | 44 | Design tenant/workspace contact inbox only with schema migration and product ownership decision. | Focused AdminContactRequestControllerSecurityTest/PublicContactServiceTest, full `.\mvnw.cmd -pl services/foundation-service -am test`, `git diff --check`. |
| tenant-get-self-scope | FOUNDATION_SERVICE_OWNER | FIXED_LOCAL: TenantService.getTenant now fails closed unless the path tenant ID equals TenantContext before repository lookup. | 45 | Decide platform-admin versus self-tenant policy for tenant list, get-by-slug, and lifecycle mutations. | Focused TenantServiceTest, full `.\mvnw.cmd -pl services/foundation-service -am test`, `git diff --check`. |
| foundation-tenant-config-ownership | FOUNDATION_SERVICE_OWNER | OPEN: read-only foundation audits still show tenant lifecycle list/get-by-slug/mutation policy gaps and unscoped config version history; config update/delete raw-ID access, config create/upsert context mismatch, differentiation null-workspace upsert matching, and getTenant self-scope are fixed locally. | 64 | Design policy-backed tenant lifecycle controls and schema-backed config version-history scoping. | Focused service tests plus full foundation-service gate for each slice. |
| foundation-differentiation-evaluate-scope | FOUNDATION_SERVICE_OWNER | READY_LOCAL: final reassessment found decision, omnichannel, and SLO evaluation paths still use broad `(:workspaceId IS NULL OR workspace_id = :workspaceId)` semantics when service calls have no workspace context; upsert is already exact-match. | 50 | Narrow evaluation to exact workspace/null-workspace matching or fail closed on missing workspace; leave list/inventory semantics for an explicit product decision. | Focused DifferentiationPlatformServiceTest for null workspace, duplicate keys, and current workspace; full foundation-service test gate; `git diff --check`. |
| foundation-core-platform-workspace-guards | FOUNDATION_SERVICE_OWNER | READY_LOCAL: core-platform membership, role binding, and access grant creation can validate related IDs by tenant without proving same-workspace ownership. | 55 | Add workspace consistency guards for membership/team/department, role binding, and access grant workspace ownership. | Focused CorePlatformServiceTest cross-workspace denial cases; full foundation-service test gate; `git diff --check`. |
| foundation-compliance-privacy-request-workspace-scope | FOUNDATION_SERVICE_OWNER | READY_LOCAL: compliance privacy-request status update resolves by raw ID plus tenant while `/api/v1/compliance` requires workspace context. | 46 | Scope privacy request update by workspace context and add same-workspace/cross-workspace tests. | Focused ComplianceEvidenceService tests; full foundation-service test gate; `git diff --check`. |
| foundation-permission-group-tenant-context | FOUNDATION_SERVICE_OWNER | READY_LOCAL: permission group creation can let request `tenantId` override current tenant, unlike role-definition creation. | 39 | Mirror role-definition tenant mismatch guard for permission groups. | Focused CorePlatformServiceTest tenant mismatch case; full foundation-service test gate; `git diff --check`. |
| segment-rule-builder-operator-filtering | FRONTEND_OWNER | FIXED: SegmentRuleBuilder now filters list operators to `list_membership`, filters scalar operators away from `list_membership`, and normalizes invalid loaded/edited field-operator combinations before submit. | 33 | Keep backend fail-closed validation authoritative; future list picker work should validate actual list IDs without trusting client filtering. | `cd frontend; npm run lint`, `npm run build:ci`, targeted Playwright segment spec, Codex validation, repo artifact hygiene, `git diff --check`. |
| frontend-context-storage-comment | FRONTEND_OWNER | Comments around tenant/workspace context storage are misleading versus localStorage-backed context headers. | 40 | Clarify frontend context contract in code/docs. | Frontend lint or focused tests if behavior changes. |

Initial debt-discovery targets:
- Very large service classes, route files, frontend consoles, and shared modules.
- Runtime/docs drift around ports, validation flags, and local commands.
- Missing focused tests around tenant isolation, event retries, high-volume paths, and route ownership.
- Release evidence gaps that local validators cannot satisfy.

Debt entries must include owner, impact, score, next action, and validation required for removal.
