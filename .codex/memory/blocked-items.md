# Blocked Items

Last updated: 2026-05-16.

- Production egress blocker, updated 2026-05-16, source `infrastructure/kubernetes/overlays/production/delete-base-egress-policy.yml`, `network-policy.yml`, and `scripts/ops/validate-production-overlay.ps1`: production no longer renders inherited broad base `allow-legent-egress`; validator passes with default-deny plus internal/DNS-only egress. Promotion remains blocked until infrastructure supplies exact managed-service/provider CIDRs and ports or confirms selected CNI FQDN egress support for external dependencies. Next action: obtain reviewed dependency data for managed data services, email providers, object storage, observability, and outbound webhook proxy egress. Do not guess CIDRs or FQDN rules.
- Loop 5 production overlay note, updated 2026-05-16, source `infrastructure/kubernetes/overlays/production/external-secrets.yml` and `scripts/ops/validate-production-overlay.ps1`: required runtime secret keys render and are checked, and production overlay validation now passes locally. Broader release blockers still include exact external egress data, audience V17 production mapping metadata, shared workspace membership authorization contract, and target-environment GA/load/restore/security/monitoring evidence. Do not weaken fail-closed validation to pass release.
- Audience migration operator blocker, 2026-05-14, source `V16__workspace_scope_data_extensions.sql`, `V17__guard_data_extension_workspace_mapping.sql`, and `docs/operations/audience-data-extension-workspace-mapping.md`: V17 fails closed for ambiguous legacy `workspace-default` rows, but production upgrade requires staging preflight, reviewed mapping metadata, and authoritative target workspace verification.
- Full runtime validation may require Docker/Kubernetes availability and enough local resources; record exact blocker if encountered.
