# Blocked Items

Last updated: 2026-05-16.

- Production egress blocker, updated 2026-05-16, source `infrastructure/kubernetes/overlays/production/delete-base-egress-policy.yml`, `network-policy.yml`, `scripts/ops/validate-production-overlay.ps1`, `scripts/ops/validate-production-egress-evidence.ps1`, and `docs/operations/production-egress-evidence.template.json`: production no longer renders inherited broad base `allow-legent-egress`; validator passes with default-deny plus internal/DNS-only egress. Release gate now has strict external-evidence mode, but promotion remains blocked until infrastructure supplies exact managed-service/provider CIDRs and ports or approved CNI FQDN egress evidence. Next action: populate a reviewed production egress JSON and run release gate with `-RequireExternalEgressEvidence`. Do not guess CIDRs or FQDN rules.
- GA evidence blocker, updated 2026-05-16, source `scripts/ops/validate-ga-evidence.ps1`, `scripts/ops/release-gate.ps1`, and `docs/operations/ga-evidence-matrix.md`: release gate can now require a manifest-backed evidence pack for synthetic smoke, live load, restore drill, CI gitleaks, Trivy, SBOM, alert routing, TLS/cert ownership, restricted admission, registry digest, registry signature, and registry provenance. Promotion remains blocked until target artifacts are attached.
- Live scale blocker, updated 2026-05-16, source `scripts/load/phase3-high-volume-load.ps1` and `docs/load-testing/phase3-high-volume-load.md`: `-RequireLive` now requires target proof for ClickHouse TTL/partitioning, Postgres purge, remote render latency, Kafka handoff pressure, and delivery provider capacity. Promotion remains blocked until this evidence is collected from target-like load.
- Image supply-chain blocker, updated 2026-05-16, source `scripts/ops/validate-production-overlay.ps1`, `scripts/ops/write-image-supply-chain-checklist.ps1`, `.github/workflows/ci-security.yml`: strict image digest/evidence validation is available and intentionally fails on current tag-only `legent/*:1.0.2` images. Promotion remains blocked until digest-pinned images and registry SBOM/signature/provenance evidence are attached.
- Audience migration operator blocker, updated 2026-05-16, source `V16__workspace_scope_data_extensions.sql`, `V17__guard_data_extension_workspace_mapping.sql`, `docs/operations/audience-data-extension-workspace-mapping.md`, and `scripts/ops/validate-audience-data-extension-workspace-mapping.ps1`: V17 fails closed for ambiguous legacy `workspace-default` rows, and offline validator/test tooling now rejects bad reviewed metadata before migration. Production upgrade still requires staging preflight and authoritative target workspace verification.
- Full runtime validation may require Docker/Kubernetes availability and enough local resources; record exact blocker if encountered.
