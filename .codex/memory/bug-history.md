# Bug History

Last updated: 2026-05-18.

- Resolved: 2026-05-18 foundation admin/global-enterprise validation gaps. Source: `WorkspaceContextFilter`, `AdminOperationsService`, `AdminSettingsService`, `GlobalEnterpriseService`, `CorePlatformRepository`, and focused tests. `/api/v1/admin/operations/**` now requires workspace context; admin operations preserve tenant-level nullable sync/bootstrap rows without cross-workspace widening; admin settings rejects unsupported type/scope before apply/reset; failover drills require an active operating model; connector instances require a matching marketplace template. Validation: foundation-service full tests passed with 80 tests, frontend admin/settings E2E passed, and release validators passed on 2026-05-18.
- Resolved: 2026-05-18 content-service rendered snapshot scheduled purge transaction error. Source: `docker compose logs content-service` showed `TransactionRequiredException: Executing an update/delete query` from `RenderedContentSnapshotRepository.deleteExpired` during scheduled purge. `RenderedContentSnapshotRepository.deleteExpired` now has a repository-level transaction, and `RenderedContentSnapshotRepositoryTest` covers the no-surrounding-transaction path. Validation: content-service full tests passed with 43 tests, full release gate passed, content image was rebuilt/recreated, Compose health/host smoke passed, and fresh Compose log scan found no error patterns.
- Resolved: 2026-05-18 repository artifact hygiene validator self-match. Source: `scripts/ops/validate-repo-artifact-hygiene.ps1` and `scripts/ops/test-repo-artifact-hygiene.ps1`. The generated Spring password detector now matches full generated output with a constructed regex instead of a bare prose literal, and the self-test covers source/prose references, real generated password output, and runtime log artifacts. Validation: artifact hygiene self-test and validator passed on 2026-05-18.
- Resolved: 2026-05-18 delivery placeholder subject/body fallback. Source: `services/delivery-service/src/main/java/com/legent/delivery/service/DeliveryOrchestrationService.java` and `DeliveryOrchestrationServiceTest`. First-attempt delivery now requires a resolved `contentReference`, `subject`, and `htmlBody` before inbox safety/provider selection; unresolved content is terminal `CONTENT_UNAVAILABLE` and never reaches provider dispatch. Validation: integrated focused Maven sweep passed on 2026-05-18.
- Resolved: 2026-05-18 audience resolution missing-workspace silent ack. Source: `services/audience-service/src/main/java/com/legent/audience/event/AudienceResolutionConsumer.java` and `AudienceResolutionConsumerTest`. Missing `workspaceId` now throws with event metadata before idempotency or side effects so Kafka retry/DLQ can engage. Validation: integrated focused Maven sweep passed on 2026-05-18.
- Resolved: 2026-05-18 tracking-service Compose startup failure. Source: `docker compose up -d` and `docker compose logs --tail=160 tracking-service` showed `RawEventRetentionService` required `@Qualifier("jdbcTemplate")` while only `clickHouseJdbcTemplate` existed after ClickHouse config caused Boot JDBC auto-config to back off. `services/tracking-service/src/main/java/com/legent/tracking/config/ClickHouseConfig.java` now defines a primary Postgres `jdbcTemplate`; `ClickHouseConfigTest` covers the bean wiring. Validation: focused tracking Maven tests, full Maven reactor test, Docker Compose rebuild/start, `validate-compose-health.ps1`, gateway health, Postgres readiness, and compose Playwright smoke passed on 2026-05-18.
- Open correctness risks are tracked in `unresolved-risks.md`, `security-findings.md`, and `performance-bottlenecks.md`.
