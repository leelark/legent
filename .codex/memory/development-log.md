# Development Log

Last updated: 2026-05-16.

- 2026-05-13: Started autonomous setup from user request. Read AGENTS, git state, Maven/frontend manifests, CI, Compose, route-map, Nginx, risk markers, tests, migrations, large-file hotspots. Created `.codex` dirs, bootstrap, command docs, checkpoint, report, and memory maps.
- 2026-05-16: Superseding validation note: older entries that say production overlay validation passed predate the later broad-egress fail-closed validator. Current truth is route validation, Compose config, and production Kustomize render pass, while `validate-production-overlay.ps1` intentionally fails on inherited broad egress until reviewed CIDR/FQDN/CNI policy data exists.
- 2026-05-13: Validation after setup: `scripts\ops\validate-route-map.ps1` passed for 41 routes; `docker compose config --quiet` passed.
- 2026-05-13: Implemented first hardening batch: DDL defaults validate, Compose health drift fixed, Kafka trusted packages narrowed. Validation: shared Kafka Maven tests passed, Compose config passed with and without Redis password, PowerShell parser check passed, rg scans found no old DDL/Kafka wildcard defaults.
- 2026-05-13: Resumed `start-org`, selected bounded P0 security risk in shared request context, and hardened workspace/environment conflict checks in `TenantFilter`. Validation: `.\mvnw.cmd -pl shared/legent-security -am test` passed.
- 2026-05-13: Parallel subagent hardening run completed. Curie patched high-volume Kafka key selection, Copernicus patched audience resolved-event chunking, and Lorentz patched delivery send-request failure propagation. Integrated validation passed with `.\mvnw.cmd -pl shared/legent-kafka,shared/legent-security,services/audience-service,services/delivery-service -am test`.
- 2026-05-13: Loop 1 repository intelligence completed with six parallel agents. Findings: audience resolution still materializes full lists and suppression checks fail open; remaining Kafka consumers swallow processing failures; `/ws/analytics` is anonymous, lacks workspace handshake, and is not routed by gateway; platform listeners/async webhook handoff can silently ack; campaign send execution performs per-recipient remote rendering; production Kustomize render still has `legent/*:latest`, `.local` hosts, and `CLICKHOUSE_DB: default`. Implementation lanes queued with disjoint ownership.
- 2026-05-13: Loop 2 specialized execution completed across six disjoint workers. Fixes: default all-subscriber audience resolution paging and fail-closed suppression checks, Kafka DLQ recoverer plus listener rethrow behavior, authenticated tracking WebSocket handshake and frontend guard, platform durable webhook/search failure behavior, campaign per-batch render cache, and production overlay drift validation/routing fixes. Integrated validation passed with impacted Maven modules, frontend lint, route-map validation, production overlay validation, Compose config, and Kustomize render.
- 2026-05-13: Loop 3 reviewer pass completed. Fixed reviewer blockers: Kafka retry exhaustion now routes to declared central `kafka.dead-letter`, refreshed access tokens preserve workspace/environment claims for analytics WebSocket auth, frontend analytics sockets derive host from API base URL, production ingress CORS allows workspace/environment headers, and audience all-subscriber resolution now uses one-pass chunk lookahead with keyset paging. Validation: `.\mvnw.cmd -pl shared/legent-common,shared/legent-security,shared/legent-kafka,services/audience-service,services/tracking-service,services/identity-service -am test` passed.
- 2026-05-13: Loop 4 integration/regression validation completed. Validation: changed-service Maven slice for shared/security/kafka/audience/tracking/deliverability/automation/campaign/platform/delivery/identity passed; `npm run lint` passed with existing `any` warnings; `git diff --check` passed with line-ending warnings only; `validate-route-map.ps1`, `validate-production-overlay.ps1`, and `docker compose config --quiet` passed.
- 2026-05-13: Loop 5 production-readiness validation completed. Validation passed: full `.\mvnw.cmd clean compile`, full `.\mvnw.cmd test`, `.\mvnw.cmd -DskipTests package`, frontend `npm run lint`, frontend `npm run build`, Playwright smoke, `docker compose build`, `docker compose up -d`, `validate-compose-health.ps1`, Nginx `nginx -t`, `docker compose config --quiet`, route validation, production overlay validation, release-gate infra slice, and recent Compose log scan for critical startup patterns. Runtime fixes during validation: health script now derives frontend host port from Compose; gateway WebSocket route no longer duplicates `proxy_http_version`.
- 2026-05-13: Resumed five-loop execution Loop 1 completed with six parallel execution lanes. Fixes: audience LIST/SEGMENT DB keyset candidate paging, identity workspace membership guard for context/delegation tokens, route-map/Nginx/ingress/controller drift sync, shared frontend DOMPurify sanitizer profiles, content-service jsoup sanitizer policy/tests, GA evidence matrix docs, and production egress blocker recording. Validation reported by workers: audience focused tests passed, identity/security focused tests passed after refreshing stale local `legent-security`, content sanitizer tests passed, frontend lint passed, route/Nginx/Compose/Kustomize checks passed.
- 2026-05-13: Resumed Loop 2 reanalysis found and fixed missed issues: frontend sanitizer TypeScript config spread, identity delegated-subject membership validation and ambiguous default workspace selection, CSS-escaped `javascript:` in sanitizer CSS URLs, missing audience candidate repository integration tests, production ingress regex precedence shadowing, and egress/route-validation wording overclaims. Validation passed: frontend `npm run build`; touched Maven slice for security/identity/audience/content; route validation with rendered ingress precedence; `docker compose config --quiet`; production Kustomize render; `git diff --check` with line-ending warnings only.
- 2026-05-13: Resumed Loop 5 final production-readiness review completed with six reviewers and disjoint audience/campaign workers. Fixed audience transactional idempotency/publish handoff, campaign completion-based idempotency and terminal failure reconciliation, production ExternalSecret required keys/validation, public optional-auth and public landing fetch behavior, identity unvalidated environment context minting, SSO/SCIM tenant-free filtering, content public landing security config, and foundation dynamic workspace-scoped helper paths. Focused validations passed; production overlay validation now fails only on the known broad egress blocker. Final full validation sweep started next.
- 2026-05-13: Final validation sweep completed. Passed: Maven clean compile, full Maven tests, Maven verify, frontend dependency install, lint with existing `any` warnings only, frontend typecheck, production build, Playwright smoke and visual smoke, sanitizer regression rerun with web-server skip, Compose config, Docker build, Docker startup, Compose health validator, Nginx config test, direct gateway/frontend health probes, route validation, Kustomize production render, `.env.example` validation, and post-health log scan. Release-gate infra slice remains blocked only by intentional production broad-egress validation.
