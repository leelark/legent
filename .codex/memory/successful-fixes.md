# Successful Fixes

Fresh baseline date: 2026-05-20.
Last compacted: 2026-05-23.

Detailed completed-fix history was compacted out of current-state memory. The source of truth for completed work is .codex/backlog/queue.json doneWork, with recovery detail in .codex/checkpoints and audit detail in .codex/audit/events.

## Current Durable Summary

- 2026-05-20 to 2026-05-24: 210 local work items are recorded as DONE in the queue after `service-to-service-internal-route-identity-expansion` closeout.
- Completed themes include tenant/workspace fail-closed guards, route/ingress validation, bounded list/read paths, audience metadata-only resolved-chunk Kafka references backed by audience-owned durable chunk rows, outbox decoupling with terminal-row retention cleanup, tracking analytics dedupe/windowing, immutable send-governance policy snapshots before delivery dispatch, Admin UI send-governance policy management with campaign selection by named version, AI provider contract/metering ledgers with no-provider-invocation guarantees, reviewed AI draft apply UX using hash/reference evidence only, public/admin AI and deliverability claim-boundary copy, Segment Builder v2 docs-only taxonomy, Automation Studio live-run lock/override controls, import-time subscriber/data-extension provenance population, audience CSV parser/preview and modal/toast workflow polish, frontend auth/context hardening, Kafka topic drift coverage, release validator hardening, Deployment Manager local-vs-strict evidence ledger UI/docs with validator mapping, backend Failsafe integration-test phase with Testcontainers routing out of the unit gate, conservative backend/frontend coverage gates with JaCoCo/Vitest reporting, full Chromium Playwright CI/manual gate, and Campaign Wizard provider/domain selectors with inline readiness gates.
- 2026-05-24: route-map validation found the audience resolution chunk internal route lacked public-edge deny guards; Nginx and Kubernetes ingress now return 404 for `^/api/v1/audience-resolution-chunks/[^/]+/internal$`, and route validation plus production Kustomize render pass locally.
- 2026-05-24: approved campaign frequency optimization evidence now persists as campaign-owned metadata and can only reduce an existing send-safety frequency cap when policy key, run id, snapshot hash, recommended cap, and approval timestamp are present; focused campaign tests and cross-service optimization/rate/reputation tests pass locally.
- 2026-05-24: audience-service now persists first-class tenant/workspace-scoped data-extension relationship/cardinality rows while preserving the existing relationships API and blocking relationship traversal in query/segment paths; focused audience tests and the full audience-service reactor pass locally.
- 2026-05-24: delivery-service send rate control now supports bounded batch reservations for one provider/domain scope using one locked state/warmup transaction while preserving idempotency, caps, and expired-lease reclaim; focused and full delivery-service tests pass locally.
- 2026-05-24: audience-service Segment Builder v2 now compiles existing subscriber, list-membership, and segment-membership rules through a bounded execution plan, adds read-only explain metadata, compiles count preview before cache lookup, and compiles recompute before membership deletion; focused and full audience-service tests pass locally.
- 2026-05-24: campaign-service now persists approved deterministic SEND_TIME decision snapshots and revalidates STO evidence through campaign schedule, Launch Command Center handoff, send-job creation, and due scheduled-job dispatch; focused foundation/campaign tests and the full campaign-service reactor pass locally.
- 2026-05-24: automation-service now moves live file-drop and artifact-backed extract artifacts through a scoped MinIO storage adapter with tenant/workspace object-key validation, size/hash/content-type verification, idempotency, activity locks, redacted run history, and explicit data-extension extract fail-closed behavior; focused and full automation-service tests, route-map validation, Compose config, and diff whitespace checks pass locally.
- 2026-05-24: audience-service now has a tenant/workspace-scoped contact lifecycle audit substrate for subscriber deletion/merge/bulk-delete, suppression create/delete/recover/bulk-create, preference update/pause/unsubscribe/resubscribe, data-extension retention/delete/overwrite aggregates, and explicit send-eligibility checks using SHA-256 email hashes and bounded metadata; focused and full audience-service tests, repo artifact hygiene, ddl-auto scan, and diff whitespace checks pass locally.
- 2026-05-24: shared Kafka and campaign/delivery services now expose local retry/DLQ readiness metrics, shared DLQ topic retention config, Prometheus/Grafana/runbook coverage, and GA Kafka broker topology evidence validation; focused tests, full shared/campaign/delivery reactor tests, release evidence validator self-test, production overlay validation, repo artifact hygiene, and diff whitespace checks pass locally.
- 2026-05-24: foundation-service now exposes provider-free AI segment and workflow generation preview contracts under target-specific RBAC, composing AI content governance and provider metering decisions while returning preview-only, non-activating, non-publishing drafts with hash evidence and no model invocation; focused foundation tests pass locally.
- 2026-05-24: automation-service now supports `WAIT_UNTIL` as a live journey runtime node using the existing Quartz resume path, bounded ISO-8601 wait timestamps, tenant/workspace wake metadata, and Journey Builder capability-aware timestamp editing; focused/full automation-service tests and targeted frontend lint/build/Playwright validation pass locally.
- 2026-05-24: shared common, campaign-service, and audience-service now have a signed internal service identity layer for the campaign-to-audience audience-resolution chunk read path. Campaign sends service name, timestamp, and HMAC signature headers bound to tenant, workspace, job, chunk, and action; audience validates the caller allowlist before reading chunk data. Focused/full affected-module tests, route validation, environment validation, Compose config, production overlay render, repo artifact hygiene, and diff whitespace checks pass locally.
- 2026-05-24: foundation-service now exposes an enterprise package export and dry-run import validation contract for Foundation-owned metadata/configuration objects. Manifests carry schema version, source tenant/workspace/environment, object hashes, dependency edges, validation requirements, and a checksum; import validation checks scope, target environment locks, object allowlist/denylist, object hashes, unsafe payload indicators, and returns a no-mutation diff. Focused and full foundation-service tests, route validation, repo artifact hygiene, and scoped diff whitespace checks pass locally.
- 2026-05-24: tracking-service now exposes a local raw/canonical analytics contract. Operational event counts, timelines, experiment metrics, journey goals, rollups, ClickHouse rollup metadata, and campaign reconciliation use canonical event-id semantics; exports remain physical raw-row diagnostics and reconciliation returns both canonical and raw counts. Focused analytics tests and the full tracking-service reactor pass locally.
- 2026-05-24: frontend Segment Builder now exposes a local mode-aware governance panel with built-in subscriber/list field classifications, PII/incomplete/index review warnings, advanced tenant/workspace/suppression/execution lock indicators, draft-only unsupported family states, responsive rule rows, and focused Playwright coverage. Frontend lint, build, and targeted audience Playwright tests pass locally.
- 2026-05-24: Automation Studio authoring now uses governed data-extension/user selectors, campaign catalog selection for journey send nodes, and scoped artifact ID verification through the existing artifact get-by-id API while preserving current activity input/output contracts. Frontend lint, build, and targeted automation Playwright tests pass locally.
- 2026-05-24: Alertmanager receiver handoff placeholders now have platform and SRE production ExternalSecret backing, and production overlay validation fails when an Alertmanager handoff key is missing from that contract. Production overlay validation, release evidence validator self-test, and scoped diff whitespace checks pass locally.
- 2026-05-24: frontend visual shell smoke is now a required CI and local release-gate step after production build, with CI screenshot artifacts and documented baseline refresh policy. Targeted visual smoke, frontend-only local release gate, and scoped diff whitespace checks pass locally.
- 2026-05-24: campaign launch readiness now consumes aggregate tenant/workspace suppression counts from deliverability-service instead of deriving complaint and hard-bounce totals from a capped suppression-list sample. Focused campaign and deliverability controller/security tests pass locally.
- 2026-05-24: delivery-service now has a production suppression-signal writer for bounced, complaint, and unsubscribe feedback events. The consumer records tenant/workspace-scoped delivery-owned signals with idempotent event claims, and InboxSafety blocks active unsubscribe signals in addition to bounce/complaint signals. Focused delivery-service tests pass locally.
- 2026-05-24: deliverability suppression internal routes now require signed service identity for campaign suppression-history reads and audience suppression bulk checks. Campaign and audience clients send service/action/timestamp HMAC headers, and deliverability validates caller allowlists before internal suppression access. Focused shared/common, audience, campaign, and deliverability tests plus route-map validation pass locally.
- 2026-05-24: Automation Studio activity authoring now matches live backend activity contracts for file-drop, extract, and send-email activities. FILE_DROP and EXTRACT are presented as live-supported, file-drop creates output artifact payloads, extract uses backend-recognized `IMPORT_ARTIFACT`/`GENERATED_EXTRACT` source values, and SEND_EMAIL uses campaign catalog selection for governed campaign handoff. Frontend lint, build, and the targeted Automation Studio Chromium Playwright spec pass locally.
- 2026-05-24: automation-service now treats deterministic BRANCH and SPLIT journey nodes as condition-handler aliases for validation and runtime dispatch. Backend capabilities and frontend fallback capabilities include both node types, and focused automation-service plus Automation Builder frontend tests pass locally.
- 2026-05-24: release frontend gate wiring now requires visual smoke, full Chromium, and smoke coverage consistently across `release-gate.ps1`, `validation-registry.json`, and `release-pass.md`; release evidence validator self-tests now fail fast if those frontend gate steps or explicit Compose env usage drift. The release evidence self-test, local-only release gate smoke, Codex validation, and scoped diff checks pass locally.
- These completions are local evidence only unless paired with target runtime, provider, release, load, restore, monitoring, and CI/security artifacts.

When adding future entries here, record only durable summaries. Put long validation logs in queue/checkpoints/audit reports.
