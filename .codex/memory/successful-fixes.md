# Successful Fixes

Last updated: 2026-05-16.

- 2026-05-16: Shared workspace context adoption gap closed. Authenticated JWT requests can no longer set arbitrary `X-Workspace-Id` when the token lacks a workspace claim; identity/foundation workspace token minting remains the membership proof boundary, while internal non-JWT service calls retain explicit tenant/workspace headers. Validation: `.\mvnw.cmd -pl shared/legent-security "-Dtest=TenantFilterTest,JwtAuthenticationFilterTest,JwtTokenProviderTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` passed with 20 tests.
- 2026-05-16: RBAC/workspace authorization locally actionable controller pass closed for automation workflows, tracking analytics/export/BI, and platform admin alias regression coverage. Automation workflow/studio endpoints now have method-level `workflow:read`/`workflow:write` checks; tracking analytics/export/funnel/segment reads require `tracking:read` or `analytics:read`; BI rollup ensure/refresh requires `tracking:write` or `analytics:write`; and platform admin webhook/search/config RBAC expressions are covered by focused tests. Validation: automation/platform RBAC Maven tests passed, and integrated tracking Maven tests including `TrackingAnalyticsControllerRbacTest` passed.
- 2026-05-16: Tracking locally actionable raw-event/outbox risks closed. Redis dedupe now reserves keys atomically with Lua `SET NX PX`; ClickHouse raw-event writes include `workspace_id`; ClickHouse schema contracts cover workspace lineage, monthly partitions, and 180-day TTL; Postgres raw events have a retention policy/purge function; and outbox publish duration plus lease-utilization metrics are emitted. Validation: `.\mvnw.cmd -pl services/tracking-service -am '-Dtest=TrackingIngestionServiceTest,TrackingOutboxServiceTest,ClickHouseRollupServiceTest,ClickHouseWriterTest,RawEventRetentionMigrationTest' '-Dsurefire.failIfNoSpecifiedTests=false' test` passed with 14 tracking tests. Residual: live ClickHouse ingest/load proof remains required before GA.
- 2026-05-16: Docs/scripts drift hygiene pass aligned CSRF docs with current unsafe cookie-auth Origin/Referer fail-closed behavior, refreshed Compose health memory to reflect live Compose publisher port detection, and moved the generic docs/scripts drift item out of unresolved risks after validating current release-gate, synthetic-smoke, backup-restore, load-harness, and Compose health flags/ports against script parameters. Source: `docs/security/csrf-cookie-threat-model.md`, `.codex/memory/deployment-flow.md`, `.codex/memory/unresolved-risks.md`, `.codex/reports/docs-scripts-drift-2026-05-16.md`. Validation: PowerShell parser checks, route-map validation, scoped `git diff --check`, and stale-phrase grep passed.
- 2026-05-16: Infra/release evidence checks hardened locally. Production ingress now renders TLS references for `api.legent.com` and `app.legent.com`, the production overlay enforces restricted Pod Security labels and deployment hardening, `validate-production-overlay.ps1` checks TLS/restricted/alert rules and can fail strict digest mode with `-RequireImageDigests`, CI uploads a Trivy filesystem SBOM and generated production image supply-chain checklist, and operations docs distinguish repo-validated checks from external TLS/admission/registry/observability evidence. Validation: production Kustomize render passed, production overlay validation passed with expected non-digest warning, image checklist generation passed, and strict digest mode failed as expected on current version-tagged images.
- 2026-05-16: Full-audit wave 2 worker follow-up closed additional local hardening items. Campaign content render calls now pass tenant/workspace plus optional environment/request/correlation headers to the internal content render endpoint, fail closed when send batches lack workspace context, and unused campaign content read helpers were removed to avoid unauthenticated service-to-service calls to RBAC-protected content routes. Frontend no longer trusts stored role fallback for admin access, redirects the old tracking route to analytics, routes unknown search results to workspace home, and keeps touched API helpers lint-clean. Production overlay validation now requires observability alert rules to render through Kustomize. Validation: combined focused Maven sweep over changed backend tests passed, tracking-service full Maven slice passed with 25 tests, touched-file frontend ESLint passed, `npm run build:next` passed in worker run, focused frontend Playwright suites passed, `kubectl kustomize infrastructure/kubernetes/observability`, production overlay validation, release-gate infra slice, `git diff --check`, and backup parser/ignore checks passed. Residual: live Prometheus/Alertmanager load/firing proof, TLS/restricted-pod/supply-chain evidence, full campaign-service suite, full frontend lint, and live GA evidence remain.
- 2026-05-16: Tracking outbox publish race reduced. `TrackingOutboxService.publishPending` now acquires a conditional database claim via `TrackingOutboxEventRepository.claimReadyForPublish` before loading/deserializing the payload or publishing to Kafka; competing pollers that lose the claim return without publishing. Existing `PUBLISHING` rows whose `next_attempt_at` has expired remain claimable for stale-worker recovery. Superseded residuals on 2026-05-16: raw-event retention, Redis atomic dedupe, ClickHouse workspace lineage, and outbox lease metrics were added later in the unresolved-risk sweep; live target load proof remains open.
- 2026-05-16: Full-audit wave 2 closed locally actionable retry/signature/suppression/frontend/backup hygiene defects. Platform webhook retry claims now have `claim_started_at`, bounded stale RETRYING recovery, and clear claims on terminal/rescheduled paths; delivery tracking URL signing and tracking verification now share `TrackingSignatureSupport` with versioned tenant keys; delivery inbox safety suppression checks are tenant/workspace/email/type/deleted scoped; frontend credentialed API calls reject external absolute URLs, public contact uses no-credential `postPublic`, and protected 401 clears local auth/context; backup artifacts and `.env*` files are ignored from git/docker context and the backup script defaults outside the repo. Validation: platform `WebhookRetryServiceTest`, shared/delivery/tracking signature tests, delivery `InboxSafetyServiceTest`, frontend API-context Playwright, frontend TypeScript no-emit, `git diff --check`, PowerShell backup guard/parser checks, and git ignore checks passed. Residual: external egress/live GA evidence, shared workspace-membership auth, audience V17 production mapping, tracking raw-events/ClickHouse/outbox risks, campaign batch lease proof, domain verification ownership tokens, remaining service RBAC inventory, observability/TLS/supply-chain evidence, and frontend large-file/lint debt remain.
- 2026-05-16: Deliverability sender-domain ownership risk closed. Registration/challenge rotation now issues tenant/workspace-specific `_legent-verification.<domain>` TXT tokens, verification requires exact token proof plus SPF/DKIM/DMARC, attempts are audited in `sender_domain_verification_history`, and auth-check/predictive readiness fail closed unless proof is fresh. Validation: `.\mvnw.cmd -pl services/deliverability-service -am test "-Dtest=DomainVerificationServiceTest,PredictiveDeliverabilityServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false"` passed with 4 tests.
- 2026-05-16: Production-blocker wave closed locally actionable release blockers. Production overlay deletes inherited broad egress and permits only same-namespace pod egress plus DNS; campaign launch/direct send now fails closed on deliverability DNS/auth, suppression health, delivery warmup, and provider capacity; delivery rate/warmup reservations use durable idempotent DB leases with release/settle behavior; content and platform authenticated resources are workspace-scoped in schema and service filters; campaign send payloads externalize to content references when required and delivery resolves references on first attempt. Validation: combined backend Maven reactor test passed, full `scripts\ops\release-gate.ps1` passed, route-map validation passed, production overlay validation passed, Docker Compose config passed, `npm audit --omit=dev --audit-level=high` found 0 vulnerabilities, and load harness dry-run passed. Residual: exact live external egress policy data, live load/restore/security/monitoring evidence, shared workspace-membership auth contract, audience production mapping metadata, and stale webhook retry lease recovery remain.
- 2026-05-16: Campaign send payload externalization now fails closed when enabled or required references cannot be created, and delivery resolves existing rendered references on first-attempt sends. `SendExecutionService` publishes reference-only payloads without `htmlBody`/`textBody` when inline content is disabled or inline payloads exceed the byte cap, externalizes oversized rendered bodies instead of rejecting them when references are available, and marks content-reference creation failures terminal before Kafka handoff; delivery resolves cached `cr_...` content before placeholder fallback. Validation: campaign reference/send tests passed with 13 tests, and `.\mvnw.cmd -pl services/delivery-service -am "-Dtest=DeliveryOrchestrationServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` passed with 11 tests.
- 2026-05-16: Platform webhook DNS rebinding residual closed. `OutboundUrlGuard` now exposes resolved-address public/private checks and covers additional reserved ranges; platform webhook dispatch/retry uses a dedicated webhook client with connector-time resolved-address validation; production direct webhook egress fails closed unless `WEBHOOK_OUTBOUND_PROXY_URL`/`legent.webhooks.outbound.proxy-url` is configured. Validation: `.\mvnw.cmd -pl shared/legent-common,services/platform-service -am "-Dtest=OutboundUrlGuardTest,WebhookWebClientConfigTest,WebhookControllerTest,WebhookDispatcherServiceTest,WebhookRetryServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` passed with 26 tests; scoped `git diff --check` passed.
- 2026-05-16: Full-audit parallel hardening wave completed across shared security, identity, audience, delivery, campaign, tracking, platform, and frontend context handling. Frontend API calls now require tenant/workspace context for non-public routes with bounded path matching, SCIM token copy errors no longer expose raw tokens, unsafe cookie-auth writes without Origin/Referer fail closed, `t=` tenant query fallback is limited to signed public tracking open/click paths, federated SSO no longer grants privileged roles from raw unmapped group names or links native/other-provider users by email fallback, SCIM user operations are provider-bound, audience/delivery mutating APIs have method-level RBAC, campaign launch/readiness now fails closed on missing or mismatched sender/sending domain alignment, tracking Redis dedupe keys include workspace, and webhook retry processing fetches bounded batches with claim-before-dispatch semantics. Validation: `git diff --check`, shared-security focused tests, focused Playwright API-context tests, frontend lint, focused Maven tests, and changed-service Maven reactor sweep passed. Superseded residuals on 2026-05-16: production broad egress inheritance, content/platform workspace scope, service-backed campaign launch gates, atomic delivery rate reservation, and full send-payload externalization were addressed; live external egress data and GA evidence remain.
- 2026-05-16: Five-loop repository organization pass hardened identity, content, foundation, platform, campaign, and delivery paths. Identity invitation create/list responses are token-free, invitation TTL is bounded, duplicate local JWT filtering is de-registered, content latest-version reads require RBAC, foundation core teams/departments/role writes are workspace/tenant scoped, platform webhook validation rejects weak/unknown inputs, campaign content references are default-off and inline-safe, and delivery retry content resolution fails closed without placeholder sends. Validation: `git diff --check`, full touched backend Maven reactor test sweep, frontend lint, `.env.example` validation, route validation, Docker Compose config, and production Kustomize render passed. Superseded residual: production overlay validation now passes after the 2026-05-16 egress overlay change.
- 2026-05-16: Final local runtime validation hardening completed. `frontend/Dockerfile` now carries a 30-minute `LEGENT_BUILD_TIMEOUT_MS` for constrained Docker builds, and `docker-compose.yml` supplies the existing required `LEGENT_INTERNAL_API_TOKEN` environment variable to content and campaign services without committing any secret value. Validation: serial Maven package produced service JARs, `docker compose build` passed, `docker compose up -d content-service campaign-service gateway` passed, full `validate-compose-health.ps1` passed, direct frontend/gateway health probes returned 200, and a post-health 5-minute critical log scan found no current application startup failures.
- 2026-05-16: Historical validation clarification: older entries that mention `validate-production-overlay.ps1` or production overlay validation passing may predate the 2026-05-13 broad-egress fail-closed check. Current validator fails by design until production egress policy data is supplied.
- 2026-05-16: `.codex` pending sweep completed across backend reliability, frontend flow, dead-code cleanup, and stale docs/memory. Foundation bootstrap now rethrows provisioning failures and awaits critical bootstrap publishes; automation workflow triggers and deliverability feedback events use pending idempotency claims; audience subscriber intelligence failures propagate for rollback/retry; workspace search routes results to owning screens; mobile shell exposes Admin/Settings through More; dependency-proven dead files/methods were removed. Validation: `git diff --check`, affected backend module Maven test sweep, frontend lint, TypeScript no-emit, build:ci, admin Playwright, smoke Playwright, visual smoke, route-map validation, and Compose config passed.
- 2026-05-16: Platform webhook create validation now fails closed for unknown, malformed, or excessive event subscriptions, trims strong client-provided `secretKey` values, and rejects weak secrets shorter than 32 mixed-character bytes before persistence. Validation: `.\mvnw.cmd -pl services/platform-service -am "-Dtest=WebhookControllerTest,WebhookDispatcherServiceTest,WebhookRetryServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` passed.
- 2026-05-16: Admin and settings workspace UI alignment completed. `EnterpriseAdminConsole` and `EnterpriseSettingsConsole` now use the shared workspace chrome (`PageHeader`, `Card`, `Panel`, and `MetricCard`) instead of bespoke animated hero/control surfaces; `framer-motion` usage and target-file animation classes were removed, target catch blocks use typed `unknown` handling, and admin/settings/smoke/visual Playwright expectations now match the shorter workspace headings. Validation: focused ESLint for touched files passed, `npx tsc --noEmit` passed, `npm run lint` passed with existing unrelated warnings, `npm run build` passed, admin Playwright passed 4/4, smoke Playwright passed 9/9, and visual smoke passed 8/8.
- 2026-05-14: Pending `.codex` sweep closed locally actionable items. Platform foundation-settings bridge now relays caller auth to foundation and fails closed without a token; platform async/event paths propagate workspace/environment context; landing-page form controls are removed from frontend/backend sanitizers and default UX; campaign send execution rejects oversized rendered send payloads before Kafka publish; stale README/API-contract docs and a dead foundation helper were cleaned. Validation: focused content/platform/campaign Maven tests passed, foundation compile passed, frontend sanitizer Playwright passed after assertion correction, frontend lint/typecheck passed, route validation passed, and `.env.example` validation passed.
- 2026-05-14: Delivery and tracking event consumers now fail closed on envelope/payload tenant or workspace mismatches and normalize payload scope to envelope scope before side effects. Delivery/tracking idempotency now uses claim/mark/release semantics with nullable pending-claim migrations (`delivery` V13, `tracking` V10) so failed work can retry instead of being permanently skipped. Validation: focused delivery/tracking Maven tests passed.
- 2026-05-14: Platform config/webhook/content publish security was hardened. Platform config now requires config/platform RBAC and fails closed when tenant/workspace context or the foundation bridge fails; webhook secrets are write-only in JSON and responses expose only `secretConfigured`; content landing-page write/publish/archive/delete routes now require content/template RBAC. Validation: focused platform/content Maven tests and frontend typecheck passed.
- 2026-05-14: Release/load/infra gates now fail closed on native command and production drift failures. `release-gate.ps1` checks native exit codes, `validate-production-overlay.ps1` checks Kustomize exit plus DDL/Flyway render invariants, route validation checks broad controller mappings, and high-volume load live mode rejects placeholder or under-specified campaign IDs. Validation: route validation, load dry-run/negative guards, and production validator to the expected egress blocker passed.
- 2026-05-14: All service-local Flyway defaults now align with production-safe behavior: `baseline-on-migrate=false`, `validate-on-migrate=true`, and `out-of-order=false`, with overrides only through explicit environment variables. Source: all service `application.yml` files and Kubernetes base `configmap.yml`.
- 2026-05-14: Landing-page sanitization now removes `<form>` tags and form controls in frontend and backend public HTML paths. Published landing-page updates are sanitized even when `publish=false`, and public responses sanitize defensively. Validation: content-service sanitizer/resource tests and sanitizer Playwright regression passed.
- 2026-05-14: Added fail-closed audience data-extension legacy workspace mapping guard. `V17__guard_data_extension_workspace_mapping.sql` blocks upgrades with existing `workspace-default` data extensions unless operators pre-create and populate reviewed `public.audience_data_extension_workspace_mapping_review` metadata, validates completeness/duplicates, then remaps data extensions and records from the reviewed table. `docs/operations/audience-data-extension-workspace-mapping.md` documents the operator preflight. Validation: `.\mvnw.cmd -pl services/audience-service -am test` passed. Residual: V17 validates mapping metadata, not target workspace existence outside the audience DB.
- 2026-05-13: Hardened content-service HTML sanitization. `EmailContentValidationService` now sanitizes via jsoup DOM parsing and explicit tag/attribute/URL/CSS policy instead of regex mutation, unwraps/removes landing-page form tags, removes dangerous script/SVG/MathML content, strips event handlers, encoded JavaScript URLs, `srcset`, and unsafe CSS `url(...)` rules. Validation: `.\mvnw.cmd -pl services/content-service -Dtest=EmailContentValidationServiceTest test` passed with 12 tests.
- 2026-05-16: Fixed identity duplicate JWT filter/principal overwrite risk. The identity-local `JwtAuthenticationFilter` is no longer component-scanned, so it cannot run as an extra servlet filter and replace the shared `com.legent.security.UserPrincipal`; `UserController` preference endpoints now accept the shared principal type. Validation: focused Surefire reports for `UserControllerTest` and `JwtAuthenticationFilterRegistrationTest` show 2 tests passed; Maven invocations timed out after report generation.
- 2026-05-13: Hardened non-test JPA DDL defaults from `update` to `validate` in all 10 service `application.yml` files and Kubernetes base `configmap.yml`. Impact: missing Flyway/schema drift now fails closed instead of mutating schemas by default. Validation: `rg -n "ddl-auto|SPRING_JPA_HIBERNATE_DDL_AUTO"` confirmed only non-test `validate` plus intentional test `create-drop`; `docker compose config --quiet` passed.
- 2026-05-13: Fixed local Compose health drift. `scripts/ops/validate-compose-health.ps1` now probes `$env:FRONTEND_HOST_PORT` or 3000; Redis healthcheck authenticates through `REDISCLI_AUTH` when `REDIS_PASSWORD` is present without printing the secret. Validation: PowerShell parser check passed; `docker compose config --quiet` passed with and without a throwaway `REDIS_PASSWORD`.
- 2026-05-13: Narrowed Kafka JSON deserialization trust. Shared `KafkaConsumerConfig` now defaults trusted packages to `java.lang,java.util,com.legent.kafka.model`, keeps default value type `EventEnvelope`, and disables type headers via property-backed defaults. Six service YAML wildcard entries were narrowed to the same allowlist. Validation: `.\mvnw.cmd -pl shared/legent-kafka -am test` passed; wildcard/broad-trust scan returned no matches.
- 2026-05-13: Hardened shared tenant/workspace request context. `TenantFilter` now rejects workspace/environment header conflicts against JWT-populated `TenantContext`, preserves JWT workspace/environment when headers are absent, and still allows trusted headers when no authenticated context exists. Validation: `.\mvnw.cmd -pl shared/legent-security -am test` passed.
- 2026-05-13: Reduced resolved-audience Kafka payload risk. `AudienceResolutionConsumer` now publishes bounded `SEND_BATCH_SIZE` chunks with deterministic chunk metadata and last-chunk semantics. Validation: integrated Maven test for shared Kafka/security plus audience/delivery services passed.
- 2026-05-13: Reduced audience-service resolution materialization and suppression bypass risk. `AudienceResolutionConsumer` now pages all-subscriber resolution with keyset cursor reads, checks suppressions per fetched page, publishes bounded chunks in one pass with lookahead last-chunk semantics, and rethrows suppression/publish failures. `DeliverabilityServiceClient` now fails closed on malformed or failed suppression responses. Validation: `.\mvnw.cmd -pl shared/legent-common,shared/legent-security,shared/legent-kafka,services/audience-service,services/tracking-service,services/identity-service -am test` passed.
- 2026-05-13: Reduced Kafka hot-partition risk. `EventPublisher` now derives or requires non-tenant keys for configured high-volume topics and preserves tenant fallback for low-volume topics. Validation: integrated Maven test for shared Kafka/security plus audience/delivery services passed.
- 2026-05-13: Reduced delivery consumer silent-ack risk. `DeliveryEventConsumer` now rethrows unexpected send-request failures after logging and preserves context cleanup. Validation: integrated Maven test for shared Kafka/security plus audience/delivery services passed.
- 2026-05-13: Wired shared Kafka listener retry exhaustion to real DLQ publishing. `KafkaConsumerConfig` now uses `DeadLetterPublishingRecoverer` with declared central topic `kafka.dead-letter`, and Kafka consumers in automation, tracking, deliverability, campaign, audience, delivery, and platform rethrow unexpected processing failures after logging while preserving explicit validation/malformed-event drops. Validation: impacted Maven slices passed.
- 2026-05-13: Hardened authenticated analytics WebSocket routing/context. `/ws/analytics` requires authentication and tenant/workspace context, handshake can derive workspace from authenticated principal, refresh preserves workspace/environment claims, frontend sockets use API-base origin, and ingress CORS allows workspace/environment headers. Validation: focused security/tracking/identity Maven slice passed; frontend lint passed in Loop 2.
- 2026-05-13: Fixed production infra drift for release rendering. Production Kustomize pins all `legent/*` images to `1.0.2`, renders production ingress hosts only, sets `CLICKHOUSE_DB=legent_analytics`, and routes `/ws/analytics` to `tracking-service` through Nginx/Kubernetes ingress. Added `scripts/ops/validate-production-overlay.ps1` and wired it into `release-gate.ps1`. Validation: `validate-route-map.ps1`, `validate-production-overlay.ps1`, and release-gate route/Kustomize slice passed.
- 2026-05-13: Fixed final runtime health validation drift. `validate-compose-health.ps1` now resolves the frontend host port from live Compose publisher metadata before falling back to env/defaults, and `config/nginx/nginx.conf` no longer duplicates `proxy_http_version` in the `/ws/analytics` location. Validation: `validate-compose-health.ps1`, `docker compose run --rm --no-deps gateway nginx -t`, `docker compose config --quiet`, route validation, production overlay validation, and recent Compose log scan passed.
- 2026-05-13: Removed LIST/SEGMENT audience full-ID materialization. `AudienceResolutionConsumer` now uses `AudienceCandidateRepository` DB keyset paging over subscribers, active list memberships, and segment memberships while preserving chunk metadata, fail-closed suppression, and publish retry semantics. Validation: `.\mvnw.cmd -pl services/audience-service clean test -Dtest=AudienceResolutionConsumerTest` passed.
- 2026-05-13: Closed identity workspace-token minting gap. `AuthService.switchContext` and delegation token exchange now require an active account/tenant/workspace membership before issuing workspace-scoped JWTs; legacy no-account fallback can only use the default workspace. Validation: `.\mvnw.cmd -pl services/identity-service -Dtest=AuthServiceTest test` passed after refreshing stale local shared security artifact.
- 2026-05-13: Synchronized gateway route ownership. Added known exposed identity/audience/foundation controller roots to route-map, Nginx, and ingress; removed unused `/api/v1/events`; route validation now checks literal/controller-root drift for known exposed roots. Validation: route validation, Nginx config test, Compose config, and production Kustomize render passed.
- 2026-05-13: Added frontend defense-in-depth sanitizer profiles for every current `dangerouslySetInnerHTML` sink. Validation: `npm run lint` passed with existing warnings only.
- 2026-05-13: Fixed production ingress regex shadowing for service-specific public/admin routes. `infrastructure/kubernetes/ingress/ingress.yml` now splits broad foundation admin/public routing into shorter paths so `/api/v1/public/landing-pages`, `/api/v1/admin/webhooks`, and `/api/v1/admin/search` keep rendered precedence; `scripts/ops/validate-route-map.ps1` now validates rendered ingress precedence and critical expected services. Validation: `powershell -ExecutionPolicy Bypass -File scripts\ops\validate-route-map.ps1` and `kubectl kustomize infrastructure/kubernetes/overlays/production` passed.
- 2026-05-13: Hardened identity delegated token minting. `exchangeDelegationToken` now verifies the delegating user and delegated subject are active and that the delegated subject owns the target tenant/workspace membership; ambiguous no-workspace context selection now fails unless a default membership is clear. Validation: `.\mvnw.cmd -pl services/identity-service -Dtest=AuthServiceTest test` passed.
- 2026-05-13: Closed CSS-escape sanitizer bypass. `EmailContentValidationService` normalizes CSS escapes before checking `url(...)` schemes, stripping vectors like `url(\6a avascript:alert(1))`. Validation: `.\mvnw.cmd -pl services/content-service -Dtest=EmailContentValidationServiceTest test` passed.
- 2026-05-13: Added repository-level coverage for audience candidate paging. `AudienceCandidateRepositoryImplTest` validates include list, include segment, exclude-only, mixed include/exclude, deleted subscriber skipping, workspace isolation, and keyset pagination. Validation: focused repository test passed and the combined touched Maven slice passed.
- 2026-05-13: Closed public landing-page external form-submit risk by stripping submit target attributes; superseded on 2026-05-14 by full `<form>` unwrap/remove in frontend and backend landing-page sanitizers pending a documented Legent-controlled submission endpoint model. Validation: `npx tsc --noEmit` passed; `.\mvnw.cmd -pl services/content-service -Dtest=EmailContentValidationServiceTest test` passed.
- 2026-05-13: Added audience candidate-query indexes for DB-backed LIST/SEGMENT paging. `V15__audience_candidate_query_indexes.sql` adds scoped indexes for active subscribers, active list memberships, and segment memberships. Validation: `.\mvnw.cmd -pl services/audience-service -DskipTests compile` passed.
- 2026-05-13: Workspace-scoped audience data extensions. `V16__workspace_scope_data_extensions.sql` adds/backfills `workspace_id` on data extensions and records, changes active name uniqueness to tenant+workspace, and service/repository filters now require workspace context. Validation: `.\mvnw.cmd -pl services/audience-service -Dtest=DataExtensionServiceTest test` passed.
- 2026-05-13: Reduced retained campaign batch payload pressure. `SendExecutionService` clears completed batch payload JSON to `[]` while preserving partial/retry payloads. Validation: `.\mvnw.cmd -pl services/campaign-service -Dtest=SendExecutionServiceTest test` passed.
- 2026-05-13: Production overlay validation now fails closed on broad inherited egress. `validate-production-overlay.ps1` rejects rendered `allow-legent-egress` and `NetworkPolicy` `ipBlock.cidr: 0.0.0.0/0`, preventing release-gate success until a reviewed production-specific egress model exists. Validation: Kustomize render passed; production overlay validation failed with the expected blocker.
- 2026-05-13: Loop 4 production ingress/network policy hardening. Added `production-frontend-ingress` NetworkPolicy for ingress-nginx to frontend pods on TCP 3000, split tracking and analytics routes into `legent-tracking-ingress` without the shared 100r/s annotation, moved child route-map prefixes before broad parent prefixes, added route-map parent-shadow validation, and changed Compose gateway API health validation to `/api/v1/health`. Validation: route map, `docker compose config --quiet`, and production Kustomize render passed. Superseded residual: inherited broad egress was deleted from the production overlay on 2026-05-16.
- 2026-05-13: Foundation public content admin updates are now workspace-scoped. `PublicContentService.upsert(..., id)` and `publish(id, ...)` load by `id + tenant_id + workspace_id` instead of bare ID; performance-intelligence/global/differentiation/compliance routes now require workspace context, reject `workspaceId` query conflicts, and service-level workspace resolvers reject request/body workspace mismatches where existing DTOs already carry `workspaceId`. Validation: `.\mvnw.cmd -pl services/foundation-service -am "-Dtest=PublicContentServiceTest,WorkspaceContextFilterTest,ComplianceEvidenceServiceTest,GlobalEnterpriseServiceTest,DifferentiationPlatformServiceTest,ExtensionGovernancePerformanceServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` passed with 17 tests.
- 2026-05-13: Campaign send handoff failures no longer disappear. `BatchingService` and `SendExecutionService` rethrow unexpected processing/publish failures for retry/DLQ, failed publish payloads remain available for retry, delivery counters are not advanced during durable handoff failure, and recipient success/failure counters remain owned by delivery feedback. Validation: `.\mvnw.cmd -pl services/campaign-service -am "-Dtest=BatchingServiceTest,SendExecutionServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` passed with 9 tests.
- 2026-05-13: Identity delegation now validates requester authority before minting delegated context. `exchangeDelegationToken` verifies requesting user/account activity and active requester membership in the target workspace before delegated-subject checks. Validation: combined touched Maven slice passed including `AuthServiceTest`.
- 2026-05-13: Audience data-extension workspace migration is fail-closed for ambiguous existing data. `V16__workspace_scope_data_extensions.sql` now preflights duplicate active names per tenant/workspace ignoring case, backfills records from parent data extensions, rejects orphan/mismatched records before `NOT NULL`, and indexes records by tenant/workspace/extension. Validation: combined touched Maven slice passed including `DataExtensionServiceTest`.
- 2026-05-13: Frontend workspace context hydration no longer overwrites a selected workspace from tenant-only match. `Header.tsx` hydrates selected context by tenant and workspace, does not persist localStorage during list hydration, and reverts the select value if context switching fails. Validation: `npm run lint`, `npx tsc --noEmit`, and sanitizer Playwright regression passed.
- 2026-05-13: Frontend sanitizer regressions now cover risky public HTML sinks. `frontend/tests/e2e/sanitize-html.spec.ts` verifies encoded `javascript:` URLs, `srcset`, event handlers, and landing-page form sanitization under Chromium. Validation: `.\\node_modules\\.bin\\playwright.cmd test tests/e2e/sanitize-html.spec.ts --project=chromium --reporter=line` passed with 4 tests.
- 2026-05-13: Audience resolution idempotency and publish handoff now fail atomically. `AudienceResolutionConsumer` is transactional, records idempotency inside the listener transaction, waits for `send.audience.resolved` Kafka futures, and rethrows async publish failures so retry/DLQ can engage instead of permanently suppressing reprocessing. Validation: `.\mvnw.cmd -pl services/audience-service -am "-Dtest=AudienceResolutionConsumerTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` and full `.\mvnw.cmd -pl services/audience-service -am test` passed.
- 2026-05-13: Campaign event idempotency and durable handoff are now completion-based. `CampaignEventIdempotencyService` claims first, marks processed only after side effects complete, releases uncompleted claims on failure, waits for critical Kafka publish futures, requeues unfinished batches after failed post-commit publishes, and reconciles terminal batch failure into job/campaign state. Validation: `.\mvnw.cmd -pl services/campaign-service -am "-Dtest=CampaignEventConsumerTest,CampaignEventPublisherTest,BatchingServiceTest,SendExecutionServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` passed with 14 tests.
- 2026-05-13: Production ExternalSecret coverage and validation are hardened. `external-secrets.yml` now includes `SPRING_DATASOURCE_PASSWORD`, `LEGENT_TRACKING_SIGNING_KEY`, `LEGENT_DELIVERY_CREDENTIAL_KEY`, and `LEGENT_DELIVERY_ENCRYPTION_SALT`, and `validate-production-overlay.ps1` checks rendered required runtime secret keys. Validation: production Kustomize render passed; production overlay validation now reaches the known fail-closed egress blocker.
- 2026-05-13: Public endpoints tolerate stale/invalid auth cookies only on documented public paths, and public landing-page reads now use a no-credential frontend client. `JwtAuthenticationFilter` lets optional-auth public paths proceed anonymously when a cookie is invalid, protected paths still 401, `publicApiClient` disables credentials, and content-service explicitly permits `/api/v1/public/landing-pages/**`. Validation: focused shared security, identity, content, frontend lint, and frontend typecheck commands passed.
- 2026-05-13: Identity no longer trusts client-supplied environment IDs during context switch. `AuthService.switchContext` ignores request `environmentId` until an authoritative environment-to-workspace validation model exists, and frontend bootstrap clears stale local environment context when the server does not provide one. Validation: `AuthServiceTest`, shared security tests, `npm run lint`, and `npx tsc --noEmit` passed.
- 2026-05-13: Foundation dynamic record helpers now enforce workspace scope on ID reads and updates. `CorePlatformRepository.updateByIdAndWorkspace` and service-level scoped `requireById` paths are used by global, differentiation, and performance helpers where workspace context exists. Validation: focused foundation Maven slice passed with 15 tests.
- 2026-05-13: Tenant-free SSO and SCIM routing now matches identity security intent. `TenantFilter` exempts `/api/v1/sso/**` and `/api/v1/scim/v2/**` from mandatory tenant headers while SCIM bearer/scope validation remains inside `ScimProvisioningService`. Validation: `TenantFilterTest` and shared security focused tests passed.
- 2026-05-13: `.env.example` now satisfies the repository environment validator while still using placeholder values. JWT and delivery credential placeholder strings were lengthened to meet `validate-env.ps1` minimum-length checks without introducing secrets. Validation: `powershell -ExecutionPolicy Bypass -File scripts\ops\validate-env.ps1 -EnvFile .env.example -AllowPlaceholders` passed.
