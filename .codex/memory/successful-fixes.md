# Successful Fixes

Last updated: 2026-05-13.

- 2026-05-13: Hardened non-test JPA DDL defaults from `update` to `validate` in all 10 service `application.yml` files and Kubernetes base `configmap.yml`. Impact: missing Flyway/schema drift now fails closed instead of mutating schemas by default. Validation: `rg -n "ddl-auto|SPRING_JPA_HIBERNATE_DDL_AUTO"` confirmed only non-test `validate` plus intentional test `create-drop`; `docker compose config --quiet` passed.
- 2026-05-13: Fixed local Compose health drift. `scripts/ops/validate-compose-health.ps1` now probes `$env:FRONTEND_HOST_PORT` or 3000; Redis healthcheck authenticates through `REDISCLI_AUTH` when `REDIS_PASSWORD` is present without printing the secret. Validation: PowerShell parser check passed; `docker compose config --quiet` passed with and without a throwaway `REDIS_PASSWORD`.
- 2026-05-13: Narrowed Kafka JSON deserialization trust. Shared `KafkaConsumerConfig` now defaults trusted packages to `java.lang,java.util,com.legent.kafka.model`, keeps default value type `EventEnvelope`, and disables type headers via property-backed defaults. Six service YAML wildcard entries were narrowed to the same allowlist. Validation: `.\mvnw.cmd -pl shared/legent-kafka -am test` passed; wildcard/broad-trust scan returned no matches.
- 2026-05-13: Hardened shared tenant/workspace request context. `TenantFilter` now rejects workspace/environment header conflicts against JWT-populated `TenantContext`, preserves JWT workspace/environment when headers are absent, and still allows trusted headers when no authenticated context exists. Validation: `.\mvnw.cmd -pl shared/legent-security -am test` passed.
- 2026-05-13: Reduced resolved-audience Kafka payload risk. `AudienceResolutionConsumer` now publishes bounded `SEND_BATCH_SIZE` chunks with deterministic chunk metadata and last-chunk semantics. Validation: integrated Maven test for shared Kafka/security plus audience/delivery services passed.
- 2026-05-13: Reduced audience-service resolution materialization and suppression bypass risk. `AudienceResolutionConsumer` now pages all-subscriber resolution with keyset cursor reads, checks suppressions per fetched page, publishes bounded chunks in one pass with lookahead last-chunk semantics, and rethrows suppression/publish failures. `DeliverabilityServiceClient` now fails closed on malformed or failed suppression responses. Validation: `.\mvnw.cmd -pl shared/legent-common,shared/legent-security,shared/legent-kafka,services/audience-service,services/tracking-service,services/identity-service -am test` passed.
- 2026-05-13: Reduced Kafka hot-partition risk. `EventPublisher` now derives or requires non-tenant keys for configured high-volume topics and preserves tenant fallback for low-volume topics. Validation: integrated Maven test for shared Kafka/security plus audience/delivery services passed.
- 2026-05-13: Reduced delivery consumer silent-ack risk. `DeliveryEventConsumer` now rethrows unexpected send-request failures after logging and preserves context cleanup. Validation: integrated Maven test for shared Kafka/security plus audience/delivery services passed.
- 2026-05-13: Wired shared Kafka listener retry exhaustion to real DLQ publishing. `KafkaConsumerConfig` now uses `DeadLetterPublishingRecoverer` with declared central topic `kafka.dead-letter`, and Kafka consumers in automation, tracking, deliverability, campaign, audience, delivery, and platform rethrow unexpected processing failures after logging while preserving explicit validation/malformed-event drops. Validation: impacted Maven slices passed.
- 2026-05-13: Hardened authenticated analytics WebSocket routing/context. `/ws/analytics` requires authentication and tenant/workspace context, handshake can derive workspace from authenticated principal, refresh preserves workspace/environment claims, frontend sockets use API-base origin, and ingress CORS allows workspace/environment headers. Validation: focused security/tracking/identity Maven slice passed; frontend lint passed in Loop 2.
- 2026-05-13: Fixed production infra drift for release rendering. Production Kustomize pins all `legent/*` images to `1.0.2`, renders production ingress hosts only, sets `CLICKHOUSE_DB=legent_analytics`, and routes `/ws/analytics` to `tracking-service` through Nginx/Kubernetes ingress. Added `scripts/ops/validate-production-overlay.ps1` and wired it into `release-gate.ps1`. Validation: `validate-route-map.ps1`, `validate-production-overlay.ps1`, and release-gate route/Kustomize slice passed.
- 2026-05-13: Fixed final runtime health validation drift. `validate-compose-health.ps1` now resolves the frontend host port from live Compose publisher metadata before falling back to env/defaults, and `config/nginx/nginx.conf` no longer duplicates `proxy_http_version` in the `/ws/analytics` location. Validation: `validate-compose-health.ps1`, `docker compose run --rm --no-deps gateway nginx -t`, `docker compose config --quiet`, route validation, production overlay validation, and recent Compose log scan passed.
