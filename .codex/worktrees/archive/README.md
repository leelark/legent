# Worktree Archive

Closed worktrees are recorded here only as metadata. Do not store generated builds, logs, secrets, customer data, or bulky artifacts.

Archive a worktree when:
- the work is merged into the main workspace,
- the work is abandoned with a documented reason,
- the branch is superseded by a newer slice.
