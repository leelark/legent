import { get, post } from '@/lib/api-client';

export const coreApi = {
  listOrganizations: () => get<any[]>('/core/organizations'),
  createOrganization: (payload: any) => post('/core/organizations', payload),
  listBusinessUnits: () => get<any[]>('/core/business-units'),
  listBusinessUnitTree: () => get<any[]>('/core/business-units/tree'),
  createBusinessUnit: (payload: any) => post('/core/business-units', payload),
  listWorkspaces: () => get<any[]>('/core/workspaces'),
  createWorkspace: (payload: any) => post('/core/workspaces', payload),
  listTeams: () => get<any[]>('/core/teams'),
  createTeam: (payload: any) => post('/core/teams', payload),
  listDepartments: () => get<any[]>('/core/departments'),
  createDepartment: (payload: any) => post('/core/departments', payload),
  listMemberships: () => get<any[]>('/core/memberships'),
  createMembership: (payload: any) => post('/core/memberships', payload),
  listRoles: () => get<any[]>('/core/roles'),
  createRole: (payload: any) => post('/core/roles', payload),
  listPermissionGroups: () => get<any[]>('/core/permission-groups'),
  createPermissionGroup: (payload: any) => post('/core/permission-groups', payload),
  listRoleBindings: () => get<any[]>('/core/role-bindings'),
  createRoleBinding: (payload: any) => post('/core/role-bindings', payload),
  listAccessGrants: () => get<any[]>('/core/access-grants'),
  createAccessGrant: (payload: any) => post('/core/access-grants', payload),
  decideAccessGrant: (id: string, payload: any) => post(`/core/access-grants/${id}/decision`, payload),
  previewAccessPolicy: (principalId: string) => get<any>(`/core/access-policy/preview?principalId=${encodeURIComponent(principalId)}`),
  listIdentityProviders: () => get<any[]>('/core/identity-providers'),
  upsertIdentityProvider: (payload: any) => post('/core/identity-providers', payload),
  listScimTokens: () => get<any[]>('/core/scim-tokens'),
  createScimToken: (payload: any) => post('/core/scim-tokens', payload),
  listInvitations: () => get<any[]>('/core/invitations'),
  createInvitation: (payload: any) => post('/core/invitations', payload),
  acceptInvitation: (payload: any) => post('/core/invitations/accept', payload),
  listQuotas: () => get<any[]>('/core/quotas'),
  upsertQuota: (payload: any) => post('/core/quotas', payload),
  listUsage: () => get<any[]>('/core/usage'),
  incrementUsage: (payload: any) => post('/core/usage/increment', payload),
  listSubscriptions: () => get<any[]>('/core/subscriptions'),
  createSubscription: (payload: any) => post('/core/subscriptions', payload),
  listEnvironments: () => get<any[]>('/core/environments'),
  createEnvironment: (payload: any) => post('/core/environments', payload),
  lockEnvironment: (payload: any) => post('/core/environments/locks', payload),
  listPromotions: () => get<any[]>('/core/promotions'),
  createPromotion: (payload: any) => post('/core/promotions', payload),
  decidePromotion: (id: string, payload: any) => post(`/core/promotions/${id}/decision`, payload),
  listFeatureControls: () => get<any[]>('/core/feature-controls'),
  upsertFeatureControl: (payload: any) => post('/core/feature-controls', payload),
  listAuditEvents: (workspaceId?: string, action?: string) =>
    get<any[]>(`/core/audit${workspaceId || action ? `?${new URLSearchParams({ ...(workspaceId ? { workspaceId } : {}), ...(action ? { action } : {}) }).toString()}` : ''}`),
};
