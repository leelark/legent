apiVersion: apps/v1
kind: Deployment
metadata:
  name: deliverability-service
  namespace: legent
  labels:
    app: deliverability-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: deliverability-service
  template:
    metadata:
      labels:
        app: deliverability-service
        tier: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8087"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: deliverability-service
          image: legent/deliverability-service:latest
          ports:
            - containerPort: 8087
          envFrom:
            - configMapRef:
                name: legent-config
            - secretRef:
                name: legent-secrets
          env:
            - name: DB_NAME
              value: "legent_deliverability"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8087
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8087
            initialDelaySeconds: 20
            periodSeconds: 10
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8087
            failureThreshold: 30
            periodSeconds: 5
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: deliverability-service
  namespace: legent
  labels:
    app: deliverability-service
spec:
  type: ClusterIP
  ports:
    - port: 8087
      targetPort: 8087
      protocol: TCP
  selector:
    app: deliverability-service
