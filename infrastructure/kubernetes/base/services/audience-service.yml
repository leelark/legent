apiVersion: apps/v1
kind: Deployment
metadata:
  name: audience-service
  namespace: legent
  labels:
    app: audience-service
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: audience-service
  template:
    metadata:
      labels:
        app: audience-service
        tier: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8082"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: audience-service
          image: legent/audience-service:latest
          ports:
            - containerPort: 8082
          envFrom:
            - configMapRef:
                name: legent-config
            - secretRef:
                name: legent-secrets
          env:
            - name: DB_NAME
              value: "legent_audience"
            - name: MINIO_BUCKET
              value: "legent-imports"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8082
            initialDelaySeconds: 20
            periodSeconds: 10
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            failureThreshold: 30
            periodSeconds: 5
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: audience-service
  namespace: legent
  labels:
    app: audience-service
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
  selector:
    app: audience-service
